/* ==========================================================================
   data/claude-extra.js — Claude Skills, segunda leva

   Mesmo formato claude-skill de data/claude-skills.js. Separado só para
   nenhum arquivo de dado ficar grande demais para ler de uma vez.
   ========================================================================== */

window.SKILL_HUB_CATALOG = (window.SKILL_HUB_CATALOG || []).concat([
  {
    id: 'mobile-release-checker',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Mobile Release Checker',
    description: 'Confere o que costuma reprovar uma build móvel antes de submeter à loja.',
    whenToUse: 'Use antes de submeter uma versão para App Store ou Play Store.',
    icon: 'rocket',
    category: 'mobile',
    tags: ['mobile', 'release', 'loja'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-27',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: ['Write'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools somente de leitura, e nega escrita.'],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: mobile-release-checker',
      'description: "Check the things that usually get a mobile build rejected or rolled back. Use before submitting a version to a store."',
      'when_to_use: "A release build is about to be submitted or promoted."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      'disallowed-tools:',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Catch the release problems that cost a review cycle.',
      '',
      '## Checklist',
      '',
      '- **Version and build number** incremented, and consistent between the',
      '  project files and the store metadata.',
      '- **Permissions**: every declared permission has a usage description and is',
      '  actually used. An unused permission is both a rejection risk and a',
      '  privacy claim you cannot defend.',
      '- **Endpoints**: no staging or localhost URL reachable in the release build.',
      '- **Secrets**: no API key committed or embedded in the bundle. A key in a',
      '  mobile binary is public — report it as such.',
      '- **Debug surface**: logging, developer menus and test accounts disabled.',
      '- **Minimum OS version** matches what the code actually requires.',
      '- **Assets**: icons and splash screens present at every required size.',
      '- **Offline and permission-denied paths** render something, not a blank',
      '  screen — these are the two most common review failures.',
      '- **Deep links** declared in the manifest resolve to a real destination.',
      '',
      '## Report',
      '',
      'Blocking issues first, then risks, each with the file. Say what you could',
      'not verify from the repository alone.',
      ''
    ].join('\n')
  },
  {
    id: 'cloud-cost-reviewer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Cloud Cost Reviewer',
    description: 'Lê a infraestrutura como código e aponta o que gasta sem entregar valor.',
    whenToUse: 'Use quando a fatura de cloud cresceu e ninguém sabe explicar por quê.',
    icon: 'cloud',
    category: 'cloud',
    tags: ['cloud', 'custo', 'infraestrutura'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'Apache-2.0',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-02',
    featured: true,
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: ['Bash', 'Write'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools somente de leitura, e nega Bash e escrita.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: cloud-cost-reviewer',
      'description: "Read infrastructure as code and point out what costs money without delivering value. Use when the cloud bill grew unexplained."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      'disallowed-tools:',
      '  - Bash',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Name the resources that cost money and say what each one is for.',
      '',
      '## What to look for',
      '',
      '- **Always-on for bursty work** — a provisioned instance where the workload',
      '  is a few minutes a day.',
      '- **No retention policy** — logs, backups, snapshots and object storage',
      '  growing forever. This is the usual answer to "why did the bill grow".',
      '- **Orphans** — a volume with no instance, an address with no attachment, a',
      '  load balancer with no target, an environment nobody named.',
      '- **Oversized by default** — an instance class or a database tier chosen',
      '  once and never revisited.',
      '- **Cross-zone or cross-region traffic** between components that could sit',
      '  together. Egress is the cost people forget.',
      '- **Duplicate environments** kept warm for a demo that already happened.',
      '- **Per-request cost an attacker controls** — an unauthenticated endpoint',
      '  that triggers paid work.',
      '',
      '## Output',
      '',
      'A table of resources: what it is, what it is for, and whether the code',
      'proves it is used. Then three lists — **remove**, **resize**, **keep** — each',
      'item with the file and line that declares it.',
      '',
      '## Limits',
      '',
      '- No spend figures unless the repository contains them. This reads code, not',
      '  a billing account.',
      '- Never delete or apply anything. Output the change and let a human run it.',
      ''
    ].join('\n')
  },
  {
    id: 'ci-pipeline-auditor',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'CI Pipeline Auditor',
    description: 'Audita o pipeline por lentidão, passo inútil e permissão exagerada.',
    whenToUse: 'Use quando o CI demora, falha sem motivo ou tem acesso demais.',
    icon: 'refresh',
    category: 'automation',
    tags: ['ci', 'automação', 'pipeline'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-15',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: ['Write'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools somente de leitura, e nega escrita.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: ci-pipeline-auditor',
      'description: "Audit the CI pipeline for slowness, useless steps and excessive permissions. Use when CI is slow, flaky or over-privileged."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      'disallowed-tools:',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'A pipeline that is fast, honest about failure, and holds only the access it',
      'needs.',
      '',
      '## Speed',
      '',
      '- What runs on every commit that only needs to run on the default branch?',
      '- Are independent jobs actually parallel, or chained by an unnecessary',
      '  dependency?',
      '- Is the dependency cache keyed on the lockfile? A cache that never hits is',
      '  slower than no cache.',
      '- Is the same build produced more than once across jobs?',
      '',
      '## Honesty',
      '',
      '- Any step with a failure that is ignored — a `continue-on-error`, a piped',
      '  exit code, a test command whose result nobody reads. A green pipeline that',
      '  cannot go red is the worst finding here.',
      '- Retries that hide flakiness instead of reporting it.',
      '- A required check that is not actually required by branch protection.',
      '',
      '## Permissions',
      '',
      '- The default token scope: read where write is not needed.',
      '- Secrets exposed to jobs that do not use them, and to steps that run',
      '  untrusted code from a fork.',
      '- Third-party actions pinned to a moving tag rather than a commit.',
      '- A deploy that can be triggered by anything other than the intended event.',
      '',
      '## Report',
      '',
      'Findings by impact: broken guarantees first, then permissions, then time.',
      'Each with the workflow file and the step name.',
      ''
    ].join('\n')
  },
  {
    id: 'feature-flag-auditor',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Feature Flag Auditor',
    description: 'Lista flags que já deveriam ter saído e o código morto que sustentam.',
    whenToUse: 'Use quando as flags acumularam e ninguém sabe quais ainda importam.',
    icon: 'sliders',
    category: 'development',
    tags: ['feature flags', 'limpeza', 'rollout'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-07-09',
    tools: { allowed: ['Read', 'Grep', 'Glob', 'Bash(git log *)'], disallowed: ['Write'] },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: ['Declara allowed-tools.', 'Pré-aprova Bash restrito a leitura do histórico do Git.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: feature-flag-auditor',
      'description: "List flags that should already be gone and the dead code they hold up. Use when flags have accumulated."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      '  - "Bash(git log *)"',
      'disallowed-tools:',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Every flag either has a reason to exist today or a removal plan.',
      '',
      '## Per flag',
      '',
      '1. **Age** — when was it added? Read the commit.',
      '2. **Purpose** — rollout, kill switch, experiment, or permanent',
      '   configuration. These have different lifetimes; a rollout flag older than',
      '   its rollout is debt.',
      '3. **Current value** in each environment, if the repository declares it.',
      '4. **Branches** — how many code paths depend on it, and whether both are',
      '   still tested.',
      '5. **Nesting** — flags read inside another flag branch. Two flags produce',
      '   four paths; three produce eight. Nobody tests eight.',
      '',
      '## Output',
      '',
      'Three lists:',
      '',
      '- **Remove** — constant value since it was added; name the branch to delete',
      '  and the tests that go with it.',
      '- **Keep** — with the one-line reason and the expected lifetime.',
      '- **Decide** — nobody can tell what it is for. These need a human, not a',
      '  guess.',
      '',
      '## Limits',
      '',
      '- Read-only. Removing a flag is a behavior change; propose, do not apply.',
      '- A flag read through a string-built key may not show in a plain search;',
      '  flag that uncertainty explicitly.',
      ''
    ].join('\n')
  },
  {
    id: 'observability-reviewer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Observability Reviewer',
    description: 'Verifica se dá para diagnosticar um incidente com o que o sistema emite hoje.',
    whenToUse: 'Use quando um incidente foi difícil de diagnosticar por falta de sinal.',
    icon: 'chart',
    category: 'devops',
    tags: ['observabilidade', 'métricas', 'logs'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-04',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools, porém somente de leitura.'],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: observability-reviewer',
      'description: "Check whether an incident can be diagnosed from what the system emits today. Use after an incident that was hard to diagnose."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      '---',
      '',
      '# Goal',
      '',
      'Answer one question: at 3am, with only what this system emits, can someone',
      'tell what is broken?',
      '',
      '## Work backwards from the questions',
      '',
      'For each of these, find the signal that answers it — or record that none',
      'exists:',
      '',
      '- Is it broken for everyone, or for some?',
      '- Which dependency is slow or failing?',
      '- Did it start with a deploy, and which one?',
      '- Is the queue draining or growing?',
      '- Which request failed, and can we follow it end to end?',
      '',
      '## Then check the signals themselves',
      '',
      '- **Correlation** — is there a request id that survives across services? A',
      '  log without one cannot be joined to anything.',
      '- **Cardinality** — a label with unbounded values (a user id, a URL with an',
      '  id in it) makes a metric unusable and expensive.',
      '- **Error paths** — a caught exception that logs nothing is invisible.',
      '- **Latency** — percentiles, not averages. An average hides the failure.',
      '- **Alerts** — does each one map to an action? An alert nobody acts on gets',
      '  ignored, and takes the real ones with it.',
      '- **Privacy** — no personal data, tokens or full bodies in logs.',
      '',
      '## Report',
      '',
      'The unanswerable questions first — those are the gaps that cost hours. Then',
      'the signals to add, each with the file where it belongs.',
      ''
    ].join('\n')
  }
]);
