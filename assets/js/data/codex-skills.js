/* ==========================================================================
   data/codex-skills.js — coleção Codex Skills

   Dois formatos do Codex, sem inventar campos:

   - codex-agents ... AGENTS.md, Markdown puro, SEM frontmatter. Carregado
     automaticamente pelo Codex no diretório onde está e abaixo dele.
   - codex-prompt ... ~/.codex/prompts/<nome>.md, frontmatter limitado a
     description e argument-hint. Placeholders $1..$9 e $ARGUMENTS.

   O modelo NÃO é campo de arquivo no Codex: vive em ~/.codex/config.toml,
   em --model ou em /model. Nenhuma entrada aqui declara modelo.
   ========================================================================== */

window.SKILL_HUB_CATALOG = (window.SKILL_HUB_CATALOG || []).concat([
  {
    id: 'codex-repo-conventions',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Repo Conventions',
    description: 'AGENTS.md com as convenções que o Codex deve seguir no repositório.',
    whenToUse: 'Use como AGENTS.md do raiz para o Codex conhecer estrutura, comandos e limites do projeto.',
    icon: 'book',
    category: 'development',
    tags: ['agents.md', 'convenções', 'onboarding'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: 'https://developers.openai.com/codex/guides/agents-md',
    updatedAt: '2026-09-04',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex: vale para toda sessão no diretório.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Repository guide for agents',
      '',
      'This file is loaded automatically by Codex for every session in this',
      'directory and below. Keep it short enough that it is worth reading every',
      'time, and concrete enough to change behaviour.',
      '',
      '## Working agreements',
      '',
      '- Prefer the smallest change that solves the stated problem.',
      '- Do not add a dependency without saying, in the PR body, what it replaces.',
      '- Never commit directly to `main`; branch from it.',
      '- If a task is ambiguous, state the assumption in the response and continue.',
      '',
      '## Repository expectations',
      '',
      '- Source lives in `src/`. Tests live next to the code as `*.test.ts`.',
      '- Install with `npm ci`, never `npm install`, so the lockfile is honoured.',
      '- Run `npm run verify` before proposing a change: it runs lint, types, tests.',
      '- Generated files are listed in `.gitattributes` as `linguist-generated`.',
      '  Do not hand-edit them.',
      '',
      '## Commit conventions',
      '',
      '- Conventional commits: `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`.',
      '- One logical change per commit. Renames go in their own commit.',
      '- The body explains **why**; the diff already shows what.',
      '',
      '## Security constraints',
      '',
      '- Never read or print the contents of `.env*`.',
      '- Never add a network call to a host that is not already used in the repo.',
      '- Secrets come from the platform, never from a committed file.',
      '',
      '## Deployment',
      '',
      '- Deploys are triggered by tagging `v*` on `main`, never by pushing.',
      '- A deploy requires a green `verify` run and a human approval.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-review-rules',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Code Review Rules',
    description: 'AGENTS.md que fixa o padrão de revisão de código do time.',
    whenToUse: 'Use num subdiretório ou no raiz para o Codex revisar sempre com o mesmo critério.',
    icon: 'shield',
    category: 'development',
    tags: ['agents.md', 'review', 'qualidade'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-27',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex no diretório onde estiver.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Code review rules',
      '',
      'Applies to every review in this directory tree. A review that skips a',
      'section below is incomplete.',
      '',
      '## Order of attention',
      '',
      '1. **Correctness** — wrong condition, off-by-one, unhandled error, lost',
      '   `await`, changed contract, missing cache invalidation.',
      '2. **Data safety** — a migration without rollback, a delete without a guard,',
      '   a query without a limit.',
      '3. **Reuse** — logic that already exists in the repo under another name.',
      '4. **Clarity** — a name that lies, an abstraction used once.',
      '',
      '## What a finding must contain',
      '',
      '- The file and line range.',
      '- The failure scenario: concrete inputs, then the wrong output or crash.',
      '- The smallest fix that removes it.',
      '',
      'A remark that cannot be written in that shape is a suggestion, not a',
      'finding, and goes in a separate list.',
      '',
      '## What not to report',
      '',
      '- Formatting that the formatter already owns.',
      '- Preferences with no stated consequence.',
      '- Speculation about performance without a measurement.',
      '',
      '## Tests',
      '',
      '- A bug fix without a regression test is an incomplete fix.',
      '- A test that cannot fail is not a test; say so.',
      '- Never state that the suite passes without having run it.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-type-safety',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Type Safety Standards',
    description: 'AGENTS.md que define onde a tipagem precisa ser estrita e por quê.',
    whenToUse: 'Use no diretório de código tipado para o Codex endurecer tipos nas fronteiras certas.',
    icon: 'lock',
    category: 'development',
    tags: ['agents.md', 'tipos', 'typescript'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-07-11',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex no diretório onde estiver.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Type safety standards',
      '',
      '## Where strictness pays off',
      '',
      'Type the **boundaries** first and let inference flow inward:',
      '',
      '- request handlers and their body/query parsing',
      '- responses from other services',
      '- reads from storage and from the message queue',
      '- anything parsed from a file or an environment variable',
      '',
      'Internal helpers rarely need explicit annotations. If inference gets it',
      'right, leave it alone.',
      '',
      '## Rules',
      '',
      '- No `any` at a boundary. Parse into a validated shape instead.',
      '- Never silence an error with a cast to satisfy the compiler. Fix the shape',
      '  or narrow explicitly and comment why.',
      '- Prefer a narrow union over an optional field when the states are mutually',
      '  exclusive.',
      '- A type that needs a comment to be understood is the wrong type.',
      '',
      '## When tightening existing code',
      '',
      '1. Start where untyped values enter the system, not where errors are loudest.',
      '2. Change one boundary per commit so the blast radius stays readable.',
      '3. Report the remaining escape hatches instead of hiding them.',
      ''
    ].join('\n')
  },
  {
    id: 'scaffold-service',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Scaffold Service',
    description: 'Prompt que monta um serviço novo com estrutura, config e um teste de fumaça.',
    whenToUse: 'Use ao iniciar um serviço e querer estrutura coerente em vez de improviso.',
    icon: 'package',
    category: 'development',
    tags: ['scaffolding', 'setup', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: 'https://developers.openai.com/codex/custom-prompts',
    updatedAt: '2026-08-24',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'O formato de prompt do Codex não declara ferramentas nem permissões.',
      'O corpo pede comandos, mas quem os aprova é você na sessão.'
    ],
    files: [{ path: 'scaffold-service.md' }],
    content: [
      '---',
      'description: Scaffold a new service with sane defaults and one smoke test',
      'argument-hint: NAME=<service-name> RUNTIME=<node|python|go>',
      '---',
      '',
      '# Scaffold $NAME',
      '',
      'Create a project skeleton for a service called `$NAME` targeting `$RUNTIME`.',
      '',
      '## Before writing anything',
      '',
      'Confirm the package manager and the test runner already used in this repo.',
      'If the repo has a sibling service, mirror its layout instead of inventing a',
      'new one.',
      '',
      '## Order of work',
      '',
      '1. Directory layout and entry point.',
      '2. Configuration: read from the environment, fail loudly when a required',
      '   variable is missing.',
      '3. One health endpoint or CLI entry that proves the process boots.',
      '4. Exactly one smoke test that **fails** if the app cannot start.',
      '5. Scripts for run, build, and test, named as in the sibling services.',
      '',
      '## Constraints',
      '',
      '- Pin dependency versions exactly.',
      '- No dependency without a one-line justification in the output.',
      '- Do not add a framework the repo does not already use.',
      '- Stop and report if `$NAME` already exists.',
      ''
    ].join('\n')
  },
  {
    id: 'write-tests',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Write Tests',
    description: 'Prompt que escreve testes para o comportamento que já existe, sem inventar.',
    whenToUse: 'Use quando um módulo não tem cobertura e você quer travar regressões.',
    icon: 'flask',
    category: 'testing',
    tags: ['testes', 'cobertura', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-19',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'O formato de prompt do Codex não declara ferramentas nem permissões.'
    ],
    files: [{ path: 'write-tests.md' }],
    content: [
      '---',
      'description: Write tests that lock in the current behavior of a module',
      'argument-hint: FILE=<path-to-module>',
      '---',
      '',
      '# Test $FILE',
      '',
      'Write tests for the behavior `$FILE` has **today**, not the behavior it',
      'should have. If something looks wrong, report it separately instead of',
      'encoding the fix into a test.',
      '',
      '## Workflow',
      '',
      '1. Read the module and list its observable behaviors: return values, thrown',
      '   errors, side effects, and edge cases at the boundaries of each input.',
      '2. For each behavior, write the smallest test that would catch a regression.',
      '3. Run the suite and report the real output — pass or fail, verbatim.',
      '4. List the behaviors you chose not to cover, and why.',
      '',
      '## Rules',
      '',
      '- Assert on outcomes, never on the order of internal calls.',
      '- One reason to fail per test.',
      '- A test that cannot fail is not a test. Delete it and say so.',
      '- Use the test runner and assertion style already present in the repo.',
      ''
    ].join('\n')
  },
  {
    id: 'containerize',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Containerize',
    description: 'Prompt que escreve Dockerfile multi-stage enxuto e mede o resultado.',
    whenToUse: 'Use ao containerizar um serviço ou reduzir uma imagem que ficou grande.',
    icon: 'cloud',
    category: 'devops',
    tags: ['docker', 'build', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'Apache-2.0',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-07-30',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'O formato de prompt do Codex não declara ferramentas nem permissões.',
      'O corpo pede um build, que você aprova na sessão.'
    ],
    files: [{ path: 'containerize.md' }],
    content: [
      '---',
      'description: Write a lean multi-stage Dockerfile and report the image size',
      'argument-hint: TARGET=<service-dir>',
      '---',
      '',
      '# Containerize $TARGET',
      '',
      '## Workflow',
      '',
      '1. Detect runtime, package manager, and build output from `$TARGET`.',
      '2. Write a multi-stage Dockerfile. The final stage carries the runtime and',
      '   the build artifact — no compilers, no package manager caches, no tests.',
      '3. Write `.dockerignore` **before** the first build, or the build context',
      '   will ship `node_modules` and `.git`.',
      '4. Build once, then report the image size and the three largest layers.',
      '',
      '## Constraints',
      '',
      '- Pin the base image by digest when reproducibility matters; say which you',
      '  chose and why.',
      '- Run as a non-root user in the final stage.',
      '- Never bake a secret into a layer — build args are visible in history.',
      '- Report the before/after size. A change with no measurement is not a',
      '  reduction.',
      ''
    ].join('\n')
  },
  {
    id: 'safe-refactor',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Safe Refactor',
    description: 'Prompt que refatora em passos pequenos, preservando comportamento observável.',
    whenToUse: 'Use quando um módulo ficou difícil de mudar e você quer refatorar sem quebrar.',
    icon: 'code',
    category: 'development',
    tags: ['refatoração', 'legado', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-06-27',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'O formato de prompt do Codex não declara ferramentas nem permissões.'
    ],
    files: [{ path: 'safe-refactor.md' }],
    content: [
      '---',
      'description: Refactor in small behavior-preserving steps, verifying between each',
      'argument-hint: FILE=<path> GOAL=<what-should-improve>',
      '---',
      '',
      '# Refactor $FILE',
      '',
      'Goal: $GOAL. Behavior must not change.',
      '',
      '## Establish the net first',
      '',
      'Find or write tests around the current behavior. If the module cannot be',
      'tested as it stands, the first step is the smallest seam that makes it',
      'testable — and nothing else.',
      '',
      '## Then, one step at a time',
      '',
      '1. Make a single structural change.',
      '2. Run the tests.',
      '3. Commit before starting the next step.',
      '',
      'Renames go in their own commit, separate from logic changes, so a reviewer',
      'can read the diff.',
      '',
      '## Stop conditions',
      '',
      '- A step requires changing behavior → stop and report it instead.',
      '- Tests go red and the cause is not obvious → revert the step, do not patch',
      '  forward.',
      '',
      '## Preferences',
      '',
      '- Deleting code beats adding an abstraction used once.',
      '- Every step leaves the project green.',
      ''
    ].join('\n')
  }
]);
