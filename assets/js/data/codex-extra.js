/* ==========================================================================
   data/codex-extra.js — Codex Skills, segunda leva

   Mesmos dois formatos de data/codex-skills.js, separados só para nenhum
   arquivo de dado ficar grande demais para ler de uma vez.
   ========================================================================== */

window.SKILL_HUB_CATALOG = (window.SKILL_HUB_CATALOG || []).concat([
  {
    id: 'codex-frontend-standards',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Frontend Standards',
    description: 'AGENTS.md com as regras de componente, estado e acessibilidade do frontend.',
    whenToUse: 'Use no diretório de UI para o Codex seguir sempre o mesmo padrão de componente.',
    icon: 'panel',
    category: 'frontend',
    tags: ['agents.md', 'ui', 'componentes'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: 'https://developers.openai.com/codex/guides/agents-md',
    updatedAt: '2026-09-05',
    featured: true,
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex no diretório onde estiver.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Frontend standards',
      '',
      'Applies to every change under this directory.',
      '',
      '## Component rules',
      '',
      '- One component per file, named after the file.',
      '- Props in, events out. A component never reaches into global state to',
      '  discover its own inputs.',
      '- No conditional rendering nested more than two levels; extract instead.',
      '- Styling lives in the stylesheet, not in inline style attributes. The',
      '  exception is a value computed at runtime, such as a bar width.',
      '',
      '## State rules',
      '',
      '- One source of truth per fact. Never keep the same value in two places',
      '  synced by hand.',
      '- Never store what can be derived. A filtered list is computed, not cached.',
      '- The DOM is not state: read values from what rendered them.',
      '',
      '## Accessibility, non-negotiable',
      '',
      '- Real `button` for actions, real `a[href]` for navigation.',
      '- Every control has an accessible name; icon-only controls get an explicit',
      '  label.',
      '- Focus stays visible. Never remove the outline without a replacement.',
      '- Status and error never rely on colour alone.',
      '- Motion respects `prefers-reduced-motion`.',
      '',
      '## Before proposing a change',
      '',
      '- Run the component in the browser, not only in a test.',
      '- Check the layout at 768px and 1440px.',
      '- Leave the console clean; a warning is a finding.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-backend-standards',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Backend Standards',
    description: 'AGENTS.md com as regras de serviço, transação, erro e log do backend.',
    whenToUse: 'Use no diretório de serviços para o Codex respeitar as fronteiras do backend.',
    icon: 'server',
    category: 'backend',
    tags: ['agents.md', 'serviços', 'transações'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-29',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex no diretório onde estiver.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Backend standards',
      '',
      '## Layers',
      '',
      '- **Handler** — parses input, calls one use case, formats the response. No',
      '  business rules, no direct database access.',
      '- **Use case** — the rule. Knows nothing about HTTP.',
      '- **Repository** — the only place that talks to the database.',
      '',
      'A handler that queries the database directly is a finding, not a shortcut.',
      '',
      '## Input',
      '',
      '- Validate at the boundary and pass a validated shape inward.',
      '- Reject unknown fields rather than ignoring them silently.',
      '- Never trust a client-supplied id without an ownership check.',
      '',
      '## Transactions',
      '',
      '- One transaction per use case, opened as late and closed as early as',
      '  possible.',
      '- Never hold a transaction across a network call to another service.',
      '- Every write path states whether it is idempotent. If it is not, it does not',
      '  get retried.',
      '',
      '## Errors',
      '',
      '- Expected failures (validation, not found, conflict) map to status codes.',
      '  Unexpected ones become 500 and are logged with the cause preserved.',
      '- No stack trace, SQL or file path reaches the client.',
      '- An empty catch block is a bug unless a comment explains why.',
      '',
      '## Logs',
      '',
      '- Structured, with a request id, at the boundaries of each use case.',
      '- Never log secrets, tokens or full request bodies.',
      '- A log line that cannot be acted on is noise; drop it.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-testing-standards',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Testing Standards',
    description: 'AGENTS.md que define o que testar, em que nível e o que nunca mockar.',
    whenToUse: 'Use no raiz para o Codex escrever testes no nível certo, sem inflar a suíte.',
    icon: 'flask',
    category: 'testing',
    tags: ['agents.md', 'testes', 'mocks'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-08-16',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex no diretório onde estiver.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Testing standards',
      '',
      '## Level',
      '',
      '- **Unit** for a rule with branches. Fast, no I/O.',
      '- **Integration** for anything crossing a boundary: database, queue, HTTP.',
      '  One per path, not one per field.',
      '- **End to end** only for the flows the business cannot lose.',
      '',
      'A test written at the wrong level is worse than no test: it passes while the',
      'real path breaks.',
      '',
      '## Rules',
      '',
      '- One reason to fail per test.',
      '- Assert on outcomes, never on the order of internal calls.',
      '- Table-driven when inputs vary and the assertion does not.',
      '- The assertion names the wrong value; `should not throw` asserts nothing.',
      '',
      '## Mocks',
      '',
      '- Never mock the thing under test.',
      '- Never mock the database in an integration test — that is what makes it an',
      '  integration test.',
      '- Mock a third-party HTTP call, and keep the recorded shape next to the test.',
      '',
      '## Determinism',
      '',
      '- No real clock, no real network, no dependence on test order.',
      '- A flaky test is a bug report about the code, not about the test runner.',
      '  Never fix it with a longer timeout or a retry.',
      '',
      '## Reporting',
      '',
      '- Never state that the suite passes without having run it.',
      '- Report the real output, verbatim, pass or fail.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-security-rules',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Security Rules',
    description: 'AGENTS.md com os limites de segredo, dependência, rede e dado sensível.',
    whenToUse: 'Use no raiz para o Codex não vazar segredo nem abrir superfície nova sem aviso.',
    icon: 'shield',
    category: 'security',
    tags: ['agents.md', 'segurança', 'segredos'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-07',
    featured: true,
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex no diretório onde estiver.',
      'Restringe comportamento, não amplia permissão.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Security rules',
      '',
      'These are limits, not suggestions. A change that crosses one is reported',
      'instead of made.',
      '',
      '## Secrets',
      '',
      '- Never read, print or copy the contents of `.env*`, key files or credential',
      '  stores. If a secret appears in a file, report the location, not the value.',
      '- Never commit a value that looks like a credential, even in a test fixture.',
      '- Secrets come from the platform at runtime. Nothing is baked into an image,',
      '  a build arg, or a client bundle.',
      '',
      '## Network',
      '',
      '- Do not add a call to a host that is not already used in this repository.',
      '  A new host needs a human decision.',
      '- Every outbound call has a timeout. A call without one is a finding.',
      '',
      '## Dependencies',
      '',
      '- A new dependency requires a one-line reason and what it replaces.',
      '- Never add one that runs code at install time.',
      '- Prefer the platform over a package for anything trivial.',
      '',
      '## Data',
      '',
      '- Never log personal data or full request bodies.',
      '- Never widen a query to make an authorization check pass.',
      '- A delete or bulk update needs a `WHERE` you can defend and a rollback.',
      '',
      '## Input',
      '',
      '- Treat every external string as hostile: user input, webhook payloads, file',
      '  contents, model output.',
      '- Never build a shell command or a query by concatenating one in.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-python-standards',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Python Standards',
    description: 'AGENTS.md com estrutura, tipagem, dependências e testes de um projeto Python.',
    whenToUse: 'Use no raiz de um projeto Python para o Codex seguir o mesmo padrão do time.',
    icon: 'code',
    category: 'development',
    tags: ['agents.md', 'python', 'convenções'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-07-15',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex no diretório onde estiver.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Python project standards',
      '',
      '## Layout',
      '',
      '- Package under `src/<package>/`, tests under `tests/` mirroring it.',
      '- No code at import time beyond definitions. Side effects live behind a',
      '  function, so importing a module is always safe.',
      '- `__init__.py` re-exports the public surface and nothing else.',
      '',
      '## Environment',
      '',
      '- Dependencies are declared in `pyproject.toml`; the lockfile is authoritative.',
      '- Never install into the system interpreter. Use the project environment.',
      '- Pin the Python version in the project files, not in a comment.',
      '',
      '## Typing',
      '',
      '- Public functions are annotated. Internal helpers only where inference fails.',
      '- Prefer a dataclass or a typed dict over a bare dict passed between layers.',
      '- Never use a broad `# type: ignore` to silence an error; narrow it or fix the',
      '  shape.',
      '',
      '## Errors',
      '',
      '- Raise a specific exception; never `except Exception: pass`.',
      '- Chain with `raise ... from err` so the cause survives.',
      '- A retry only wraps an idempotent call.',
      '',
      '## Tests',
      '',
      '- `pytest`, one reason to fail per test, fixtures over setup functions.',
      '- No network in unit tests. No `sleep` standing in for a condition.',
      '- Run the suite and report the real output before claiming anything passes.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-monorepo-map',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-agents',
    name: 'Monorepo Map',
    description: 'AGENTS.md que descreve fronteiras de pacote e quem pode importar quem.',
    whenToUse: 'Use no raiz de um monorepo para o Codex não criar dependência cruzada indevida.',
    icon: 'layers',
    category: 'architecture',
    tags: ['agents.md', 'monorepo', 'fronteiras'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'Apache-2.0',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-19',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false, autoLoaded: true },
    risk: 'low',
    riskFactors: [
      'Markdown puro, sem frontmatter e sem declaração de ferramentas.',
      'Carregado automaticamente pelo Codex no diretório onde estiver.'
    ],
    files: [{ path: 'AGENTS.md' }],
    content: [
      '# Monorepo map',
      '',
      '## Packages',
      '',
      '- `packages/core` — domain rules. Imports nothing from this repo.',
      '- `packages/data` — storage and queries. May import `core`.',
      '- `packages/api` — HTTP surface. May import `core` and `data`.',
      '- `apps/web` — the interface. May import `core` and the API client only.',
      '- `packages/ui` — presentational components. Imports nothing but `core` types.',
      '',
      '## Import rules',
      '',
      '- Dependencies point **inward**, toward `core`. An import that goes the other',
      '  way is the finding; do not add a re-export to hide it.',
      '- Always import through a package entry point. A deep path into another',
      '  package is not a public API and breaks on any internal move.',
      '- Two packages needing the same helper means it belongs in `core`, not copied.',
      '',
      '## Adding a package',
      '',
      '1. Say which existing package it is being extracted from.',
      '2. Declare its allowed dependencies in this file, in the same shape as above.',
      '3. Move code and tests together, in a commit that changes nothing else.',
      '',
      '## Changing a shared package',
      '',
      '- List the dependent packages before editing; a change in `core` reaches all',
      '  of them.',
      '- A breaking change to a package entry point needs a migration note in the PR.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-fix-failing-test',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Fix Failing Test',
    description: 'Prompt que investiga um teste vermelho e corrige a causa, não o teste.',
    whenToUse: 'Use quando a suíte quebra e você quer a causa, não um assert ajustado.',
    icon: 'bug',
    category: 'testing',
    tags: ['testes', 'depuração', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: 'https://developers.openai.com/codex/guides/prompts',
    updatedAt: '2026-09-06',
    featured: true,
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'Frontmatter limitado a description e argument-hint.',
      'Nenhuma ferramenta declarada: o Codex usa a permissão da sessão.'
    ],
    files: [{ path: 'fix-failing-test.md' }],
    content: [
      '---',
      'description: Investigate a failing test and fix the cause, not the assertion',
      'argument-hint: <test name or file>',
      '---',
      '',
      '# Fix the failing test: $1',
      '',
      '## Order of work',
      '',
      '1. Run the test and read the **actual** failure output. Do not work from the',
      '   test name.',
      '2. State in one sentence what the test expects and what it got.',
      '3. Decide which is wrong — the code or the expectation — and say why. This is',
      '   the whole decision; everything after it is mechanical.',
      '4. If the code is wrong, fix the code. If the expectation is wrong, explain',
      '   what changed in the requirements before touching it.',
      '5. Re-run the single test, then the full suite.',
      '',
      '## Forbidden',
      '',
      '- Changing an assertion so it matches current output, without step 3.',
      '- Adding a retry, a longer timeout, or a skip.',
      '- Deleting the test.',
      '- Mocking away the thing that fails.',
      '',
      '## Report',
      '',
      '- The failure output, before and after.',
      '- The cause, in one sentence.',
      '- Anything else the same cause could be breaking.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-explain-diff',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Explain Diff',
    description: 'Prompt que explica um diff em linguagem de revisor, com riscos e o que testar.',
    whenToUse: 'Use ao herdar um PR grande e precisar entender rápido o que ele faz.',
    icon: 'git-branch',
    category: 'development',
    tags: ['diff', 'review', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-24',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'Frontmatter limitado a description e argument-hint.',
      'Nenhuma ferramenta declarada: o Codex usa a permissão da sessão.'
    ],
    files: [{ path: 'explain-diff.md' }],
    content: [
      '---',
      'description: Explain what a diff does, what it risks, and what to test',
      'argument-hint: [base-ref]',
      '---',
      '',
      '# Explain the diff against $1',
      '',
      'Write for someone who has to approve this today and did not write it.',
      '',
      '## Sections',
      '',
      '1. **What changes, in one paragraph.** Behaviour, not file names.',
      '2. **Why it was likely done**, from the code and the commit messages. Label',
      '   this as inference.',
      '3. **Change inventory** grouped by area, each line naming the observable',
      '   effect. Pure renames and formatting go in a single line at the end.',
      '4. **Risk** — what breaks if this is wrong, and who notices first.',
      '5. **What to test manually**, beyond the suite.',
      '6. **Questions for the author** — only ones the code cannot answer.',
      '',
      '## Rules',
      '',
      '- Read the surrounding code, not only the changed lines. A diff hides the',
      '  caller that makes a change wrong.',
      '- Separate what the diff **does** from what you **suspect** about it.',
      '- Do not review style. This is an explanation, not a review.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-optimize-query',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Optimize Query',
    description: 'Prompt que otimiza uma consulta lenta com plano de execução e medição.',
    whenToUse: 'Use quando uma query específica está lenta e você tem acesso ao plano.',
    icon: 'database',
    category: 'database',
    tags: ['sql', 'performance', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-08-02',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'Frontmatter limitado a description e argument-hint.',
      'Nenhuma ferramenta declarada: o Codex usa a permissão da sessão.'
    ],
    files: [{ path: 'optimize-query.md' }],
    content: [
      '---',
      'description: Optimize a slow query using its execution plan, with measurements',
      'argument-hint: <query file or identifier>',
      '---',
      '',
      '# Optimize: $1',
      '',
      '## Workflow',
      '',
      '1. Read the query and the schema of every table it touches, including the',
      '   existing indexes. Do not propose anything yet.',
      '2. Get the execution plan. Identify the step that dominates the cost, and the',
      '   row estimate that is furthest from reality.',
      '3. Only then propose changes, ranked by expected saving:',
      '   - a predicate that can use an index instead of a function on a column',
      '   - a missing or badly ordered composite index',
      '   - a join order or join type the planner got wrong because of stale stats',
      '   - a `LIMIT` that lets the planner stop early',
      '   - fewer columns, so a covering index becomes possible',
      '4. Re-measure after each change, one at a time.',
      '',
      '## Rules',
      '',
      '- Report timings with the row counts they were measured at. A gain on the dev',
      '  seed proves nothing.',
      '- A new index costs write throughput and disk. Say what it costs.',
      '- Never change what the query returns to make it faster. If a rewrite changes',
      '  the result set, that is a different question — stop and say so.',
      '- Never run a schema change. Output the DDL and let a human apply it.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-migrate-dependency',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Migrate Dependency',
    description: 'Prompt que migra uma dependência de versão maior em etapas verificáveis.',
    whenToUse: 'Use ao subir uma major version que muda API e quebra a build.',
    icon: 'package',
    category: 'development',
    tags: ['dependências', 'migração', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-07-08',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'Frontmatter limitado a description e argument-hint.',
      'Nenhuma ferramenta declarada: o Codex usa a permissão da sessão.'
    ],
    files: [{ path: 'migrate-dependency.md' }],
    content: [
      '---',
      'description: Migrate a dependency across a major version in verifiable steps',
      'argument-hint: <package> <target-version>',
      '---',
      '',
      '# Migrate $1 to $2',
      '',
      '## Step 0 — before editing anything',
      '',
      '1. Find every import site of `$1` in the repository and count them.',
      '2. Read the changelog between the installed version and `$2`. List only the',
      '   breaking changes that touch the APIs actually used here.',
      '3. Report both lists and the estimated blast radius. Stop if the list is empty',
      '   — an empty list means the bump is not a migration.',
      '',
      '## Steps',
      '',
      '1. Bump the dependency alone, in its own commit, and record what breaks.',
      '2. Fix one breaking change at a time, mechanical renames first, semantic',
      '   changes last.',
      '3. After each, run the build and the tests, and report the real output.',
      '4. Where the new API has no equivalent, stop and describe the options rather',
      '   than inventing a shim.',
      '',
      '## Rules',
      '',
      '- Never suppress a type error or a deprecation warning to make the build pass.',
      '- Never mix the migration with unrelated refactoring.',
      '- A removed feature is reported, never silently reimplemented.',
      '- Say explicitly which behaviour changed for the user, if any.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-add-endpoint',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Add Endpoint',
    description: 'Prompt que adiciona um endpoint completo: validação, erro, teste e doc.',
    whenToUse: 'Use ao criar uma rota nova e querer que ela já nasça inteira.',
    icon: 'server',
    category: 'api',
    tags: ['api', 'endpoint', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-21',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'Frontmatter limitado a description e argument-hint.',
      'Nenhuma ferramenta declarada: o Codex usa a permissão da sessão.'
    ],
    files: [{ path: 'add-endpoint.md' }],
    content: [
      '---',
      'description: Add a complete endpoint with validation, errors, tests and docs',
      'argument-hint: <method> <path>',
      '---',
      '',
      '# Add endpoint: $1 $2',
      '',
      '## Before writing code',
      '',
      'Read two existing endpoints in this repository and follow their shape. A new',
      'endpoint that invents its own conventions is a finding, not a feature.',
      '',
      '## Deliverables, all of them',
      '',
      '1. **Route** registered where the others are, same middleware order.',
      '2. **Input validation** at the boundary, rejecting unknown fields.',
      '3. **Authorization**: who may call this, checked against the resource owner,',
      '   never against a client-supplied id alone.',
      '4. **Use case** holding the rule. The handler stays thin.',
      '5. **Responses**: the success shape plus every error status, using the',
      '   project error envelope.',
      '6. **Tests**: the happy path, one rejected input, one unauthorized call.',
      '7. **Docs** updated in the same change.',
      '',
      '## Rules',
      '',
      '- Paginate any collection response, with a documented default limit.',
      '- No unbounded query, no `SELECT *` reaching the client.',
      '- Idempotency stated explicitly for anything that writes.',
      '- Never log the request body.',
      '',
      '## Report',
      '',
      'The endpoint contract as a consumer would read it, then the test output.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-cleanup-dead-code',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Cleanup Dead Code',
    description: 'Prompt que encontra e remove código morto com prova de que ninguém usa.',
    whenToUse: 'Use quando a base acumulou código sem uso e você quer remover com segurança.',
    icon: 'trash',
    category: 'refactoring',
    tags: ['limpeza', 'código morto', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-06-26',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'Frontmatter limitado a description e argument-hint.',
      'Nenhuma ferramenta declarada: o Codex usa a permissão da sessão.'
    ],
    files: [{ path: 'cleanup-dead-code.md' }],
    content: [
      '---',
      'description: Find and remove dead code, with evidence that nothing uses it',
      'argument-hint: [path]',
      '---',
      '',
      '# Remove dead code under $1',
      '',
      '## What counts as dead',
      '',
      '- An exported symbol with zero import sites.',
      '- A branch whose condition cannot be true.',
      '- A parameter never read.',
      '- A file nothing reaches, transitively, from an entry point.',
      '- A feature flag whose value has been constant since it was added.',
      '',
      '## Evidence required per removal',
      '',
      '1. The search that found zero references, and what it covered.',
      '2. A check for dynamic access: a module path built from a string, a method',
      '   looked up by name, a template that references it, a config value that',
      '   names it. Anything reachable that way is **not** dead.',
      '3. Whether it is part of a published API. A public export is a breaking',
      '   change, not a cleanup.',
      '',
      '## Rules',
      '',
      '- One category per commit: unused exports, then dead branches, then files.',
      '- Never remove something only tests use without saying so — that is a test to',
      '  delete too, or a gap to fill.',
      '- Run the build and the suite after each commit and report the output.',
      '- Uncertain items go in a separate "candidates" list, not into the diff.',
      ''
    ].join('\n')
  },
  {
    id: 'codex-write-adr',
    type: 'codex-skill',
    tool: 'codex',
    format: 'codex-prompt',
    name: 'Write ADR',
    description: 'Prompt que registra uma decisão técnica com contexto, alternativas e custo.',
    whenToUse: 'Use ao tomar uma decisão de arquitetura que alguém vai questionar depois.',
    icon: 'doc',
    category: 'architecture',
    tags: ['adr', 'decisão', 'prompt'],
    platform: 'codex',
    compatibility: ['Codex'],
    license: 'Apache-2.0',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-07-31',
    tools: { allowed: [], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: [
      'Frontmatter limitado a description e argument-hint.',
      'Nenhuma ferramenta declarada: o Codex usa a permissão da sessão.'
    ],
    files: [{ path: 'write-adr.md' }],
    content: [
      '---',
      'description: Record an architecture decision with context, alternatives and cost',
      'argument-hint: <decision title>',
      '---',
      '',
      '# ADR: $ARGUMENTS',
      '',
      'Write the record someone will read in two years when they want to undo this.',
      '',
      '## Sections',
      '',
      '1. **Status** — proposed, accepted, or superseded by which record.',
      '2. **Context** — the forces that make a decision necessary. Constraints that',
      '   are real, not preferences.',
      '3. **Decision** — one sentence, in the present tense, stating what will be done.',
      '4. **Alternatives considered** — at least two, each with the reason it lost.',
      '   An ADR with no rejected alternative is a note, not a decision.',
      '5. **Consequences** — what gets easier, what gets harder, and what this now',
      '   forbids. Include the cost, not only the benefit.',
      '6. **Reversal** — what it would take to undo this later.',
      '',
      '## Rules',
      '',
      '- Ground the context in the actual repository: files, sizes, current shapes.',
      '- No marketing language about the chosen option.',
      '- If the decision is already implemented, say so — a retroactive ADR is fine,',
      '  pretending it is a proposal is not.',
      ''
    ].join('\n')
  }
]);
