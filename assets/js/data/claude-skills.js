/* ==========================================================================
   data/claude-skills.js — coleção Claude Skills

   Formato claude-skill: diretório com SKILL.md e frontmatter YAML do Claude
   Code. Cada `content` usa apenas campos documentados (ver formats.js).

   `description` e `whenToUse` em pt-BR (texto de UI); `content` no idioma do
   próprio artefato. `featured: true` é curadoria, não métrica.
   ========================================================================== */

window.SKILL_HUB_CATALOG = (window.SKILL_HUB_CATALOG || []).concat([
  {
    id: 'api-reviewer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'API Reviewer',
    description: 'Revisa mudanças de API contra convenções, contratos e breaking changes.',
    whenToUse: 'Use antes de publicar alterações em endpoints, ou quando alguém pedir uma revisão de API.',
    icon: 'shield',
    category: 'api',
    tags: ['api', 'review', 'contratos'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: 'https://code.claude.com/docs/en/skills',
    updatedAt: '2026-08-28',
    featured: true,
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools, porém somente ferramentas de leitura.'],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: api-reviewer',
      'description: "Review API changes against project conventions. Use when the user asks for an API review or before shipping endpoint changes."',
      'when_to_use: "A route, request schema, response shape, or error format changed and consumers could break."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      '---',
      '',
      '# Goal',
      '',
      'Report the API changes that will break a consumer, before they ship.',
      '',
      '## Workflow',
      '',
      '1. Locate the changed surface: route definitions, request/response schemas,',
      '   serializers, and error handlers.',
      '2. For each endpoint, compare against the previous shape:',
      '   - path and method',
      '   - required vs optional fields, and their types',
      '   - status codes and error envelope',
      '   - pagination and sort contracts',
      '   - auth scope required',
      '3. Classify each change as **breaking**, **additive**, or **internal**.',
      '4. For every breaking change, name the consumer that breaks and the migration',
      '   that avoids it.',
      '',
      '## Quality checks',
      '',
      '- Cite concrete files and line ranges; no findings without a location.',
      '- Separate confirmed problems from suggestions, explicitly.',
      '- Do not invent conventions that are absent from the codebase.',
      '- If the previous shape cannot be determined from the repo, say so instead',
      '  of assuming it.',
      ''
    ].join('\n')
  },
  {
    id: 'pr-reviewer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'PR Reviewer',
    description: 'Revisa o diff pendente por severidade, com cenário de falha em cada achado.',
    whenToUse: 'Use quando pedirem revisão de um PR ou do diff atual antes do merge.',
    icon: 'git-branch',
    category: 'development',
    tags: ['review', 'git', 'qualidade'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-30',
    featured: true,
    tools: { allowed: ['Read', 'Grep', 'Bash(git diff *)'], disallowed: ['Write', 'Edit'] },
    flags: { scripts: false, shell: 'bash', shellInjection: true, fork: false, hooks: false, portable: false, claudeCodeOnly: true },
    risk: 'elevated',
    riskFactors: [
      'Declara allowed-tools.',
      'Pré-aprova comandos Bash.',
      'Injeta contexto dinâmico de shell no corpo: o git diff roda a cada invocação.',
      'Usa campos específicos de Claude Code, reduzindo portabilidade.'
    ],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: pr-reviewer',
      'description: "Review the pending diff for correctness and maintainability. Use when the user asks for a code or PR review."',
      'argument-hint: "[base-branch]"',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - "Bash(git diff *)"',
      'disallowed-tools:',
      '  - Write',
      '  - Edit',
      '---',
      '',
      '# Goal',
      '',
      'Report the defects in this diff that the author can act on today.',
      '',
      '## Pending changes',
      '',
      '- Diff: !`git diff $ARGUMENTS`',
      '- Files touched: !`git diff --stat $ARGUMENTS`',
      '',
      '## Workflow',
      '',
      '1. Read the diff above, then open the surrounding code — a diff alone hides',
      '   the caller that makes a change wrong.',
      '2. Look for correctness first: wrong condition, off-by-one, unhandled error,',
      '   lost await, changed contract, N+1 query, missing invalidation.',
      '3. Then reuse and clarity: duplicated logic, an abstraction used once, a name',
      '   that lies.',
      '4. For each finding give the **file**, the **failure scenario** (concrete',
      '   inputs → wrong output), and the **fix**.',
      '',
      '## Report format',
      '',
      'Most severe first. Anything you cannot defend with the code you read goes',
      'under "worth a look" rather than in the findings list.',
      '',
      '## Limits',
      '',
      '- Read-only: this skill reports, it never edits.',
      '- Never claim a test passes without having seen it run.',
      ''
    ].join('\n')
  },
  {
    id: 'bug-reproducer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Bug Reproducer',
    description: 'Transforma um relato vago no menor teste que falha de forma determinística.',
    whenToUse: 'Use quando chega um bug sem passos claros e você precisa de uma reprodução confiável.',
    icon: 'bug',
    category: 'debugging',
    tags: ['bug', 'reprodução', 'testes'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-05',
    featured: true,
    tools: { allowed: ['Read', 'Grep', 'Glob', 'Write'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: ['Declara allowed-tools.', 'Inclui ferramenta de escrita em arquivos.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: bug-reproducer',
      'description: "Turn a vague bug report into the smallest deterministic failing test. Use when a bug has no reliable reproduction steps."',
      'when_to_use: "The report says it is broken but nobody can make it fail on demand."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'A failing test that pins the bug, before anyone tries to fix it.',
      '',
      '## Workflow',
      '',
      '1. Extract from the report what is **observed** and what was **expected**.',
      '   If either is missing, ask for that one thing and stop.',
      '2. Find the code path that could produce the observed behavior. State the',
      '   hypothesis in one sentence.',
      '3. Write the smallest test that fails for that reason. No mocks that assume',
      '   the bug.',
      '4. Run it. A test that passes means the hypothesis was wrong — say so and go',
      '   back to step 2 instead of adjusting the test until it fails.',
      '5. Reduce: remove every line that is not needed to make it fail.',
      '',
      '## Quality checks',
      '',
      '- Deterministic. If it fails one run in five, report the rate and the source',
      '  of nondeterminism instead of calling it reproduced.',
      '- The assertion names the wrong value, not "should not throw".',
      '- Do not fix the bug here. The fix is a separate change on top of a red test.',
      ''
    ].join('\n')
  },
  {
    id: 'test-writer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Test Writer',
    description: 'Escreve testes para o comportamento que existe hoje, travando regressões.',
    whenToUse: 'Use quando um módulo sem cobertura precisa de testes antes de ser mexido.',
    icon: 'flask',
    category: 'testing',
    tags: ['testes', 'cobertura', 'regressão'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-03',
    tools: { allowed: ['Read', 'Grep', 'Write', 'Bash(npm test *)'], disallowed: [] },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools.',
      'Pré-aprova comandos Bash restritos ao runner de testes.',
      'Inclui ferramenta de escrita em arquivos.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: test-writer',
      'description: "Write tests that lock in current behavior. Use before refactoring a module that has no coverage."',
      'argument-hint: "[path]"',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Write',
      '  - "Bash(npm test *)"',
      '---',
      '',
      '# Goal',
      '',
      'Cover what the code does today, so a refactor cannot change it silently.',
      '',
      '## Workflow',
      '',
      '1. Read the target and list its observable behaviors: return values, thrown',
      '   errors, side effects, and the boundaries of each input.',
      '2. Write the smallest test per behavior. Table-driven where the inputs vary',
      '   but the assertion does not.',
      '3. Run the suite and report the real output, pass or fail, verbatim.',
      '4. List what you chose not to cover, and why.',
      '',
      '## Rules',
      '',
      '- Test the behavior that exists, not the behavior that should exist. Suspected',
      '  bugs go in a separate report.',
      '- Assert on outcomes, never on the order of internal calls.',
      '- One reason to fail per test.',
      '- Match the runner and assertion style already in the repo — see reference.md.',
      ''
    ].join('\n')
  },
  {
    id: 'flaky-test-hunter',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Flaky Test Hunter',
    description: 'Encontra a causa real de um teste intermitente em vez de aumentar o timeout.',
    whenToUse: 'Use quando um teste passa e falha sem mudança de código.',
    icon: 'refresh',
    category: 'testing',
    tags: ['flaky', 'testes', 'concorrência'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-07-28',
    tools: { allowed: ['Read', 'Grep', 'Bash(npm test *)'], disallowed: ['Write'] },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: ['Declara allowed-tools.', 'Pré-aprova comandos Bash restritos ao runner.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: flaky-test-hunter',
      'description: "Find why a test is intermittent instead of masking it. Use when a test passes and fails with no code change."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - "Bash(npm test *)"',
      'disallowed-tools:',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Name the source of nondeterminism, with evidence.',
      '',
      '## Usual suspects, in order of likelihood',
      '',
      '1. **Shared state** between tests: a module-level cache, a global, a database',
      '   row another test wrote.',
      '2. **Order dependence**: the test passes alone and fails in the suite, or the',
      '   reverse.',
      '3. **Time**: a real clock, a timezone, a date boundary, a sleep standing in',
      '   for a condition.',
      '4. **Concurrency**: an unawaited promise, a race between setup and assertion.',
      '5. **External resources**: network, filesystem, port already bound.',
      '',
      '## Workflow',
      '',
      '1. Run the test alone and in the full suite; record both results.',
      '2. Run it repeatedly and report the observed failure rate. A rate is data; "it',
      '   is flaky" is not.',
      '3. Work the list above and say which one the evidence supports.',
      '',
      '## Limits',
      '',
      '- Never raise a timeout or add a retry as the fix. Those hide the cause.',
      '- If the rate is zero after 20 runs, say the flake did not reproduce rather',
      '  than declaring it fixed.',
      ''
    ].join('\n')
  },
  {
    id: 'perf-profiler',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Perf Profiler',
    description: 'Mede antes de otimizar e aponta o gargalo com número, não com palpite.',
    whenToUse: 'Use quando algo está lento e ninguém sabe onde o tempo é gasto.',
    icon: 'zap',
    category: 'performance',
    tags: ['performance', 'medição', 'otimização'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-22',
    tools: { allowed: ['Read', 'Grep', 'Bash(node --prof *)', 'Bash(npm run bench *)'], disallowed: ['Write'] },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: ['Declara allowed-tools.', 'Pré-aprova comandos Bash de profiling.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: perf-profiler',
      'description: "Locate the real bottleneck with a measurement before changing anything. Use when something is slow."',
      'when_to_use: "Someone wants to optimize but there is no profile yet."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - "Bash(node --prof *)"',
      '  - "Bash(npm run bench *)"',
      'disallowed-tools:',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Turn "it feels slow" into a ranked list of costs with numbers.',
      '',
      '## Workflow',
      '',
      '1. Ask what "slow" means here: which operation, at what input size, and what',
      '   number would count as fixed. Without a target there is no done.',
      '2. Measure the baseline. Record the machine, the input, and the run count.',
      '3. Attribute the time: the top contributors, each with its share.',
      '4. Rank candidate changes by **expected saving ÷ risk**, and say which are',
      '   guesses.',
      '',
      '## Rules',
      '',
      '- No optimization proposal without a measurement behind it.',
      '- Report p95 alongside the mean; an average hides the case users complain about.',
      '- Distinguish algorithmic cost from constant factors — the first survives a',
      '  faster machine.',
      '- If the bottleneck is I/O, say so plainly instead of micro-tuning the CPU path.',
      ''
    ].join('\n')
  },
  {
    id: 'refactor-planner',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Refactor Planner',
    description: 'Quebra uma refatoração grande em passos reversíveis que mantêm a suíte verde.',
    whenToUse: 'Use antes de mexer num módulo central, para não travar o time num PR gigante.',
    icon: 'code',
    category: 'refactoring',
    tags: ['refatoração', 'legado', 'planejamento'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-18',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: ['Write', 'Edit'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools, somente de leitura, e nega escrita.'],
    files: [{ path: 'SKILL.md' }, { path: 'examples.md' }],
    content: [
      '---',
      'name: refactor-planner',
      'description: "Break a large refactor into reversible steps that each keep the suite green. Use before touching a central module."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      'disallowed-tools:',
      '  - Write',
      '  - Edit',
      '---',
      '',
      '# Goal',
      '',
      'A sequence of steps a reviewer can read one commit at a time.',
      '',
      '## Workflow',
      '',
      '1. Map the blast radius: every caller, test, and mock that touches the target.',
      '2. Identify the **seam** — the smallest interface that lets old and new coexist.',
      '3. Order the steps so each one:',
      '   - is independently mergeable',
      '   - leaves the suite green',
      '   - can be reverted without touching the others',
      '4. Keep renames in their own steps, never mixed with logic changes.',
      '5. Name the step that cannot be reverted, if one exists, and what makes it so.',
      '',
      '## Output',
      '',
      'A numbered plan. Per step: what changes, what proves it worked, and the',
      'rollback. See examples.md for the expand-and-contract shape.',
      '',
      '## Limits',
      '',
      '- Read-only: this skill plans, it does not edit.',
      '- A plan whose first step is "rewrite the module" is not a plan; split it.',
      ''
    ].join('\n')
  },
  {
    id: 'sql-reviewer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'SQL Reviewer',
    description: 'Revisa consultas por N+1, índice ausente, lock e varredura completa.',
    whenToUse: 'Use quando uma tela ficou lenta ou uma query nova vai para produção.',
    icon: 'database',
    category: 'database',
    tags: ['sql', 'índices', 'performance'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-01',
    tools: { allowed: ['Read', 'Grep'], disallowed: ['Bash', 'Write'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools somente de leitura, e nega Bash e escrita.'],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: sql-reviewer',
      'description: "Review queries for N+1, missing indexes, locks and full scans. Use when a screen got slow or a new query is shipping."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      'disallowed-tools:',
      '  - Bash',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Catch the query that will fall over at production data volume.',
      '',
      '## Checklist per query',
      '',
      '- **N+1**: a query inside a loop, or a lazy relation read per row.',
      '- **Index**: does a `WHERE`, `JOIN` or `ORDER BY` column have one? Is the',
      '  column order of a composite index compatible with this predicate?',
      '- **Selectivity**: how many rows at production volume, not in the dev seed?',
      '- **Unbounded**: any `SELECT` without `LIMIT` on a growing table.',
      '- **Locks**: an update touching many rows in one transaction; a long',
      '  transaction holding a row lock across an external call.',
      '- **Function on a column**: `WHERE lower(email) = ...` defeats a plain index.',
      '- **`SELECT *`** where a wide column (blob, JSON) is not needed.',
      '',
      '## Output',
      '',
      'Per finding: the file, the query, what breaks at scale, and the fix — the',
      'index definition, the eager load, or the rewritten predicate.',
      '',
      '## Limits',
      '',
      '- Never run a migration or an `EXPLAIN` against production.',
      '- Say when a verdict needs real cardinality you do not have.',
      ''
    ].join('\n')
  },
  {
    id: 'error-path-reviewer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Error Path Reviewer',
    description: 'Revisa o que acontece quando dá errado: engolir, vazar ou perder o contexto.',
    whenToUse: 'Use quando falhas estão sumindo silenciosamente ou os logs não explicam nada.',
    icon: 'alert',
    category: 'development',
    tags: ['erros', 'resiliência', 'logs'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-14',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools, porém somente de leitura.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: error-path-reviewer',
      'description: "Review error handling for swallowed failures, leaked internals and lost context. Use when failures vanish or logs explain nothing."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      '---',
      '',
      '# Goal',
      '',
      'Make every failure either handled deliberately or loudly visible.',
      '',
      '## What to look for',
      '',
      '- **Swallowed**: an empty catch, a catch that only logs and continues, a',
      '  rejected promise with no handler.',
      '- **Lost context**: rethrowing a new error without the cause, or replacing a',
      '  specific error with a generic one.',
      '- **Leaked internals**: a stack trace, SQL, or file path reaching the user.',
      '- **Wrong granularity**: one try/catch around fifty lines, so the message',
      '  cannot say what failed.',
      '- **Retry without care**: retrying a non-idempotent call, or with no backoff',
      '  and no cap.',
      '- **Cleanup**: a resource left open when the error path is taken.',
      '',
      '## Output',
      '',
      'Per finding: the file, what the user or the operator sees today, and what they',
      'should see. An empty catch that is genuinely intentional gets a comment',
      'explaining why, not a removal.',
      '',
      '## Quality checks',
      '',
      '- Distinguish expected failures (validation, not found) from bugs. They need',
      '  different handling, not the same catch.',
      ''
    ].join('\n')
  },
  {
    id: 'type-hardener',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Type Hardener',
    description: 'Fecha lacunas de tipagem começando pelas fronteiras, onde o dado entra.',
    whenToUse: 'Use quando a base tem tipos frouxos e você quer endurecer sem parar o time.',
    icon: 'lock',
    category: 'development',
    tags: ['tipos', 'typescript', 'fronteiras'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-07-11',
    tools: { allowed: ['Read', 'Grep', 'Write'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: ['Declara allowed-tools.', 'Inclui ferramenta de escrita em arquivos.'],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: type-hardener',
      'description: "Tighten types starting at input boundaries. Use when the codebase leaks untyped values."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Make invalid states unrepresentable where it actually pays off.',
      '',
      '## Workflow',
      '',
      '1. Map the boundaries: request handlers, parsers, storage reads, environment',
      '   variables, message queue payloads.',
      '2. Type the boundary first and let inference flow inward. Internal helpers',
      '   rarely need annotations.',
      '3. Replace a wide union with a narrow one only where every call site agrees.',
      '4. Report the remaining escape hatches instead of hiding them.',
      '',
      '## Rules',
      '',
      '- No untyped value at a boundary. Parse into a validated shape instead.',
      '- Never silence an error with a cast to satisfy the compiler. Fix the shape,',
      '  or narrow explicitly and comment why.',
      '- Prefer a discriminated union over an optional field when the states are',
      '  mutually exclusive.',
      '- A type that needs a comment to be understood is the wrong type.',
      '- One boundary per commit, so the blast radius stays readable.',
      ''
    ].join('\n')
  },
  {
    id: 'dependency-auditor',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Dependency Auditor',
    description: 'Audita dependências por risco, tamanho, manutenção e o que dá para remover.',
    whenToUse: 'Use antes de adicionar uma dependência, ou ao revisar o peso do projeto.',
    icon: 'package',
    category: 'security',
    tags: ['dependências', 'supply chain', 'manutenção'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-08-09',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: ['Bash'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools somente de leitura, e nega Bash.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: dependency-auditor',
      'description: "Audit dependencies for risk, size, maintenance and removability. Use before adding one, or when reviewing project weight."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      'disallowed-tools:',
      '  - Bash',
      '---',
      '',
      '# Goal',
      '',
      'Know what each dependency costs and whether it still earns its place.',
      '',
      '## Per dependency',
      '',
      '- **Used?** Find the import sites. Zero means it can go.',
      '- **How much of it is used?** One helper from a large library is a candidate',
      '  for inlining.',
      '- **Transitive weight**: how many packages come with it.',
      '- **Surface**: does it run at install time, touch the network, or need native',
      '  build steps?',
      '- **Overlap**: is another dependency already doing this?',
      '- **Replaceable by the platform**: many small utilities are now built in.',
      '',
      '## Output',
      '',
      'Three lists: **remove** (unused or trivially replaceable), **watch** (heavy or',
      'barely used), **keep** (with the one-line reason). Each removal names the',
      'import sites that must change.',
      '',
      '## Limits',
      '',
      '- Do not claim a package is unmaintained or vulnerable without a source; this',
      '  skill reads the repo, not a registry.',
      '- Never remove a dependency used only at runtime through a dynamic import',
      '  without checking for string-built module paths.',
      ''
    ].join('\n')
  },
  {
    id: 'a11y-auditor',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Accessibility Auditor',
    description: 'Audita um componente por nome acessível, foco, teclado e contraste.',
    whenToUse: 'Use antes de publicar um componente novo de interface.',
    icon: 'eye',
    category: 'accessibility',
    tags: ['acessibilidade', 'aria', 'teclado'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-25',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools, porém somente de leitura.'],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: a11y-auditor',
      'description: "Audit a UI component for accessible name, focus, keyboard operation and contrast. Use before shipping new interface code."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      '---',
      '',
      '# Goal',
      '',
      'A component someone can operate without a mouse and without sight.',
      '',
      '## Checklist',
      '',
      '- **Semantics first**: a real `button` for actions, a real `a[href]` for',
      '  navigation. A `div` with a click handler fails here.',
      '- **Accessible name**: every control has one, from its text, its `label`, or',
      '  `aria-label`. Icon-only controls always need the explicit form.',
      '- **Keyboard**: reachable by Tab in a sensible order, operable by Enter and',
      '  Space, and escapable — a dialog traps focus and closes on Escape.',
      '- **Visible focus**: never removed without a replacement.',
      '- **State in the DOM**: `aria-expanded`, `aria-pressed`, `aria-current`,',
      '  `aria-invalid` match what the pixels show.',
      '- **Not colour alone**: status, error and risk carry text or an icon too.',
      '- **Live regions**: async messages announce; a toast is `role="status"`.',
      '- **Motion**: honours `prefers-reduced-motion`.',
      '',
      '## Output',
      '',
      'Per finding: the element, who it excludes, and the concrete fix. Order by who',
      'is blocked entirely before who is merely inconvenienced.',
      ''
    ].join('\n')
  },
  {
    id: 'frontend-state-reviewer',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Frontend State Reviewer',
    description: 'Revisa estado de interface: fonte de verdade, dado derivado e sincronia.',
    whenToUse: 'Use quando a interface mostra dado velho ou o estado está espalhado.',
    icon: 'panel',
    category: 'frontend',
    tags: ['estado', 'ui', 'sincronia'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-07-19',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools, porém somente de leitura.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: frontend-state-reviewer',
      'description: "Review UI state for source of truth, derived data and sync bugs. Use when the interface shows stale data or state is scattered."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      '---',
      '',
      '# Goal',
      '',
      'One source of truth per fact, and nothing stored that can be derived.',
      '',
      '## What to look for',
      '',
      '- **Duplicated truth**: the same fact in two places, kept in sync by hand.',
      '  This is the usual cause of "it shows the old value".',
      '- **Derived data stored**: a total, a filtered list, or a flag that could be',
      '  computed. Storing it creates a second thing to invalidate.',
      '- **The DOM as state**: reading a value back out of an input or a class name',
      '  instead of from the state that rendered it.',
      '- **Effects that fight**: two effects writing the same value, or an effect',
      '  whose dependency list hides a stale closure.',
      '- **Server vs client**: cached response treated as truth after a mutation.',
      '- **Reset gaps**: state that survives a navigation it should not, or is',
      '  cleared when the user expected it kept.',
      '',
      '## Output',
      '',
      'A table of facts: where each one lives, who writes it, and who should. Then',
      'the findings, each with the concrete stale-data scenario it produces.',
      ''
    ].join('\n')
  },
  {
    id: 'log-triage',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Log Triage',
    description: 'Lê um despejo de log e reconstrói o caminho que falhou, com o momento exato.',
    whenToUse: 'Use quando há muito log e nenhuma pista de onde o erro começou.',
    icon: 'terminal',
    category: 'debugging',
    tags: ['logs', 'incidente', 'diagnóstico'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-06-30',
    tools: { allowed: ['Read', 'Grep'], disallowed: ['Write'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools somente de leitura, e nega escrita.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: log-triage',
      'description: "Read a log dump and reconstruct the failing path with its first symptom. Use when there is a lot of log and no lead."',
      'argument-hint: "[log-file]"',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      'disallowed-tools:',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Find the **first** symptom, not the loudest one.',
      '',
      '## Workflow',
      '',
      '1. Establish the window: when did behavior change? Everything before that is',
      '   background noise.',
      '2. Find the earliest anomaly in that window. Cascading errors are usually',
      '   consequences; the first one is the lead.',
      '3. Follow one request or trace end to end, in order, and write the timeline.',
      '4. Separate **symptom**, **probable cause**, and **speculation**, labelled.',
      '5. Map the log lines back to the code that emits them.',
      '',
      '## Rules',
      '',
      '- Quote the log lines you rely on, with their timestamps.',
      '- A recurring warning that predates the window is not the cause; say so.',
      '- Never redact-by-omission: if a line contains a secret, note that it was',
      '  present rather than quoting it.',
      '- If the logs do not contain enough to decide, say what to log next.',
      ''
    ].join('\n')
  },
  {
    id: 'monorepo-navigator',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Monorepo Navigator',
    description: 'Descobre onde uma responsabilidade vive num monorepo grande e quem depende dela.',
    whenToUse: 'Use ao entrar num repositório grande ou antes de mover código entre pacotes.',
    icon: 'layers',
    category: 'architecture',
    tags: ['monorepo', 'dependências', 'onboarding'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-06',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: ['Write'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: true, hooks: false, portable: false, claudeCodeOnly: true },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools somente de leitura.',
      'Executa em contexto isolado (context: fork), útil para varreduras longas.'
    ],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: monorepo-navigator',
      'description: "Locate where a responsibility lives in a large monorepo and who depends on it. Use when onboarding or before moving code between packages."',
      'argument-hint: "[responsibility or symbol]"',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      'disallowed-tools:',
      '  - Write',
      'context: fork',
      'effort: high',
      '---',
      '',
      '# Goal',
      '',
      'Answer "where does X live, and what breaks if I move it?"',
      '',
      '## Workflow',
      '',
      '1. Read the workspace manifest to learn the package boundaries before',
      '   grepping. The directory layout often lies about ownership.',
      '2. Find every definition of the responsibility. Multiple definitions are the',
      '   finding, not a nuisance.',
      '3. Build the dependent list: which packages import it, and whether through a',
      '   public entry point or a deep path.',
      '4. Report the **owning** package, the **duplicates**, and the **deep imports**',
      '   that would break a move.',
      '',
      '## Output',
      '',
      'Conclusion first: one package name. Then the evidence, each item with its',
      'path. Then what a move would require, in order.',
      '',
      '## Limits',
      '',
      '- Read-only. Never move a file.',
      '- Deep imports found by string search may be dynamic; flag the uncertain ones.',
      ''
    ].join('\n')
  },
  {
    id: 'git-archaeologist',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Git Archaeologist',
    description: 'Usa o histórico para explicar por que o código está assim antes de você mudá-lo.',
    whenToUse: 'Use quando um trecho parece errado e você quer saber se foi de propósito.',
    icon: 'git-branch',
    category: 'git',
    tags: ['git', 'histórico', 'contexto'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-06',
    tools: { allowed: ['Read', 'Bash(git log *)', 'Bash(git blame *)', 'Bash(git show *)'], disallowed: ['Write'] },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools.',
      'Pré-aprova comandos Bash, restritos a leitura do histórico do Git.'
    ],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: git-archaeologist',
      'description: "Use history to explain why the code looks the way it does, before changing it. Use when something looks wrong and might be deliberate."',
      'argument-hint: "[file or path]"',
      'allowed-tools:',
      '  - Read',
      '  - "Bash(git log *)"',
      '  - "Bash(git blame *)"',
      '  - "Bash(git show *)"',
      'disallowed-tools:',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Recover the reason behind the current shape, so a "cleanup" does not',
      'reintroduce a fixed bug.',
      '',
      '## Workflow',
      '',
      '1. Find when the odd lines appeared, and read that commit in full — message,',
      '   diff, and the sibling changes in the same commit.',
      '2. Look for a **revert** or a **fix-after** pair in the surrounding history.',
      '   A defensive check usually marks an incident.',
      '3. Follow the file through renames; history stops at a rename otherwise.',
      '4. Report: what the code does, when it started doing that, and the stated or',
      '   inferred reason — labelled as stated or inferred.',
      '',
      '## Rules',
      '',
      '- Quote the commit hash and subject for every claim.',
      '- A commit message that says "fix" without saying what is evidence of a bug,',
      '  not evidence of which bug. Say that plainly.',
      '- Read-only: never rewrite history, never revert.',
      ''
    ].join('\n')
  },
  {
    id: 'env-config-auditor',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Config Auditor',
    description: 'Audita configuração e variáveis de ambiente: default perigoso e falha silenciosa.',
    whenToUse: 'Use quando o app se comporta diferente entre ambientes sem motivo claro.',
    icon: 'sliders',
    category: 'backend',
    tags: ['configuração', 'ambiente', 'deploy'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-07-24',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: ['Bash'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools somente de leitura, e nega Bash.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: env-config-auditor',
      'description: "Audit configuration and environment handling for unsafe defaults and silent failures. Use when behavior differs between environments."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      'disallowed-tools:',
      '  - Bash',
      '---',
      '',
      '# Goal',
      '',
      'Configuration that fails loudly at startup instead of subtly at runtime.',
      '',
      '## What to look for',
      '',
      '- **Missing variable, silent default**: a fallback that makes the app run in',
      '  the wrong mode instead of refusing to start.',
      '- **Unsafe default**: debug on, auth off, permissive CORS, verbose errors —',
      '  safe only because production happens to override it.',
      '- **Read scattered**: `process.env` sprinkled across modules instead of one',
      '  validated config object.',
      '- **No validation**: a port read as a string, a boolean parsed from `"false"`,',
      '  a URL never checked.',
      '- **Undocumented**: a variable the code needs and the example file omits.',
      '- **Secrets in the repo**: any committed value that looks like a credential.',
      '  Report its presence and location; do not print the value.',
      '',
      '## Output',
      '',
      'A table of every variable: where it is read, whether it is required, its',
      'default, and what happens when it is absent. Then the findings, unsafe',
      'defaults first.',
      ''
    ].join('\n')
  },
  {
    id: 'api-contract-differ',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'API Contract Differ',
    description: 'Compara duas versões de um contrato e classifica cada mudança por impacto.',
    whenToUse: 'Use antes de subir a versão de uma API pública ou de um schema compartilhado.',
    icon: 'code',
    category: 'api',
    tags: ['contrato', 'versionamento', 'breaking change'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'Apache-2.0',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-06-14',
    tools: { allowed: ['Read', 'Grep', 'Bash(git show *)'], disallowed: ['Write'] },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: ['Declara allowed-tools.', 'Pré-aprova Bash restrito a leitura do Git.'],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: api-contract-differ',
      'description: "Diff two versions of an API contract and classify each change by consumer impact. Use before bumping a public API or shared schema."',
      'argument-hint: "[from-ref] [to-ref]"',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - "Bash(git show *)"',
      'disallowed-tools:',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Decide whether this is a patch, a minor, or a major — with the reason.',
      '',
      '## Classification',
      '',
      '**Breaking** — removing a field, renaming one, narrowing a type, making an',
      'optional field required, removing an enum value, changing a status code, or',
      'tightening validation.',
      '',
      '**Additive** — a new optional field, a new endpoint, a new enum value in a',
      'field the client only reads.',
      '',
      '**Ambiguous** — a new enum value in a field the client switches on, or a',
      'loosened type. These break strict consumers; list them separately rather than',
      'calling them safe.',
      '',
      '## Workflow',
      '',
      '1. Read both versions of the contract, not the implementation.',
      '2. Walk every field and every response, in order.',
      '3. Classify, then state the resulting version bump.',
      '4. For each breaking change, write the migration note a consumer needs.',
      '',
      '## Limits',
      '',
      '- Never infer the contract from a single example payload.',
      '- If a field is undocumented in both versions, say so instead of guessing its',
      '  intent.',
      ''
    ].join('\n')
  },
  {
    id: 'race-condition-finder',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Race Condition Finder',
    description: 'Procura corridas, await perdido e escrita concorrente no mesmo estado.',
    whenToUse: 'Use quando o bug só aparece sob carga ou em uma máquina específica.',
    icon: 'refresh',
    category: 'debugging',
    tags: ['concorrência', 'async', 'corrida'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-31',
    tools: { allowed: ['Read', 'Grep', 'Glob'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Declara allowed-tools, porém somente de leitura.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: race-condition-finder',
      'description: "Look for races, lost awaits and concurrent writes to shared state. Use when a bug only appears under load."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - Glob',
      '---',
      '',
      '# Goal',
      '',
      'Find the interleaving that produces the wrong result.',
      '',
      '## Patterns to hunt',
      '',
      '- **Lost await**: an async call whose promise is never awaited, so the next',
      '  line runs against an unfinished state.',
      '- **Check-then-act**: reading a value, then writing based on it, with an await',
      '  in between. Anything can run in that gap.',
      '- **Shared mutable state**: a module-level object or cache written by more',
      '  than one flow.',
      '- **Non-idempotent retry**: a retry that can apply the same effect twice.',
      '- **Order assumption**: code that assumes two independent async calls resolve',
      '  in the order they were started.',
      '- **Cleanup after unmount / cancel**: a callback writing state after the',
      '  caller is gone.',
      '',
      '## Output',
      '',
      'Per finding: the file, the **interleaving** written as an ordered sequence of',
      'two flows, and the wrong result it produces. A race you cannot write as a',
      'sequence is a suspicion — put it in a separate list.',
      '',
      '## Limits',
      '',
      '- Do not propose a lock as the first fix; often the right fix is removing the',
      '  shared state.',
      ''
    ].join('\n')
  },
  {
    id: 'bundle-budget-guard',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Bundle Budget Guard',
    description: 'Atribui o crescimento do bundle a imports concretos e propõe cortes medidos.',
    whenToUse: 'Use quando o build cresceu e você precisa saber exatamente o que cortar.',
    icon: 'chart',
    category: 'frontend',
    tags: ['bundle', 'performance', 'build'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: null,
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-05-30',
    tools: { allowed: ['Read', 'Grep', 'Bash(npx *)'], disallowed: [] },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools.',
      'Pré-aprova comandos Bash que baixam e executam pacotes (npx *).'
    ],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: bundle-budget-guard',
      'description: "Attribute bundle growth to concrete imports and propose measured cuts. Use when the build output grew."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - "Bash(npx *)"',
      '---',
      '',
      '# Goal',
      '',
      'Every proposed cut carries a measured before and after.',
      '',
      '## Workflow',
      '',
      '1. Produce a size report for the current build. Record whether the numbers are',
      '   raw, minified, or compressed — comparing across those is meaningless.',
      '2. Attribute the top ten contributors to specific import sites.',
      '3. For each, decide: **lazy-load**, **replace**, **inline the used part**, or',
      '   **drop**.',
      '4. Estimate the saving and the risk. Apply, then measure again.',
      '',
      '## Rules',
      '',
      '- No claimed saving without a second measurement.',
      '- Do not recommend removing something still used at runtime through a dynamic',
      '  import.',
      '- A polyfill required by the support target is not waste; check before cutting.',
      '- Report the largest **first-load** cost separately from total size; the user',
      '  only waits for the first.',
      ''
    ].join('\n')
  },
  {
    id: 'migration-planner',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Migration Planner',
    description: 'Planeja migrações de schema em fases seguras e reversíveis.',
    whenToUse: 'Use antes de alterar schema em produção ou renomear colunas em uso.',
    icon: 'database',
    category: 'database',
    tags: ['banco', 'migração', 'schema'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-05',
    tools: { allowed: ['Read', 'Grep'], disallowed: ['Bash', 'Write'] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: true, hooks: false, portable: false, claudeCodeOnly: true },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools, somente de leitura.',
      'Executa em contexto isolado (context: fork).',
      'Nega Bash e Write, então não consegue aplicar migração.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'examples.md' }],
    content: [
      '---',
      'name: migration-planner',
      'description: "Plan a database schema migration as reversible phases. Use before altering, renaming or dropping a column live code reads."',
      'when_to_use: "The user wants to change a column that production traffic depends on."',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      'disallowed-tools:',
      '  - Bash',
      '  - Write',
      'context: fork',
      '---',
      '',
      '# Goal',
      '',
      'Turn a risky schema change into expand → migrate → contract, where every',
      'phase deploys and rolls back on its own.',
      '',
      '## Workflow',
      '',
      '1. Find **every** read and write of the affected columns: application code,',
      '   raw SQL, views, triggers, reports, and background jobs.',
      '2. Split into phases:',
      '   - **Expand** — add the new shape, nothing reads it yet',
      '   - **Backfill** — populate it, idempotently, in batches',
      '   - **Switch** — reads move over, writes go to both',
      '   - **Contract** — drop the old shape',
      '3. For each phase give the rollback and the query that proves success.',
      '4. Name the phase that cannot be rolled back, if one exists.',
      '',
      '## Limits',
      '',
      '- Never run migrations. Output SQL and let the user apply it.',
      '- A plan without a backfill batch size is incomplete; state one.',
      '- An unlocatable consumer is the usual cause of a failed migration — say when',
      '  you could not find one.',
      ''
    ].join('\n')
  },
  {
    id: 'release-assistant',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Release Assistant',
    description: 'Prepara checklist, changelog e comandos de release com revisão explícita.',
    whenToUse: 'Use ao preparar uma release: conferir o repositório, montar changelog e checklist.',
    icon: 'rocket',
    category: 'devops',
    tags: ['release', 'git', 'changelog'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'Apache-2.0',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-01',
    tools: { allowed: ['Read', 'Grep', 'Bash(git status *)', 'Bash(git log *)', 'Bash(git tag -l *)'], disallowed: ['Bash(git push *)'] },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: true },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools.',
      'Pré-aprova comandos Bash, ainda que restritos a leitura do Git.',
      'Nega explicitamente git push, então não consegue publicar sozinha.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'examples.md' }],
    content: [
      '---',
      'name: release-assistant',
      'description: "Prepare release notes and verify repository state. Use when cutting a release, tagging a version or writing a changelog."',
      'when_to_use: "The user is about to ship, or asks what changed since the last tag."',
      'argument-hint: "[from-tag]"',
      'allowed-tools:',
      '  - Read',
      '  - Grep',
      '  - "Bash(git status *)"',
      '  - "Bash(git log *)"',
      '  - "Bash(git tag -l *)"',
      'disallowed-tools:',
      '  - "Bash(git push *)"',
      '---',
      '',
      '# Goal',
      '',
      'A release checklist and changelog built from the real repository state.',
      '',
      '## Workflow',
      '',
      '1. Read the working tree status. A dirty tree is reported first — a release',
      '   from one is not reproducible.',
      '2. Resolve the previous tag (use $ARGUMENTS when given) and read the commit',
      '   range since it.',
      '3. Group into **breaking**, **features**, **fixes**, **internal**. A commit',
      '   that fits none is listed as unclassified, not guessed.',
      '4. Draft the changelog, newest first, one line per user-visible change.',
      '5. List the manual checks the suite does not cover.',
      '6. Print the release commands and stop.',
      '',
      '## Limits',
      '',
      '- Never push, tag or publish. Those commands are printed, not executed.',
      '- Never invent a version number; read it from the project files.',
      '- Never claim the suite passes without having seen it run.',
      ''
    ].join('\n')
  },
  {
    id: 'docker-hardener',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Docker Hardener',
    description: 'Reduz imagem e superfície de ataque, medindo antes e depois.',
    whenToUse: 'Use quando a imagem está grande, roda como root ou carrega o toolchain.',
    icon: 'cloud',
    category: 'devops',
    tags: ['docker', 'imagem', 'segurança'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'Apache-2.0',
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-07-30',
    tools: { allowed: ['Read', 'Write', 'Bash(docker build *)', 'Bash(docker image *)'], disallowed: [] },
    flags: { scripts: true, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'elevated',
    riskFactors: [
      'Declara allowed-tools.',
      'Pré-aprova comandos Bash de build de imagem.',
      'Inclui script auxiliar executável no pacote.',
      'Inclui ferramenta de escrita em arquivos.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'scripts/build.sh' }],
    content: [
      '---',
      'name: docker-hardener',
      'description: "Shrink the image and its attack surface, measuring before and after. Use when the image is large or runs as root."',
      'allowed-tools:',
      '  - Read',
      '  - Write',
      '  - "Bash(docker build *)"',
      '  - "Bash(docker image *)"',
      '---',
      '',
      '# Goal',
      '',
      'A smaller image that carries only what runtime needs.',
      '',
      '## Workflow',
      '',
      '1. Measure the current image and list its largest layers.',
      '2. Split into stages. The final stage carries the runtime and the artifact —',
      '   no compilers, no package manager caches, no tests, no `.git`.',
      '3. Write `.dockerignore` **before** the next build, or the context ships the',
      '   whole working tree.',
      '4. Drop privileges: create a user, `USER` it, and make the filesystem',
      '   read-only where the app allows.',
      '5. Rebuild and report before/after size plus the new largest layers.',
      '',
      '## Rules',
      '',
      '- Pin the base image by digest when reproducibility matters; say which you',
      '  chose and why.',
      '- Never bake a secret into a layer — build args stay in the history.',
      '- Combine only the `RUN` steps that share a cache lifetime; merging',
      '  everything makes rebuilds slower, not faster.',
      '- A size claim without the second measurement is not a result.',
      ''
    ].join('\n')
  },
  {
    id: 'api-docs-generator',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'API Docs Generator',
    description: 'Gera documentação de API a partir das rotas e schemas que realmente existem.',
    whenToUse: 'Use quando a API mudou e a documentação ficou para trás.',
    icon: 'book',
    category: 'documentation',
    tags: ['documentação', 'api', 'openapi'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-07-22',
    tools: { allowed: ['Read', 'Glob', 'Write'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: ['Declara allowed-tools.', 'Inclui ferramenta de escrita em arquivos.'],
    files: [{ path: 'SKILL.md' }, { path: 'templates/endpoint.md' }],
    content: [
      '---',
      'name: api-docs-generator',
      'description: "Generate API reference docs from route and schema files. Use when endpoints changed and the docs went stale."',
      'allowed-tools:',
      '  - Read',
      '  - Glob',
      '  - Write',
      '---',
      '',
      '# Goal',
      '',
      'Keep the API reference truthful to the code that actually ships.',
      '',
      '## Workflow',
      '',
      '1. Locate route definitions and request/response schemas across the project.',
      '2. For each endpoint record: method, path, path/query params, request body,',
      '   success response, error responses, and required auth scope.',
      '3. Fill templates/endpoint.md once per endpoint, in registration order.',
      '4. Produce a reconciliation list:',
      '   - endpoints in code but missing from the docs',
      '   - endpoints in the docs but absent from the code',
      '   - fields whose type changed since the docs were written',
      '',
      '## Quality checks',
      '',
      '- Document only what the code proves. Anything undetermined is written as',
      '  `unknown`, never guessed.',
      '- Preserve hand-written prose and examples instead of overwriting them.',
      '- An endpoint with no documented error response is flagged, not left silent.',
      ''
    ].join('\n')
  },
  {
    id: 'excel-analyst',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Excel Analyst',
    description: 'Perfila planilhas e propõe limpeza e validação antes de confiar em qualquer número.',
    whenToUse: 'Use ao receber uma planilha ou CSV e precisar entender e validar o conteúdo.',
    icon: 'table',
    category: 'data',
    tags: ['excel', 'csv', 'análise'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-08-12',
    tools: { allowed: ['Read'], disallowed: [] },
    flags: { scripts: false, shell: null, shellInjection: false, fork: false, hooks: false, portable: true, claudeCodeOnly: false },
    risk: 'low',
    riskFactors: ['Somente leitura de arquivos; nenhuma permissão de escrita ou shell.'],
    files: [{ path: 'SKILL.md' }],
    content: [
      '---',
      'name: excel-analyst',
      'description: "Profile a spreadsheet or CSV and propose a reliable cleaning and exploration workflow. Use before trusting any number from it."',
      'allowed-tools:',
      '  - Read',
      '---',
      '',
      '# Goal',
      '',
      'Turn a raw tabular file into a dataset whose numbers can be defended.',
      '',
      '## Workflow',
      '',
      '1. **Profile before claiming.** Report row count, column names, inferred type',
      '   per column, and how many rows actually look like data.',
      '2. **Find the damage.** Per column: missing values, duplicates, mixed types,',
      '   inconsistent units, stray totals rows, merged headers.',
      '3. **Propose the cleaning order**, cheapest and least destructive first. Each',
      '   step states what it changes and how to undo it.',
      '4. **Say what the data can answer** — and, explicitly, what it cannot.',
      '',
      '## Quality checks',
      '',
      '- Never state an aggregate without saying how many rows it covers.',
      '- Flag a column whose type is ambiguous instead of picking one.',
      '- A total that does not reconcile is reported, not silently corrected.',
      '- Percentages always carry their denominator.',
      ''
    ].join('\n')
  },
  {
    id: 'data-cleaner',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Data Cleaner',
    description: 'Padroniza datasets sujos com um plano reversível e auditável.',
    whenToUse: 'Use quando um dataset tem duplicatas, tipos inconsistentes ou campos faltando.',
    icon: 'database',
    category: 'data',
    tags: ['dados', 'etl', 'qualidade'],
    platform: 'claude',
    compatibility: ['Claude Code', 'Agent Skills'],
    license: null,
    author: 'Comunidade',
    sourceUrl: null,
    updatedAt: '2026-06-18',
    tools: { allowed: ['Read', 'Bash(${CLAUDE_SKILL_DIR}/scripts/profile.sh *)'], disallowed: [] },
    flags: { scripts: true, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: true },
    risk: 'elevated',
    riskFactors: [
      'Declara allowed-tools.',
      'Pré-aprova comandos Bash, restritos ao script do próprio pacote.',
      'Inclui script auxiliar executável no pacote.',
      'Usa ${CLAUDE_SKILL_DIR}, específico de Claude Code.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }, { path: 'scripts/profile.sh' }],
    content: [
      '---',
      'name: data-cleaner',
      'description: "Standardize a messy dataset with a reversible, auditable cleaning plan. Use before analysis or before importing into a system of record."',
      'allowed-tools:',
      '  - Read',
      '  - "Bash(${CLAUDE_SKILL_DIR}/scripts/profile.sh *)"',
      '---',
      '',
      '# Goal',
      '',
      'A cleaning plan another person can audit, rerun and undo.',
      '',
      '## Workflow',
      '',
      '1. Profile the file with `${CLAUDE_SKILL_DIR}/scripts/profile.sh` to get',
      '   types, null counts and cardinality per column.',
      '2. Classify each problem: **structural** (headers, encoding, delimiters),',
      '   **type** (mixed or wrong), **value** (nulls, outliers, units), or',
      '   **identity** (duplicates, missing keys).',
      '3. Propose the steps in that order — structure before values — each with its',
      '   rationale, its rollback, and the check that proves it worked.',
      '4. Apply nothing until the user approves the plan.',
      '5. Record every applied decision in a changelog next to the output.',
      '',
      '## Limits',
      '',
      '- Never delete rows silently. Quarantine them and count them in the report.',
      '- Never widen a type to make an error disappear.',
      '- Never impute a value without labelling the column as imputed.',
      '',
      'See reference.md for the unit-normalization table.',
      ''
    ].join('\n')
  }
]);
