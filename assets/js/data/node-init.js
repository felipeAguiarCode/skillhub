/* ==========================================================================
   data/node-init.js — Claude Skills de inicialização de projeto Node

   Mesmo formato claude-skill dos demais arquivos de dado. Não é coleção nova:
   `type: 'claude-skill'` já põe estas entradas em #/claude-skills. Separado só
   para a leva de scaffolding Node ficar legível de uma vez.

   Uma Skill por tipo de projeto — ESM puro, TypeScript, CLI, API, biblioteca
   publicável e monorepo — porque as decisões de cada um divergem cedo demais
   para caber num arquivo só, parametrizado.
   ========================================================================== */

window.SKILL_HUB_CATALOG = (window.SKILL_HUB_CATALOG || []).concat([
  {
    id: 'node-esm-init',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Node ESM Init',
    description: 'Cria um projeto Node em ESM puro, que roda no runtime sem etapa de build.',
    whenToUse: 'Use ao começar um projeto Node novo que não quer bundler nem transpilador.',
    icon: 'package',
    category: 'development',
    tags: ['node', 'esm', 'scaffolding'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-10',
    tools: {
      allowed: ['Read', 'Glob', 'Write', 'Bash(node --version)', 'Bash(npm init *)', 'Bash(npm install *)', 'Bash(npm test *)'],
      disallowed: ['Bash(npm publish *)']
    },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools com Write: cria arquivos no diretório do projeto.',
      'Pré-aprova npm install, que baixa e executa código de terceiros.',
      'Nega npm publish: inicializar projeto nunca precisa publicar.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: node-esm-init',
      'description: "Scaffold a Node project that runs straight on the runtime: ESM only, node: prefixed built-ins, node:test, no build step. Use when starting a new Node project."',
      'when_to_use: "A new Node project is starting and no bundler or transpiler is wanted."',
      'argument-hint: "<project-name>"',
      'allowed-tools:',
      '  - Read',
      '  - Glob',
      '  - Write',
      '  - Bash(node --version)',
      '  - Bash(npm init *)',
      '  - Bash(npm install *)',
      '  - Bash(npm test *)',
      'disallowed-tools:',
      '  - Bash(npm publish *)',
      '---',
      '',
      '# Goal',
      '',
      'A project where `node src/index.js` and `npm test` both work on a clean',
      'machine, with nothing between the source and the runtime.',
      '',
      '## Before writing anything',
      '',
      '1. **Stop if the target directory exists and is not empty.** Report what is',
      '   already there. Never overwrite a project you did not create.',
      '2. Read `node --version` and record the major. The project declares the',
      '   version it was built against; do not assume one.',
      '3. Ask only what cannot be inferred: package name when it differs from the',
      '   directory, license, and whether the package stays private.',
      '',
      '## Layout',
      '',
      '```',
      '<project-name>/',
      '  package.json',
      '  .gitignore',
      '  .editorconfig',
      '  .nvmrc',
      '  README.md',
      '  src/index.js',
      '  test/index.test.js',
      '```',
      '',
      '## package.json',
      '',
      '- `"type": "module"` — that is the whole point of this setup.',
      '- `"engines": { "node": ">=<major read from node --version>" }`.',
      '- `"main": "src/index.js"`. No `"exports"` block unless another package is',
      '  meant to import this one; that is a different skill.',
      '- Scripts, and only ones that already work:',
      '  - `"start": "node src/index.js"`',
      '  - `"test": "node --test"`',
      '- `"private": true` unless the user said it will be published.',
      '',
      '## Rules the generated code follows',
      '',
      '- Built-ins are imported with the `node:` prefix (`node:fs/promises`, not',
      '  `fs`). It is unambiguous and cannot be shadowed by a dependency.',
      '- No `require`, no `__dirname`. Use `import.meta.dirname` where a path is',
      '  needed.',
      '- Top-level `await` is available here, so no wrapper `main()` just to await.',
      '- Every dependency lands pinned to the version `npm install` actually',
      '  resolved. Read it back from package.json instead of typing a range.',
      '',
      '## Finish by proving it runs',
      '',
      '1. `npm install`, only if a dependency was added. A dependency-free project',
      '   does not need it, and running it anyway just creates noise.',
      '2. `npm test` must pass with one real test that asserts something about',
      '   `src/index.js` — not a placeholder that asserts true.',
      '3. Report both commands and their output. If a command was not run, say',
      '   which and why.',
      '',
      '## Quality checks',
      '',
      '- No `.env` written. If configuration is needed, write `.env.example` and',
      '  put `.env` in `.gitignore`.',
      '- No secret, token or absolute personal path in any generated file.',
      '- `.gitignore` covers `node_modules`, `.env` and log files, and does **not**',
      '  ignore the lockfile — the lockfile is what makes the install reproducible.',
      '- The README names the required Node version and the two commands above.',
      '',
      'See `reference.md` in this skill directory for the field-by-field',
      'package.json checklist.',
      ''
    ].join('\n')
  },
  {
    id: 'node-ts-init',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Node TypeScript Init',
    description: 'Cria um projeto Node em TypeScript com tsconfig estrito e saída compilada em dist.',
    whenToUse: 'Use ao começar um projeto Node novo que vai ser escrito em TypeScript.',
    icon: 'code',
    category: 'development',
    tags: ['node', 'typescript', 'scaffolding'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-10',
    tools: {
      allowed: ['Read', 'Glob', 'Write', 'Bash(node --version)', 'Bash(npm init *)', 'Bash(npm install *)', 'Bash(npm run build *)', 'Bash(npm test *)'],
      disallowed: ['Bash(npm publish *)']
    },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools com Write: cria arquivos no diretório do projeto.',
      'Pré-aprova npm install, que baixa e executa código de terceiros.',
      'Pré-aprova npm run build, que executa o script declarado no package.json.',
      'Nega npm publish: inicializar projeto nunca precisa publicar.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: node-ts-init',
      'description: "Scaffold a Node project in TypeScript: strict tsconfig, NodeNext resolution, src to dist, declarations emitted. Use when starting a new TypeScript project on Node."',
      'when_to_use: "A new Node project is starting and it will be written in TypeScript."',
      'argument-hint: "<project-name>"',
      'allowed-tools:',
      '  - Read',
      '  - Glob',
      '  - Write',
      '  - Bash(node --version)',
      '  - Bash(npm init *)',
      '  - Bash(npm install *)',
      '  - Bash(npm run build *)',
      '  - Bash(npm test *)',
      'disallowed-tools:',
      '  - Bash(npm publish *)',
      '---',
      '',
      '# Goal',
      '',
      'A TypeScript project where `npm run build` produces `dist/` and `npm test`',
      'passes, with the compiler strict from the first commit — loosening it later',
      'is a decision, tightening it later is a migration nobody schedules.',
      '',
      '## Before writing anything',
      '',
      '1. **Stop if the target directory exists and is not empty.** Report what is',
      '   there rather than overwriting it.',
      '2. Read `node --version`; `target` and `engines` both follow from it.',
      '3. Decide how tests run, and **ask instead of guessing**:',
      '   - **Compile first** (default, no extra dependency): `npm run build`, then',
      '     `node --test dist/`.',
      '   - **Strip types at run time**: only if the installed Node advertises it —',
      '     check `node --help` for the flag before writing a script that needs it.',
      '   - **A test runner as a dependency**: one more package to keep updated.',
      '     Only if the user asks for it.',
      '',
      '## Layout',
      '',
      '```',
      '<project-name>/',
      '  package.json',
      '  tsconfig.json',
      '  .gitignore',
      '  .editorconfig',
      '  .nvmrc',
      '  README.md',
      '  src/index.ts',
      '  test/index.test.ts',
      '```',
      '',
      '`dist/` is generated, so it belongs in `.gitignore` and nowhere else.',
      '',
      '## tsconfig.json',
      '',
      'Every option below earns its place. Do not paste a config larger than this.',
      '',
      '- `"strict": true`, plus `"noUncheckedIndexedAccess": true` — indexing an',
      '  array is the most common source of a runtime undefined that the compiler',
      '  otherwise waves through.',
      '- `"module": "nodenext"` and `"moduleResolution": "nodenext"` — this is Node,',
      '  so resolution has to behave like Node, file extensions and all.',
      '- `"verbatimModuleSyntax": true` — what you write is what gets emitted, so',
      '  import elision stops being a mystery.',
      '- `"target"` at the newest level the recorded Node major supports.',
      '- `"rootDir": "src"`, `"outDir": "dist"`, `"declaration": true`,',
      '  `"sourceMap": true`.',
      '- `"include": ["src", "test"]`, so the tests are type-checked too.',
      '',
      '## package.json',
      '',
      '- `"type": "module"`, and `"engines"` with the Node major.',
      '- `typescript` as a **devDependency**, pinned to what npm resolved.',
      '- Scripts: `"build": "tsc"`, `"typecheck": "tsc --noEmit"`,',
      '  `"test": "npm run build && node --test dist/"` (adjusted to the choice',
      '  made above), `"start": "node dist/index.js"`.',
      '- `"private": true` unless the user said it will be published.',
      '',
      '## Finish by proving it compiles and runs',
      '',
      '1. `npm install`.',
      '2. `npm run build` — must finish with no error and produce `dist/index.js`',
      '   together with `dist/index.d.ts`.',
      '3. `npm test` — one real test asserting something exported by `src/index.ts`.',
      '4. Report each command and its output. Never claim a step passed without',
      '   having run it.',
      '',
      '## Quality checks',
      '',
      '- Zero `any` in the generated code, and no `@ts-ignore`. When a type is',
      '  genuinely unknown, use `unknown` and narrow it.',
      '- `dist/` ignored by git; the lockfile committed.',
      '- No `.env` written — `.env.example` instead, with `.env` in `.gitignore`.',
      '- The README names the Node version, the build command and the test command.',
      '',
      'See `reference.md` in this skill directory for each compiler option and the',
      'failure it prevents.',
      ''
    ].join('\n')
  },
  {
    id: 'node-cli-init',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Node CLI Init',
    description: 'Cria uma ferramenta de linha de comando em Node, com bin, parseArgs e códigos de saída honestos.',
    whenToUse: 'Use ao começar um utilitário de terminal em Node, que vai ser executado e não importado.',
    icon: 'terminal',
    category: 'development',
    tags: ['node', 'cli', 'scaffolding'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-10',
    tools: {
      allowed: ['Read', 'Glob', 'Write', 'Bash(node --version)', 'Bash(npm init *)', 'Bash(npm install *)', 'Bash(node ./bin/*)', 'Bash(npm test *)'],
      disallowed: ['Bash(npm link *)', 'Bash(npm publish *)']
    },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools com Write: cria arquivos no diretório do projeto.',
      'Pré-aprova npm install, que baixa e executa código de terceiros.',
      'Pré-aprova executar o próprio bin gerado, para provar que o CLI responde.',
      'Nega npm link, que escreve no prefixo global do npm, fora do projeto.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: node-cli-init',
      'description: "Scaffold a Node command-line tool: bin entry with a shebang, argument parsing with node:util parseArgs, --help and --version, exit codes a script can branch on. Use when starting a new CLI."',
      'when_to_use: "A new command-line tool is starting and it will be executed, not imported."',
      'argument-hint: "<project-name>"',
      'allowed-tools:',
      '  - Read',
      '  - Glob',
      '  - Write',
      '  - Bash(node --version)',
      '  - Bash(npm init *)',
      '  - Bash(npm install *)',
      '  - Bash(node ./bin/*)',
      '  - Bash(npm test *)',
      'disallowed-tools:',
      '  - Bash(npm link *)',
      '  - Bash(npm publish *)',
      '---',
      '',
      '# Goal',
      '',
      'A tool that answers `--help` with no side effect, writes results to stdout',
      'and errors to stderr, and exits with a code another script can branch on.',
      '',
      '## Before writing anything',
      '',
      '1. **Stop if the target directory exists and is not empty.**',
      '2. Ask what the command is called on the terminal, if it differs from the',
      '   package name — the `bin` key is the terminal name and it is what users',
      '   type.',
      '3. Ask what the tool does when given no arguments: print help, or read',
      '   stdin. Never leave it blocking on input that is not coming.',
      '',
      '## Layout',
      '',
      '```',
      '<project-name>/',
      '  package.json',
      '  .gitignore',
      '  .nvmrc',
      '  README.md',
      '  bin/cli.js        thin: parse argv, call src, set the exit code',
      '  src/run.js        the actual work, importable and testable',
      '  test/run.test.js',
      '```',
      '',
      'Keeping the logic out of `bin/` is what lets the tests call a function',
      'instead of spawning a process for every assertion.',
      '',
      '## The bin file',
      '',
      '- First line is exactly `#!/usr/bin/env node`, with **LF endings and no BOM**.',
      '  A CRLF shebang fails on Linux with a message that names no cause.',
      '- Parse with `parseArgs` from `node:util`. It is built in; a flag parser is',
      '  not worth a dependency at the start.',
      '- `--help` and `--version` answer and exit `0`, **even when other arguments',
      '  are wrong**. Help that refuses to print is help nobody reads.',
      '- Exit codes, documented in the README: `0` success, `1` a failure the user',
      '  can act on, `2` a usage error. Anything else is unhandled and should crash',
      '  loudly rather than exit `0`.',
      '',
      '## Output discipline',
      '',
      '- Results on stdout, diagnostics on stderr. That split is what makes the',
      '  tool pipeable into another command.',
      '- No color when stdout is not a TTY, and honor `NO_COLOR`.',
      '- No progress spinner in non-interactive mode; it becomes garbage in a log.',
      '- One line, one fact. A CLI that prints a paragraph per file cannot be',
      '  grepped.',
      '',
      '## package.json',
      '',
      '- `"bin": { "<command>": "bin/cli.js" }` and `"type": "module"`.',
      '- `"engines"` with the Node major read from `node --version`.',
      '- `"files": ["bin", "src"]` if this will ever be published — an allowlist,',
      '  so nothing ships by accident.',
      '- `"test": "node --test"`.',
      '',
      '## Finish by proving it responds',
      '',
      '1. `node ./bin/cli.js --help` — prints usage, exits `0`.',
      '2. `node ./bin/cli.js --version` — prints the version from package.json,',
      '   not a hardcoded string.',
      '3. One real invocation that does the actual work, plus `npm test`.',
      '4. Report every command and its output.',
      '',
      'To install it globally, the user runs `npm link` themselves: it writes into',
      'the global npm prefix, outside this project, so it is their call and not a',
      'step this skill takes.',
      '',
      '## Quality checks',
      '',
      '- No secret read from an argument. Arguments land in shell history and in',
      '  the process list; secrets come from the environment or a file.',
      '- Unknown flags fail with exit `2` and a usage line, never a silent no-op.',
      '- The README shows the install line, one real example and the exit codes.',
      '',
      'See `reference.md` in this skill directory for the `parseArgs` option shapes.',
      ''
    ].join('\n')
  },
  {
    id: 'node-api-init',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Node API Init',
    description: 'Cria um serviço HTTP em Node com config validada no boot, health, shutdown limpo e um smoke test.',
    whenToUse: 'Use ao começar um serviço HTTP em Node que precisa subir e ser testável no primeiro dia.',
    icon: 'server',
    category: 'api',
    tags: ['node', 'api', 'scaffolding'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-10',
    tools: {
      allowed: ['Read', 'Glob', 'Write', 'Bash(node --version)', 'Bash(npm init *)', 'Bash(npm install *)', 'Bash(npm test *)'],
      disallowed: ['Bash(npm publish *)']
    },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools com Write: cria arquivos no diretório do projeto.',
      'Pré-aprova npm install, que baixa e executa código de terceiros.',
      'Escreve configuração de serviço: exige .env.example sem valor real e .env ignorado.',
      'Nega npm publish: um serviço não é publicado no registro.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: node-api-init',
      'description: "Scaffold a Node HTTP service: configuration validated at boot, a health endpoint, graceful shutdown, structured logs, and one smoke test that binds port zero. Use when starting a new HTTP service."',
      'when_to_use: "A new HTTP service is starting and it has to be runnable and testable on day one."',
      'argument-hint: "<project-name>"',
      'allowed-tools:',
      '  - Read',
      '  - Glob',
      '  - Write',
      '  - Bash(node --version)',
      '  - Bash(npm init *)',
      '  - Bash(npm install *)',
      '  - Bash(npm test *)',
      'disallowed-tools:',
      '  - Bash(npm publish *)',
      '---',
      '',
      '# Goal',
      '',
      'A service that refuses to start when it is misconfigured, answers a health',
      'check, shuts down without dropping a request, and has one test that proves',
      'all three.',
      '',
      '## Before writing anything',
      '',
      '1. **Stop if the target directory exists and is not empty.**',
      '2. **Ask which HTTP layer**: `node:http` with no dependency, or the framework',
      '   the team already runs in production. Do not pick a framework on their',
      '   behalf — that choice outlives the scaffold by years.',
      '3. Ask which environment variables the service needs, and which of them are',
      '   secrets. A variable nobody can name today becomes a default nobody',
      '   reviews tomorrow.',
      '',
      '## Layout',
      '',
      '```',
      '<project-name>/',
      '  package.json',
      '  .env.example',
      '  .gitignore',
      '  .nvmrc',
      '  README.md',
      '  src/config.js     reads and validates process.env, once',
      '  src/app.js        builds the handler, exports it, binds nothing',
      '  src/server.js     binds the port, wires the signals',
      '  test/health.test.js',
      '```',
      '',
      'The `app` / `server` split is not ceremony: it is what lets the test start',
      'the app on port `0` and never fight over a fixed port.',
      '',
      '## Configuration',
      '',
      '- `config.js` reads `process.env` **once**, validates, and throws at boot',
      '  naming the variable that is missing. A service that starts and then fails',
      '  on the first request is harder to diagnose than one that refuses to start.',
      '- **No default for a secret.** A fallback password is an incident waiting for',
      '  a quiet deploy. Defaults are fine for port and log level, nothing else.',
      '- `.env.example` lists every variable with an empty or obviously fake value,',
      '  and `.env` goes in `.gitignore`.',
      '',
      '## Runtime rules',
      '',
      '- `GET /health` returns `200` and a small body, and **checks nothing',
      '  external**. A health check that queries the database turns a slow',
      '  dependency into an outage.',
      '- On `SIGTERM` and `SIGINT`: stop accepting connections, let in-flight',
      '  requests finish, force exit after a stated timeout, and log which of the',
      '  two paths was taken.',
      '- Logs are one JSON object per line on stdout. Never log an authorization',
      '  header, a token or a full request body.',
      '- `unhandledRejection` and `uncaughtException` are logged and exit non-zero.',
      '  Swallowing them keeps a broken process alive and serving errors.',
      '- Every outbound call gets a timeout. Without one, a hung dependency becomes',
      '  a hung service.',
      '',
      '## Finish by proving it boots',
      '',
      '1. `npm install`.',
      '2. `npm test` — the smoke test starts the app on port `0`, calls `/health`,',
      '   asserts `200`, and closes the server in a teardown.',
      '3. Report the commands and their output.',
      '',
      '## Quality checks',
      '',
      '- No `.env` committed and no real credential anywhere in the tree.',
      '- No fixed port in any test.',
      '- The README has the run command, the test command and a table of every',
      '  environment variable with whether it is required.',
      '- No Dockerfile unless it was asked for — containerizing is a separate task',
      '  with its own decisions.',
      '',
      'See `reference.md` in this skill directory for the shutdown sequence in full.',
      ''
    ].join('\n')
  },
  {
    id: 'node-lib-init',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Node Library Init',
    description: 'Cria um pacote npm publicável, com exports, lista de arquivos explícita e conferência do que vai no tarball.',
    whenToUse: 'Use ao começar um pacote que outros projetos vão instalar do registro.',
    icon: 'hexagon',
    category: 'development',
    tags: ['node', 'biblioteca', 'npm', 'scaffolding'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-10',
    tools: {
      allowed: ['Read', 'Glob', 'Write', 'Bash(node --version)', 'Bash(npm init *)', 'Bash(npm install *)', 'Bash(npm view *)', 'Bash(npm pack *)', 'Bash(npm test *)'],
      disallowed: ['Bash(npm publish *)', 'Bash(npm version *)']
    },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools com Write: cria arquivos no diretório do projeto.',
      'Pré-aprova npm install, que baixa e executa código de terceiros.',
      'Pré-aprova npm view, que consulta o registro público pela rede.',
      'Nega npm publish: publicar é irreversível na prática e é decisão de quem pediu.',
      'Nega npm version, que cria commit e tag no repositório.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: node-lib-init',
      'description: "Scaffold a publishable npm package: exports map, an explicit files allowlist, semver starting at 0.1.0, and npm pack --dry-run to see exactly what would ship. Use when starting a package other projects will install."',
      'when_to_use: "A new package is starting that will be installed by other projects."',
      'argument-hint: "<package-name>"',
      'allowed-tools:',
      '  - Read',
      '  - Glob',
      '  - Write',
      '  - Bash(node --version)',
      '  - Bash(npm init *)',
      '  - Bash(npm install *)',
      '  - Bash(npm view *)',
      '  - Bash(npm pack *)',
      '  - Bash(npm test *)',
      'disallowed-tools:',
      '  - Bash(npm publish *)',
      '  - Bash(npm version *)',
      '---',
      '',
      '# Goal',
      '',
      'A package whose published contents are known before the first publish, with',
      'one documented entry point and a version number that tells the truth.',
      '',
      '## Before writing anything',
      '',
      '1. **Stop if the target directory exists and is not empty.**',
      '2. `npm view <name> version` to see whether the name is taken. A taken name',
      '   is cheaper to discover now than after the README is written.',
      '3. Ask: scoped (`@org/name`) or bare; which license; and **ESM only or dual',
      '   ESM plus CommonJS**. Recommend ESM only unless a known consumer needs',
      '   CommonJS — dual means two builds, two resolution paths and two ways to',
      '   break.',
      '',
      '## Layout',
      '',
      '```',
      '<package-name>/',
      '  package.json',
      '  LICENSE',
      '  README.md',
      '  .gitignore',
      '  .npmrc            only if a registry or scope needs configuring',
      '  src/index.js      the only public entry point',
      '  src/*.js          internals, not reachable from outside',
      '  test/index.test.js',
      '```',
      '',
      '## package.json, field by field',
      '',
      '- `"exports"` is the public surface. Declare `"."` and nothing else unless a',
      '  second entry point was asked for. Put the `"types"` condition **first**;',
      '  conditions are matched in order.',
      '- `"files": ["dist"]` — or `["src"]` when there is no build. An allowlist is',
      '  the only way to know what ships; `.npmignore` is a denylist and it forgets.',
      '- `"version": "0.1.0"`. Below 1.0 the API is allowed to move, and saying so',
      '  is more honest than a 1.0.0 that breaks next week.',
      '- `"sideEffects": false` **only if it is true** — it is a promise to bundlers,',
      '  and a wrong one silently deletes code from a consumer bundle.',
      '- `"publishConfig": { "access": "public" }` for a scoped package, otherwise',
      '  the first publish is rejected.',
      '- `"prepublishOnly": "npm test"` (plus the build when there is one).',
      '- `"repository"`, `"license"` and `"engines"` filled in, not left as defaults.',
      '',
      '## Finish by inspecting the tarball',
      '',
      '1. `npm install` and `npm test`.',
      '2. `npm pack --dry-run`, then **read the file list out loud**. A test file, a',
      '   `.env`, a `.github/` directory or a source map nobody asked for means the',
      '   allowlist is wrong — fix it and pack again.',
      '3. Report the file list and the unpacked size.',
      '',
      '**Do not publish.** Publishing is effectively irreversible: the unpublish',
      'window is narrow and other projects may already depend on the version. The',
      'user runs `npm publish` when they are ready.',
      '',
      '## Quality checks',
      '',
      '- Everything exported is documented in the README with a runnable example.',
      '- No deep import path is promised in the README that `"exports"` does not',
      '  actually expose.',
      '- `LICENSE` exists and matches the `"license"` field.',
      '- The lockfile is committed, and no dependency was added that the package',
      '  does not use.',
      '',
      'See `reference.md` in this skill directory for the exports map shapes, ESM',
      'only and dual.',
      ''
    ].join('\n')
  },
  {
    id: 'node-monorepo-init',
    type: 'claude-skill',
    tool: 'claude-code',
    format: 'claude-skill',
    name: 'Node Monorepo Init',
    description: 'Cria um monorepo Node com workspaces: um lockfile na raiz, pacotes em packages e scripts que se espalham.',
    whenToUse: 'Use quando vários pacotes relacionados vão viver no mesmo repositório.',
    icon: 'layers',
    category: 'architecture',
    tags: ['node', 'monorepo', 'workspaces', 'scaffolding'],
    platform: 'claude',
    compatibility: ['Claude Code'],
    license: 'MIT',
    author: 'Skill Hub Editorial',
    sourceUrl: null,
    updatedAt: '2026-09-10',
    tools: {
      allowed: ['Read', 'Glob', 'Write', 'Bash(node --version)', 'Bash(npm init *)', 'Bash(npm install *)', 'Bash(npm run *)', 'Bash(npm test *)'],
      disallowed: ['Bash(npm publish *)']
    },
    flags: { scripts: false, shell: 'bash', shellInjection: false, fork: false, hooks: false, portable: false, claudeCodeOnly: false },
    risk: 'moderate',
    riskFactors: [
      'Declara allowed-tools com Write: cria arquivos em vários pacotes de uma vez.',
      'Pré-aprova npm install na raiz, que resolve o grafo inteiro do workspace.',
      'Pré-aprova npm run, que executa os scripts declarados em cada pacote.',
      'Nega npm publish: publicar pacote de workspace é decisão separada.'
    ],
    files: [{ path: 'SKILL.md' }, { path: 'reference.md' }],
    content: [
      '---',
      'name: node-monorepo-init',
      'description: "Scaffold a Node workspaces monorepo: one lockfile at the root, packages under packages/, internal dependencies referenced by package name, root scripts that fan out. Use when several packages will live in one repository."',
      'when_to_use: "Several related packages will be developed in one repository."',
      'argument-hint: "<repo-name>"',
      'allowed-tools:',
      '  - Read',
      '  - Glob',
      '  - Write',
      '  - Bash(node --version)',
      '  - Bash(npm init *)',
      '  - Bash(npm install *)',
      '  - Bash(npm run *)',
      '  - Bash(npm test *)',
      'disallowed-tools:',
      '  - Bash(npm publish *)',
      '---',
      '',
      '# Goal',
      '',
      'A repository where one install at the root wires every package together,',
      'and a package imports its sibling by package name — never by a relative',
      'path that climbs out of its own directory.',
      '',
      '## Before writing anything',
      '',
      '1. **Stop if the target directory exists and is not empty.**',
      '2. **Ask which package manager.** npm, pnpm and yarn spell workspaces and',
      '   the internal dependency differently, and guessing produces a lockfile the',
      '   team cannot install. This question comes before any file is written.',
      '3. Ask how many packages to create now. Two is enough to prove the wiring;',
      '   more is guesswork about a structure nobody has used yet.',
      '4. Ask whether TypeScript is in play — it adds a shared base config and',
      '   project references, and retrofitting those is worse than starting with',
      '   them.',
      '',
      '## Layout',
      '',
      '```',
      '<repo-name>/',
      '  package.json          private, declares the workspaces',
      '  .gitignore',
      '  .nvmrc                the Node version, declared once for everyone',
      '  README.md',
      '  packages/',
      '    <a>/package.json',
      '    <a>/src/index.js',
      '    <b>/package.json    depends on <a>',
      '    <b>/src/index.js',
      '    <b>/test/index.test.js',
      '```',
      '',
      '## The root package.json',
      '',
      '- `"private": true`. The root is never published, and without this flag a',
      '  stray publish at the root tries to ship the whole repository.',
      '- `"workspaces": ["packages/*"]` for npm and yarn; pnpm uses',
      '  `pnpm-workspace.yaml` instead. Follow the manager that was chosen.',
      '- Scripts **fan out** instead of naming packages:',
      '  `"test": "npm test --workspaces --if-present"`, and the same shape for',
      '  build and lint. A root script that lists package names rots the day',
      '  someone adds a package.',
      '- No source code at the root. The root is wiring, not a package.',
      '',
      '## The internal dependency',
      '',
      '- npm and yarn: depend on the sibling by **its package name**, with `"*"` as',
      '  the range. The install links the local folder.',
      '- pnpm: the same, with `"workspace:*"`.',
      '- Never a relative path in a dependency field, and never an import that',
      '  reaches across packages (`../../<b>/src/...`). Such an import works',
      '  locally and breaks the moment either package is published or moved.',
      '',
      '## Rules that keep it from rotting',
      '',
      '- **One lockfile, at the root, committed.** A lockfile inside a package means',
      '  the workspace is not actually wired.',
      '- **No cycles.** If `<b>` depends on `<a>`, then `<a>` never depends on',
      '  `<b>`. Write the direction down in the README while it is still obvious.',
      '- Shared configuration (tsconfig base, editorconfig, lint config) lives at',
      '  the root and is extended, not copied into each package.',
      '- Each package owns its own build and test scripts; the root only',
      '  orchestrates them.',
      '',
      '## Finish by proving the wiring',
      '',
      '1. `npm install` at the root — one lockfile appears, and `<b>` resolves',
      '   `<a>` to the local folder.',
      '2. `npm test` at the root, with a real test in `<b>` that imports `<a>` **by',
      '   package name** and asserts on what it returns. That single test is the',
      '   proof the workspace works.',
      '3. Report both commands and their output.',
      '',
      '## Quality checks',
      '',
      '- `node_modules` ignored, lockfile committed.',
      '- Every package has a `name`, a `version`, and either `"private": true` or a',
      '  deliberate publish configuration.',
      '- The README shows the layout, the dependency direction and the root',
      '  commands.',
      '',
      'See `reference.md` in this skill directory for the workspace syntax of each',
      'package manager.',
      ''
    ].join('\n')
  }
]);
