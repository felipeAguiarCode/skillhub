/* ==========================================================================
   data/coding-styles-extra.js — window.SKILL_HUB_CODING_STYLES

   ARQUIVO GERADO. Não editar à mão.

   Os guias de estilo do usuário moram fora do repositório, em
   ~/.claude/coding-styles/. O navegador não lê aquele caminho e o app não faz
   fetch (ADR-004), então o texto entra aqui como dado estático — o mesmo
   tratamento que o catálogo recebe.

   A coleção é dividida em dois arquivos por profundidade, e cada um faz
   concat: a ordem entre eles não importa, só precisam vir antes de
   catalog.js no index.html.

   Para regerar depois de editar um guia, ver a seção Coding Styles do
   CLAUDE.md. O conteúdo é idêntico ao arquivo de origem, byte a byte.
   ========================================================================== */

window.SKILL_HUB_CODING_STYLES = (window.SKILL_HUB_CODING_STYLES || []).concat([
  {
    id: 'node-typescript-essential',
    title: 'Node.js + TypeScript',
    area: 'backend',
    depth: 'essential',
    family: 'node-typescript',
    summary: 'As regras que mais mudam decisão, condensadas: paradigma funcional, prefixo como contrato, types sem any, Promise.all e erro de domínio com código.',
    icon: 'server',
    tags: ['referência', 'funcional', 'types'],
    stack: ['Node.js', 'TypeScript'],
    fileName: 'node-typescript-essential.md',
    source: '~/.claude/coding-styles/node-typescript-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# 🧭 Node.js + TypeScript — Essencial',
      '',
      '> Cartão de referência. A versão completa é `node-typescript.md`.',
      '',
      'Só as regras que mais mudam decisão ao escrever código. Quando faltar um caso,',
      'o guia completo decide. Não define arquitetura de software.',
      '',
      '---',
      '',
      '# 🧠 1. Paradigma',
      '',
      'Função e dado. Sem `class`, sem herança, sem `this`.',
      '',
      '❌',
      '',
      '```ts',
      'class UserService {',
      '  constructor(private readonly repo: UserRepository) {}',
      '  async create(input: CreateUserInput) { /* ... */ }',
      '}',
      '```',
      '',
      '✅',
      '',
      '```ts',
      'async function createUser(input: CreateUserInput, repo: UserRepository) {',
      '  /* ... */',
      '}',
      '```',
      '',
      'Dependência entra como parâmetro. Módulo exporta funções, não um objeto de',
      'serviço.',
      '',
      '---',
      '',
      '# 🔤 2. Convenções de case',
      '',
      '| Elemento           | Padrão             | Exemplo            |',
      '| ------------------ | ------------------ | ------------------ |',
      '| Variáveis, funções | `camelCase`        | `createUser`       |',
      '| Parâmetros, props  | `camelCase`        | `userId`           |',
      '| Constantes globais | `UPPER_SNAKE_CASE` | `MAX_RETRY_COUNT`  |',
      '| Types              | `PascalCase`       | `CreateUserInput`  |',
      '| Arquivos, pastas   | `kebab-case`       | `create-user.ts`   |',
      '| Rotas              | `kebab-case`       | `/payment-methods` |',
      '| Error codes        | `UPPER_SNAKE_CASE` | `USER_NOT_FOUND`   |',
      '| Env variables      | `UPPER_SNAKE_CASE` | `DATABASE_URL`     |',
      '',
      'Código, nomes e comentários em inglês. Texto de interface no idioma do produto.',
      '',
      '---',
      '',
      '# 📛 3. Prefixo diz o contrato',
      '',
      'O prefixo é promessa sobre o retorno, não estilo:',
      '',
      '| Prefixo  | Promessa                        | Retorno         |',
      '| -------- | ------------------------------- | --------------- |',
      '| `get`    | o recurso existe                | `User`          |',
      '| `find`   | pode não existir                | `User \\| null`  |',
      '| `fetch`  | vem de fonte externa            | `User`          |',
      '| `load`   | carregamento                    | `Config`        |',
      '| `list`   | coleção                         | `User[]`        |',
      '| `is/has` | booleano                        | `boolean`       |',
      '',
      '✅',
      '',
      '```ts',
      'function getUserById(userId: string): User { /* ... */ }',
      'function findUserByEmail(email: string): User | null { /* ... */ }',
      'function listOrders(userId: string): Order[] { /* ... */ }',
      '```',
      '',
      'Escrita: `create`, `add`, `insert`, `update`, `set`, `save`, `delete`, `remove`.',
      '',
      'Coleção no plural, sem sufixo redundante: `users`, não `usersList`. Unidade no',
      'nome quando houver ambiguidade: `timeoutMs`, `cacheTtlSeconds`.',
      '',
      '---',
      '',
      '# 🧬 4. Types',
      '',
      '`type`, não `interface`. Union fechada em vez de string solta.',
      '',
      '✅',
      '',
      '```ts',
      'type UserStatus = \'active\' | \'inactive\' | \'blocked\';',
      '',
      'type CreateUserInput = {',
      '  name: string;',
      '  email: string;',
      '  status: UserStatus;',
      '};',
      '```',
      '',
      '`import type` para o que só existe em tipo — o import desaparece no build.',
      '',
      '```ts',
      'import type { User } from \'./user-types\';',
      '```',
      '',
      '---',
      '',
      '# 🚫 5. `any`, `null` e `undefined`',
      '',
      '`any` desliga o compilador. Use `unknown` e faça narrowing.',
      '',
      '❌',
      '',
      '```ts',
      'function parse(payload: any) { return payload.data.id; }',
      '```',
      '',
      '✅',
      '',
      '```ts',
      'function parse(payload: unknown) {',
      '  if (!isPayload(payload)) throw new InvalidPayloadError();',
      '  return payload.data.id;',
      '}',
      '```',
      '',
      '`null` é ausência deliberada; `undefined` é "não informado". Escolha um por',
      'campo e não misture os dois no mesmo contrato.',
      '',
      '---',
      '',
      '# 🔄 6. Imutabilidade',
      '',
      'Construir novo, não mutar.',
      '',
      '❌',
      '',
      '```ts',
      'users.push(user);',
      'user.name = \'Felipe\';',
      '```',
      '',
      '✅',
      '',
      '```ts',
      'const updatedUsers = [...users, user];',
      'const updatedUser = { ...user, name: \'Felipe\' };',
      '```',
      '',
      '`const` por padrão. `let` só quando a reatribuição é o ponto. Nunca mute o array',
      'que está sendo iterado.',
      '',
      '---',
      '',
      '# 🧱 7. Parâmetros e early return',
      '',
      'Mais de dois parâmetros relacionados: objeto nomeado.',
      '',
      '✅',
      '',
      '```ts',
      'function createUser({ name, email, role }: CreateUserInput) { /* ... */ }',
      '```',
      '',
      'Guard clause primeiro, separada por uma linha em branco. Sem `else` depois de',
      '`return`.',
      '',
      '✅',
      '',
      '```ts',
      'function processUser(user: User | null) {',
      '  if (!user) return;',
      '',
      '  if (!user.isActive) return;',
      '',
      '  sendEmail(user.email);',
      '}',
      '```',
      '',
      'Máximo de dois níveis de indentação dentro de uma função.',
      '',
      '---',
      '',
      '# ⚡ 8. Async',
      '',
      '`async/await`, nunca `.then` encadeado. Independente vai em paralelo.',
      '',
      '❌',
      '',
      '```ts',
      'const user = await getUser(userId);',
      'const orders = await listOrders(userId);',
      '```',
      '',
      '✅',
      '',
      '```ts',
      'const [user, orders] = await Promise.all([',
      '  getUser(userId),',
      '  listOrders(userId)',
      ']);',
      '```',
      '',
      'Sequencial só quando o segundo depende do primeiro. `await` dentro de laço sobre',
      'chamadas independentes é o mesmo erro em outra forma.',
      '',
      '---',
      '',
      '# ⚠️ 9. Erros',
      '',
      'Erro de domínio com código, nunca identificado por mensagem.',
      '',
      '✅',
      '',
      '```ts',
      'class UserNotFoundError extends Error {',
      '  code = \'USER_NOT_FOUND\';',
      '}',
      '',
      'if (error instanceof UserNotFoundError) { /* ... */ }',
      '```',
      '',
      '❌',
      '',
      '```ts',
      'if (error.message === \'User not found\') { /* ... */ }',
      '```',
      '',
      'Nunca engula: `catch` que não trata, relança. Mensagem descreve o que falhou,',
      'sem vazar detalhe interno para a resposta HTTP.',
      '',
      '---',
      '',
      '# ↕️ 10. Espaçamento',
      '',
      '```text',
      '0 linhas  → dentro do mesmo bloco lógico',
      '1 linha   → entre blocos lógicos, entre funções, entre types',
      '0 linhas  → após {, antes de }, entre } else {, entre } catch {',
      'máximo de 1 linha vazia consecutiva',
      '```',
      '',
      'Uma linha antes do `return` quando houve processamento. Formatter decide o',
      'visual; não alinhe colunas à mão.',
      '',
      '---',
      '',
      '# 🌐 11. Rotas e status',
      '',
      'Recurso no plural, `kebab-case`, sem verbo:',
      '',
      '```text',
      'GET    /v1/users',
      'POST   /v1/users',
      'GET    /v1/users/:userId/orders',
      '```',
      '',
      'Sem verbo em CRUD (`/getUsers` é erro), sem trailing slash, sem extensão.',
      '`201` cria, `204` deleta, `422` falha de validação, `409` conflito.',
      '',
      '---',
      '',
      '# 🛡️ 12. Fronteiras',
      '',
      'Todo dado externo é não confiável: body, query, header, env, webhook, resposta',
      'de API. Valide na entrada com schema e passe a forma validada para dentro.',
      '',
      'Env lida e validada na inicialização, para o processo morrer cedo. Nunca',
      'registre senha, token, chave, cookie sensível ou dado financeiro completo.',
      '',
      '---',
      '',
      '# 🚦 13. Checklist final',
      '',
      '* [ ] sem `class`, sem herança, sem `this`',
      '* [ ] código em inglês, `camelCase` em função e variável',
      '* [ ] prefixo compatível com o retorno (`find` devolve `| null`)',
      '* [ ] `type` em vez de `interface`, `import type` onde cabe',
      '* [ ] sem `any`; `unknown` com narrowing',
      '* [ ] `const` por padrão, nada mutado no lugar',
      '* [ ] mais de dois parâmetros relacionados viraram objeto',
      '* [ ] guard clause no topo, no máximo dois níveis de indentação',
      '* [ ] chamadas independentes em `Promise.all`',
      '* [ ] erro de domínio com `code`, identificado por tipo',
      '* [ ] no máximo uma linha vazia consecutiva',
      '* [ ] entrada externa validada na fronteira',
      '* [ ] nenhum segredo no código, nada sensível em log',
      '',
      '---',
      '',
      '# 🏁 14. Regras principais',
      '',
      '> **Função e dado. `class` é a exceção que não aconteceu.**',
      '',
      '> **O prefixo é contrato: `find` devolve `null`, `get` não.**',
      '',
      '> **`any` é uma decisão de desligar o compilador.**',
      '',
      '> **Independente vai em paralelo.**',
      '',
      '> **Valide na fronteira, confie no núcleo.**'
    ].join('\n')
  },
  {
    id: 'vanilla-web-essential',
    title: 'Vanilla Web',
    area: 'frontend',
    depth: 'essential',
    family: 'vanilla-web',
    summary: 'O cartão de referência da stack sem build: namespace único, ordem de script, zero innerHTML, estado fora do DOM e teardown de listener.',
    icon: 'panel',
    tags: ['referência', 'sem build', 'dom'],
    stack: ['HTML', 'CSS', 'JavaScript'],
    fileName: 'vanilla-web-essential.md',
    source: '~/.claude/coding-styles/vanilla-web-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# 🌿 Vanilla Web — Essencial',
      '',
      '> Cartão de referência. A versão completa é `vanilla-web.md`.',
      '',
      'Só as regras que mais mudam decisão em HTML, CSS e JavaScript puro, sem build',
      'step. Não define arquitetura de aplicação.',
      '',
      '---',
      '',
      '# 🧱 1. A restrição que define tudo',
      '',
      'Se o app tem de abrir por `file://` sem flags, então:',
      '',
      '* não há ES modules — `import`/`export` falham em `file://` por CORS',
      '* não há `fetch` de arquivo local do próprio projeto',
      '* não há transpilação: o JavaScript escrito é o JavaScript que roda',
      '',
      'Todo o resto é consequência disso.',
      '',
      '---',
      '',
      '# 📦 2. Namespace único',
      '',
      'Cada arquivo é um IIFE que pendura sua API num único objeto global.',
      '',
      '✅',
      '',
      '```js',
      'window.App = window.App || {};',
      '',
      '(function (App) {',
      '  \'use strict\';',
      '',
      '  function normalizeEmail(email) {',
      '    return email.trim().toLowerCase();',
      '  }',
      '',
      '  App.text = { normalizeEmail: normalizeEmail };',
      '})(window.App);',
      '```',
      '',
      'Função solta no escopo global colide com a próxima pessoa que tiver a mesma',
      'ideia.',
      '',
      '---',
      '',
      '# 🔢 3. Ordem de `<script>` é dependência real',
      '',
      'Sem bundler, a ordem no HTML **é** o grafo de dependências: dependência antes de',
      'quem depende, bootstrap por último. Ao acrescentar um arquivo, insira na posição',
      'certa — não no fim por preguiça. Nada valida isso em runtime.',
      '',
      '---',
      '',
      '# 🧬 4. Dado estático é script, não `fetch`',
      '',
      '✅',
      '',
      '```js',
      'window.APP_CATALOG = (window.APP_CATALOG || []).concat([',
      '  { id: \'first\', title: \'First\' }',
      ']);',
      '```',
      '',
      'Cada arquivo de dado concatena no mesmo array, então a ordem entre eles não',
      'importa — só precisam vir antes de quem consome. `fetch(\'./data.json\')` quebra',
      'em `file://`.',
      '',
      '---',
      '',
      '# 🛡️ 5. Nunca `innerHTML`',
      '',
      'A regra mais importante. Construa DOM com uma fábrica que **não tem** caminho',
      'para injetar HTML: string em `children` sempre vira `createTextNode`, e não',
      'existe `props.html`.',
      '',
      '❌',
      '',
      '```js',
      'node.innerHTML = \'<p>\' + item.title + \'</p>\';',
      '```',
      '',
      '✅',
      '',
      '```js',
      'node.appendChild(el(\'p\', null, item.title));',
      '```',
      '',
      'Toda URL de dado externo passa por uma barreira que rejeita o que não for',
      '`http:`, `https:` ou `mailto:` — `javascript:` num `href` executa.',
      '',
      '---',
      '',
      '# 🚫 6. Nunca executar conteúdo de terceiros',
      '',
      'Sem `eval`, sem `new Function`, sem `setTimeout(\'...\')` com string. Conteúdo de',
      'terceiros é exibido como **texto**, nunca interpretado.',
      '',
      '---',
      '',
      '# 🎛️ 7. Estado num objeto, nunca no DOM',
      '',
      'O DOM é saída, não fonte de verdade.',
      '',
      '❌',
      '',
      '```js',
      'const isActive = button.classList.contains(\'is-active\');',
      '```',
      '',
      '✅',
      '',
      '```js',
      'const state = { isActive: false };',
      '```',
      '',
      'Ler estado do DOM cria duas verdades e um bug quando elas divergem. O estado',
      'fica num objeto serializável, e o DOM é derivado dele.',
      '',
      '---',
      '',
      '# 🔄 8. Re-render dirigido por região',
      '',
      'Digitar num campo atualiza **só** a região afetada; mudança estrutural',
      're-renderiza o bloco. Recriar o nó que tem foco tira o caret do lugar — por isso',
      'chip e contagem são atualizados **no lugar**, sem recriar.',
      '',
      '---',
      '',
      '# 🧹 9. Um listener, e teardown de verdade',
      '',
      '✅',
      '',
      '```js',
      'function render() {',
      '  document.removeEventListener(\'keydown\', onKeydown);',
      '  document.addEventListener(\'keydown\', onKeydown);',
      '}',
      '',
      'function teardown() {',
      '  document.removeEventListener(\'keydown\', onKeydown);',
      '  window.clearTimeout(timer);',
      '  window.cancelAnimationFrame(frame);',
      '  refs = {};',
      '}',
      '```',
      '',
      'Remover antes de adicionar torna um teardown perdido não fatal. Sem isso,',
      'navegar cinco vezes deixa cinco handlers e cada tecla dispara cinco vezes.',
      '',
      'Onde a lista é longa, um listener no contêiner com `event.target.closest(...)`,',
      'não um por item.',
      '',
      '---',
      '',
      '# 🎨 10. CSS em camadas, com token',
      '',
      'Camadas em ordem explícita: tokens → base → layout → componentes → páginas →',
      'utilitários → responsivo. **Nenhum valor visual novo sem token equivalente.**',
      '',
      'Estilo estático vive no CSS; `style` em JS só para valor calculado em runtime.',
      '',
      'Duas armadilhas da mesma família:',
      '',
      '* item de grid ou flex não encolhe abaixo do conteúdo sem `min-width: 0` /',
      '  `min-height: 0`',
      '* linha de grid dimensionada por `auto` **também** não encolhe — para uma coluna',
      '  com região que rola, use flex:',
      '',
      '```css',
      '.panel      { display: flex; flex-direction: column; max-height: 80vh; }',
      '.panel__list { flex: 1; min-height: 0; overflow-y: auto; }',
      '```',
      '',
      '`flex: 1` mais `min-height: 0` é o par que cria contêiner de rolagem de verdade.',
      '',
      '---',
      '',
      '# 🧮 11. Caminhos sempre relativos',
      '',
      '`assets/js/app.js`, nunca `/assets/js/app.js`. Caminho absoluto funciona no',
      '`localhost` e quebra quando o site é servido de uma subpasta.',
      '',
      '---',
      '',
      '# ⏱️ 12. Movimento',
      '',
      'Ligue a transição **depois** do primeiro paint, senão a página abre animando.',
      'E respeite a preferência do sistema globalmente:',
      '',
      '```css',
      '@media (prefers-reduced-motion: reduce) {',
      '  *, *::before, *::after {',
      '    animation-duration: .01ms !important;',
      '    transition-duration: .01ms !important;',
      '  }',
      '}',
      '```',
      '',
      '---',
      '',
      '# 💾 13. `localStorage` com `try/catch` sempre',
      '',
      'O acessor **lança** em modo restrito, não só devolve vazio. Toda leitura e toda',
      'escrita em `try/catch`, e a página tem de renderizar certo sem valor guardado.',
      '',
      '---',
      '',
      '# 🔤 14. Naming e abstração',
      '',
      'Código, nomes e comentários em inglês; UI no idioma do produto. Arquivo em',
      '`kebab-case`, função em `camelCase`, constante global em `UPPER_SNAKE_CASE`.',
      '',
      '**Sem abstração de uso único**: um pub/sub sem assinante é código morto com',
      'aparência de arquitetura. Comentário explica o **porquê**, não o quê — de',
      'preferência a armadilha que custou tempo.',
      '',
      '---',
      '',
      '# 🚦 15. Checklist final',
      '',
      '* [ ] abre por `file://` sem flags e em subpasta',
      '* [ ] cada arquivo é IIFE pendurado num namespace único',
      '* [ ] ordem de `<script>` correta, bootstrap por último',
      '* [ ] dado estático como script com `concat`, nenhum `fetch` local',
      '* [ ] zero `innerHTML`; toda URL passa por barreira de protocolo',
      '* [ ] nenhum `eval`/`new Function`; conteúdo de terceiros como texto',
      '* [ ] estado em objeto, DOM nunca é fonte de verdade',
      '* [ ] listener removido antes de adicionado, `teardown` real',
      '* [ ] nenhum valor visual sem token; `min-width: 0` onde encolhe',
      '* [ ] nenhum caminho absoluto',
      '* [ ] `prefers-reduced-motion` respeitado',
      '* [ ] `localStorage` sempre em `try/catch`',
      '* [ ] sem overflow horizontal de 360 a 1920 px',
      '* [ ] todo input com label, todo botão com nome acessível, foco visível',
      '* [ ] zero erro de console, 404 incluído',
      '',
      '---',
      '',
      '# 🏁 16. Regras principais',
      '',
      '> **`file://` é a restrição. Todo o resto é consequência.**',
      '',
      '> **Zero `innerHTML`, por construção — não por disciplina.**',
      '',
      '> **O DOM é saída, nunca fonte de verdade.**',
      '',
      '> **Remova o listener antes de adicionar.**',
      '',
      '> **Nenhum valor visual novo sem token equivalente.**'
    ].join('\n')
  },
  {
    id: 'react-typescript-essential',
    title: 'React + TypeScript',
    area: 'frontend',
    depth: 'essential',
    family: 'react-typescript',
    summary: 'O essencial de componente: ordem estável de hooks, nada de estado derivado, useEffect só para o mundo externo e key por identidade.',
    icon: 'layers',
    tags: ['referência', 'hooks', 'componentes'],
    stack: ['React', 'TypeScript'],
    fileName: 'react-typescript-essential.md',
    source: '~/.claude/coding-styles/react-typescript-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# ⚛️ React + TypeScript — Essencial',
      '',
      '> Cartão de referência. A versão completa é `react-typescript.md`.',
      '',
      'Só as regras que mais mudam decisão ao escrever componente. Não define',
      'arquitetura de aplicação nem escolha de biblioteca de estado.',
      '',
      '---',
      '',
      '# 🧩 1. Componente é função',
      '',
      'Sem `class`, sem `React.FC`.',
      '',
      '✅',
      '',
      '```tsx',
      'type UserCardProps = {',
      '  user: User;',
      '  onSelect: (userId: string) => void;',
      '};',
      '',
      'export function UserCard({ user, onSelect }: UserCardProps) {',
      '  return (',
      '    <button type="button" onClick={() => onSelect(user.id)}>',
      '      {user.name}',
      '    </button>',
      '  );',
      '}',
      '```',
      '',
      '`React.FC` adiciona `children` implícito e complica generics sem dar nada em',
      'troca.',
      '',
      '---',
      '',
      '# 🏷️ 2. Naming',
      '',
      '| Elemento           | Padrão                 | Exemplo               |',
      '| ------------------ | ---------------------- | --------------------- |',
      '| Componente         | `PascalCase`           | `UserCard`            |',
      '| Props type         | `PascalCase` + `Props` | `UserCardProps`       |',
      '| Hook               | `use` + `camelCase`    | `useUserProfile`      |',
      '| Handler interno    | `handle` + evento      | `handleSubmit`        |',
      '| Prop de callback   | `on` + evento          | `onSelect`            |',
      '| Arquivo            | `kebab-case`           | `user-card.tsx`       |',
      '| Booleano de prop   | `is` / `has` / `can`   | `isLoading`, `canEdit`|',
      '',
      '`on*` é prop, `handle*` é implementação: a leitura já diz de onde a função vem.',
      '',
      '---',
      '',
      '# 📤 3. Props',
      '',
      'Desestruture na assinatura, tipe com `type`, union fechada em vez de `string`.',
      '',
      '✅',
      '',
      '```tsx',
      'type BadgeProps = {',
      '  label: string;',
      '  variant?: \'info\' | \'warning\' | \'danger\';',
      '};',
      '',
      'export function Badge({ label, variant = \'info\' }: BadgeProps) { /* ... */ }',
      '```',
      '',
      'Default no parâmetro, não com `||` no corpo. `children: ReactNode` declarado',
      'explicitamente. Nunca espalhar `{...props}` sem tipo — vira porta para qualquer',
      'atributo.',
      '',
      '---',
      '',
      '# 🎣 4. Ordem estável de hooks',
      '',
      'Sempre a mesma ordem no corpo:',
      '',
      '```text',
      '1. contexto e roteamento',
      '2. useState',
      '3. dados derivados (cálculo direto, sem hook)',
      '4. useCallback / useMemo, se houver motivo medido',
      '5. useEffect',
      '6. early returns',
      '7. return do JSX',
      '```',
      '',
      'Hook nunca dentro de condição, laço ou callback. Early return **depois** dos',
      'hooks, nunca antes.',
      '',
      '---',
      '',
      '# 🧠 5. Não guarde o que dá para derivar',
      '',
      '❌',
      '',
      '```tsx',
      'const [fullName, setFullName] = useState(\'\');',
      '',
      'useEffect(() => {',
      '  setFullName(`${user.firstName} ${user.lastName}`);',
      '}, [user]);',
      '```',
      '',
      '✅',
      '',
      '```tsx',
      'const fullName = `${user.firstName} ${user.lastName}`;',
      '```',
      '',
      'Estado derivado cria dois lugares para a verdade morar e um render extra.',
      'Cálculo direto no corpo é a forma padrão; `useMemo` só com medição.',
      '',
      '---',
      '',
      '# 🚫 6. `useEffect` é a última opção',
      '',
      '`useEffect` serve para sincronizar com o mundo **externo**: subscription,',
      'listener, título do documento, integração imperativa.',
      '',
      'Não serve para: transformar props em estado, reagir a clique (isso é handler),',
      'buscar dados quando existe biblioteca de query, nem para "rodar quando algo',
      'muda" no meio do render.',
      '',
      'Todo efeito que assina algo devolve o teardown:',
      '',
      '```tsx',
      'useEffect(() => {',
      '  window.addEventListener(\'resize\', handleResize);',
      '  return () => window.removeEventListener(\'resize\', handleResize);',
      '}, [handleResize]);',
      '```',
      '',
      '---',
      '',
      '# 🔑 7. `key` é identidade',
      '',
      '✅',
      '',
      '```tsx',
      '{users.map((user) => <UserCard key={user.id} user={user} />)}',
      '```',
      '',
      '❌',
      '',
      '```tsx',
      '{users.map((user, index) => <UserCard key={index} user={user} />)}',
      '```',
      '',
      '`key` por índice reaproveita o nó errado quando a lista reordena ou remove um',
      'item: o estado interno vaza de uma linha para outra.',
      '',
      '---',
      '',
      '# 🎯 8. Handler curto, regra extraída',
      '',
      'Handler orquestra; a regra vive em função pura fora do componente.',
      '',
      '✅',
      '',
      '```tsx',
      'function handleSubmit(event: FormEvent) {',
      '  event.preventDefault();',
      '  const errors = validateUserForm(values);',
      '',
      '  if (errors.length) {',
      '    setErrors(errors);',
      '    return;',
      '  }',
      '',
      '  onSave(values);',
      '}',
      '```',
      '',
      'Handler com trinta linhas de regra é função de domínio no lugar errado.',
      '',
      '---',
      '',
      '# 🧱 9. JSX legível',
      '',
      '* condicional curta com `&&` ou ternário; três ramos viram variável ou',
      '  componente',
      '* nada de lógica de negócio dentro do JSX',
      '* fragmento com `<>` quando não precisa de nó',
      '* atributo booleano sem `={true}`',
      '',
      'Componente que passa de ~150 linhas normalmente contém dois componentes.',
      '',
      '---',
      '',
      '# 🛡️ 10. Nunca `dangerouslySetInnerHTML`',
      '',
      'Conteúdo de terceiros entra como texto. Se HTML for realmente necessário,',
      'sanitize antes e trate a exceção como decisão registrada, não como conveniência.',
      '',
      '---',
      '',
      '# ♿ 11. Acessibilidade',
      '',
      '* elemento semântico antes de `div` com `onClick`',
      '* `<button type="button">` sempre que não for submit',
      '* todo input com `<label>` associado',
      '* foco visível preservado; nunca `outline: none` sem substituto',
      '* estado dinâmico anunciado com `aria-live` onde importa',
      '',
      'Um `div` clicável precisa de `role`, `tabIndex` e handler de teclado — o botão',
      'já tem os três.',
      '',
      '---',
      '',
      '# ↕️ 12. Espaçamento',
      '',
      '```text',
      '1 linha   → entre grupos de hooks, antes do return, entre funções',
      '0 linhas  → após { e antes de }',
      'máximo de 1 linha vazia consecutiva',
      '```',
      '',
      'Formatter decide o visual.',
      '',
      '---',
      '',
      '# 🚦 13. Checklist final',
      '',
      '* [ ] componente é função, sem `React.FC`',
      '* [ ] props desestruturadas e tipadas com `type`, union fechada em variante',
      '* [ ] ordem de hooks estável, nenhum hook condicional',
      '* [ ] nada de estado derivado; cálculo direto no corpo',
      '* [ ] `useEffect` só para sincronizar com o mundo externo, com teardown',
      '* [ ] `key` por identidade, nunca por índice',
      '* [ ] handler curto, regra em função pura',
      '* [ ] sem `dangerouslySetInnerHTML`',
      '* [ ] elemento semântico, input com label, foco visível',
      '* [ ] no máximo uma linha vazia consecutiva',
      '',
      '---',
      '',
      '# 🏁 14. Regras principais',
      '',
      '> **Se dá para derivar, não guarde.**',
      '',
      '> **`useEffect` é para o mundo externo, não para o seu estado.**',
      '',
      '> **`key` é identidade, não posição.**',
      '',
      '> **Handler orquestra; a regra é função pura.**',
      '',
      '> **Elemento semântico antes de `div` com `onClick`.**'
    ].join('\n')
  },
  {
    id: 'python-essential',
    title: 'Python',
    area: 'backend',
    depth: 'essential',
    family: 'python',
    summary: 'O essencial de PEP 8 e do resto: dataclass congelado, type hints modernos, sem default mutável, Decimal para dinheiro e datetime com timezone.',
    icon: 'flask',
    tags: ['referência', 'pep 8', 'type hints'],
    stack: ['Python'],
    fileName: 'python-essential.md',
    source: '~/.claude/coding-styles/python-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# 🐍 Python — Essencial',
      '',
      '> Cartão de referência. A versão completa é `python.md`.',
      '',
      'Só as regras que mais mudam decisão. Segue PEP 8 onde PEP 8 decide. Não define',
      'arquitetura de software.',
      '',
      '---',
      '',
      '# 🧠 1. Paradigma',
      '',
      'Função e dado simples. Classe só quando houver **estado** com invariante a',
      'proteger, ou quando um protocolo exigir.',
      '',
      '❌',
      '',
      '```python',
      'class EmailNormalizer:',
      '    def normalize(self, email: str) -> str:',
      '        return email.strip().lower()',
      '```',
      '',
      '✅',
      '',
      '```python',
      'def normalize_email(email: str) -> str:',
      '    return email.strip().lower()',
      '```',
      '',
      'Para dados, `dataclass` congelado em vez de classe com setters:',
      '',
      '```python',
      '@dataclass(frozen=True, slots=True)',
      'class User:',
      '    id: str',
      '    name: str',
      '```',
      '',
      '`frozen=True` dá imutabilidade e hashability; `slots=True` corta memória e pega',
      'atributo digitado errado.',
      '',
      '---',
      '',
      '# 🔤 2. Convenções de case',
      '',
      '| Elemento               | Padrão             | Exemplo           |',
      '| ---------------------- | ------------------ | ----------------- |',
      '| Variáveis, funções     | `snake_case`       | `create_user`     |',
      '| Parâmetros             | `snake_case`       | `user_id`         |',
      '| Constantes de módulo   | `UPPER_SNAKE_CASE` | `MAX_RETRY_COUNT` |',
      '| Classes, type aliases  | `PascalCase`       | `CreateUserInput` |',
      '| Módulos, pacotes       | `snake_case`       | `payment_methods` |',
      '| Privado do módulo      | `_prefixo`         | `_build_payload`  |',
      '| Env variables          | `UPPER_SNAKE_CASE` | `DATABASE_URL`    |',
      '',
      'Nada de `mixedCase`, mesmo vindo de uma API que usa. Código em inglês; texto de',
      'interface no idioma do produto.',
      '',
      '---',
      '',
      '# 📛 3. Nomes',
      '',
      'Função começa com verbo; booleano parece pergunta.',
      '',
      '✅',
      '',
      '```python',
      'def get_user_by_id(user_id: str) -> User: ...',
      'def find_user_by_email(email: str) -> User | None: ...',
      'def list_orders(user_id: str) -> list[Order]: ...',
      '',
      'is_active = True',
      'has_permission = False',
      '```',
      '',
      '`find` pode não achar e devolve `| None`; `get` promete que existe. Coleção no',
      'plural sem sufixo: `users`, não `users_list`. Unidade no nome quando houver',
      'ambiguidade: `timeout_ms`, `cache_ttl_seconds`.',
      '',
      '---',
      '',
      '# 🧬 4. Type hints',
      '',
      'Anotar a superfície pública, com sintaxe moderna — sem `typing.List`, sem',
      '`Optional`.',
      '',
      '✅',
      '',
      '```python',
      'def find_user(user_id: str) -> User | None: ...',
      '',
      'def group_by_status(users: list[User]) -> dict[str, list[User]]: ...',
      '```',
      '',
      'Conjunto fechado de valores é `Literal` ou `StrEnum`:',
      '',
      '```python',
      'type UserStatus = Literal[\'active\', \'inactive\', \'blocked\']',
      '```',
      '',
      '`Any` não é solução rápida. Prefira `object` quando de fato não importa, e faça',
      'narrowing antes de usar.',
      '',
      '---',
      '',
      '# ❓ 5. `None` e default mutável',
      '',
      '`None` é ausência. Nunca use coleção como valor padrão.',
      '',
      '❌',
      '',
      '```python',
      'def add_tag(tags: list[str] = []) -> list[str]:',
      '    tags.append(\'new\')',
      '    return tags',
      '```',
      '',
      'O default é criado **uma vez**, na definição, e vaza entre chamadas.',
      '',
      '✅',
      '',
      '```python',
      'def add_tag(tags: list[str] | None = None) -> list[str]:',
      '    return [*(tags or []), \'new\']',
      '```',
      '',
      '---',
      '',
      '# 📤 6. Early return',
      '',
      'Guard clauses separadas por uma linha em branco, em vez de aninhar.',
      '',
      '✅',
      '',
      '```python',
      'def process_user(user: User | None) -> None:',
      '    if user is None:',
      '        return',
      '',
      '    if not user.is_active:',
      '        return',
      '',
      '    send_email(user.email)',
      '```',
      '',
      '---',
      '',
      '# 🧱 7. Argumentos',
      '',
      'Mais de dois ou três argumentos relacionados: force palavra-chave com `*`.',
      '',
      '✅',
      '',
      '```python',
      'def create_user(*, name: str, email: str, role: str) -> User: ...',
      '',
      'create_user(name=\'Felipe\', email=\'felipe@example.com\', role=\'admin\')',
      '```',
      '',
      'Chamada posicional com cinco strings é um bug esperando ordem trocada.',
      '',
      '---',
      '',
      '# 🚨 8. Exceções',
      '',
      'Específica, do domínio, com a causa preservada.',
      '',
      '✅',
      '',
      '```python',
      'try:',
      '    process_payment()',
      'except PaymentDeclinedError as error:',
      '    raise PaymentFailedError(\'Unable to process payment\') from error',
      '```',
      '',
      '`from error` preserva a causa; sem ele o traceback original desaparece.',
      '',
      '❌',
      '',
      '```python',
      'try:',
      '    process_payment()',
      'except Exception:',
      '    pass',
      '```',
      '',
      'Nunca identifique erro por mensagem — use `isinstance`.',
      '',
      '---',
      '',
      '# 🧯 9. Recursos e encoding',
      '',
      'Recurso sempre em `with`, e `open` sempre com `encoding`.',
      '',
      '✅',
      '',
      '```python',
      'with open(path, encoding=\'utf-8\') as handle:',
      '    content = handle.read()',
      '```',
      '',
      'Sem `encoding`, o default depende da plataforma: é assim que um script funciona',
      'no Linux e quebra no Windows.',
      '',
      '---',
      '',
      '# 🔢 10. Dinheiro e datas',
      '',
      'Nunca `float` para dinheiro; `Decimal` construído de **string**.',
      '',
      '✅',
      '',
      '```python',
      'total = Decimal(\'0.1\') + Decimal(\'0.2\')',
      '```',
      '',
      '`datetime` sempre com timezone:',
      '',
      '```python',
      'created_at = datetime.now(UTC)',
      '```',
      '',
      '`datetime.utcnow()` é ingênuo e obsoleto: devolve um objeto sem tzinfo que',
      'parece UTC e não é. ISO 8601 na fronteira.',
      '',
      '---',
      '',
      '# 🧵 11. Compreensões',
      '',
      'Compreensão para transformar e filtrar; laço quando houver efeito colateral.',
      '',
      '✅',
      '',
      '```python',
      'active_emails = [user.email for user in users if user.is_active]',
      '```',
      '',
      '❌',
      '',
      '```python',
      '[send_email(user.email) for user in users]',
      '```',
      '',
      'Compreensão existe pelo valor que devolve. Descartado o valor, é laço',
      'disfarçado. Mais de duas cláusulas vira laço ou função nomeada.',
      '',
      '---',
      '',
      '# ↕️ 12. Espaçamento e ferramentas',
      '',
      '* **duas** linhas entre funções e classes de nível de módulo (PEP 8)',
      '* uma linha entre métodos e entre blocos lógicos',
      '* nunca linha em branco logo após `def` nem antes do fim do bloco',
      '* máximo de uma linha em branco consecutiva dentro da função',
      '',
      '```text',
      'ruff format     formatação',
      'ruff check      lint',
      'mypy            typecheck',
      'pytest          testes',
      '```',
      '',
      'Indentação de 4 espaços, nunca tab. O formatter decide o visual.',
      '',
      '---',
      '',
      '# 🛡️ 13. Fronteiras',
      '',
      'Todo dado externo é não confiável: request, query, header, arquivo, webhook,',
      'env, resposta de API. Valide na fronteira e passe a forma validada para dentro.',
      '',
      'Segredo nunca no código: leia de ambiente e valide na inicialização, para o',
      'processo morrer cedo. Nunca registre senha, token, chave, cookie sensível ou',
      'dado financeiro completo.',
      '',
      '---',
      '',
      '# 🚦 14. Checklist final',
      '',
      '* [ ] função antes de classe; `dataclass` congelado para dado',
      '* [ ] `snake_case` em função e variável, `PascalCase` em classe',
      '* [ ] type hints na superfície pública, sintaxe moderna',
      '* [ ] sem `Any` desnecessário',
      '* [ ] sem default mutável',
      '* [ ] sem `except Exception: pass`; `raise ... from error`',
      '* [ ] `open` sempre com `encoding`',
      '* [ ] `Decimal` para dinheiro, construído de string',
      '* [ ] `datetime` sempre com timezone',
      '* [ ] compreensão só pelo valor que devolve',
      '* [ ] duas linhas entre funções de módulo',
      '* [ ] entrada externa validada na fronteira',
      '* [ ] `ruff format`, `ruff check`, `mypy` e `pytest` passaram',
      '',
      '---',
      '',
      '# 🏁 15. Regras principais',
      '',
      '> **Função até precisar de estado. Então `dataclass` congelado.**',
      '',
      '> **Explícito melhor que implícito — e o Zen não é decoração.**',
      '',
      '> **Valide na fronteira, confie no núcleo.**',
      '',
      '> **Exceção específica, com a causa preservada.**',
      '',
      '> **O formatter decide o visual. Você decide o nome.**'
    ].join('\n')
  },
  {
    id: 'sql-postgres-essential',
    title: 'SQL + PostgreSQL',
    area: 'data',
    depth: 'essential',
    family: 'sql-postgres',
    summary: 'O essencial de schema e consulta: timestamptz, numeric para dinheiro, nunca concatenar SQL, is null, transação sem rede e mudança em três fases.',
    icon: 'database',
    tags: ['referência', 'schema', 'índices'],
    stack: ['PostgreSQL', 'SQL'],
    fileName: 'sql-postgres-essential.md',
    source: '~/.claude/coding-styles/sql-postgres-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# 🐘 SQL + PostgreSQL — Essencial',
      '',
      '> Cartão de referência. A versão completa é `sql-postgres.md`.',
      '',
      'Só as regras que mais mudam decisão em schema, consulta e migração. Não define',
      'modelagem de domínio.',
      '',
      '---',
      '',
      '# 🔤 1. Naming',
      '',
      '| Elemento          | Padrão                    | Exemplo                         |',
      '| ----------------- | ------------------------- | ------------------------------- |',
      '| Tabela            | `snake_case`, plural      | `users`, `payment_methods`      |',
      '| Coluna            | `snake_case`, singular    | `email`, `created_at`           |',
      '| Chave primária    | `id`                      | `id`                            |',
      '| Chave estrangeira | `<tabela_singular>_id`    | `user_id`                       |',
      '| Índice            | `idx_<tabela>_<colunas>`  | `idx_orders_user_id_created_at` |',
      '| Único             | `uq_<tabela>_<colunas>`   | `uq_users_email`                |',
      '| Check             | `ck_<tabela>_<regra>`     | `ck_orders_total_non_negative`  |',
      '',
      'Nunca use palavra reservada como nome: `user`, `order`, `group`, `end` obrigam',
      'aspas para sempre. Nome sem aspas é dobrado para minúsculo pelo Postgres, então',
      '`"createdAt"` precisa de aspas em toda consulta — use `snake_case` e esqueça as',
      'aspas.',
      '',
      '---',
      '',
      '# 🧱 2. Escolha de tipo',
      '',
      '| Precisa de       | Use               | Não use                  |',
      '| ---------------- | ----------------- | ------------------------ |',
      '| Texto            | `text`            | `varchar(n)` sem motivo  |',
      '| Dinheiro         | `numeric(12, 2)`  | `float`, `real`, `money` |',
      '| Instante         | `timestamptz`     | `timestamp`              |',
      '| Data civil       | `date`            | `timestamptz`            |',
      '| Identificador    | `uuid`/`bigint`   | `int` em tabela que cresce |',
      '| Documento        | `jsonb`           | `json`, `text`           |',
      '| Conjunto fechado | `text` + `check`  | inteiro mágico           |',
      '',
      '`timestamp` sem timezone guarda um instante ambíguo. `float` para dinheiro erra',
      'centavos. `json` guarda o texto cru, `jsonb` guarda parseado e indexável. `int`',
      'como PK cabe 2,1 bilhões — vale trocar antes, não depois.',
      '',
      '---',
      '',
      '# 🔑 3. Restrição no banco',
      '',
      'A aplicação tem bug; o banco é a última linha. `not null`, `unique`, `check` e',
      'chave estrangeira ficam no schema, não só no código.',
      '',
      '```sql',
      'alter table orders',
      '  add constraint ck_orders_total_non_negative',
      '  check (total_cents >= 0);',
      '```',
      '',
      '---',
      '',
      '# 📇 4. Índices',
      '',
      'Índice existe para um padrão de acesso. Sem consulta correspondente, é só custo',
      'de escrita.',
      '',
      '```sql',
      'create index idx_orders_user_id_created_at',
      '  on orders (user_id, created_at desc);',
      '```',
      '',
      '* **ordem da coluna importa**: `(a, b)` serve para `a` e para `a, b`, mas',
      '  **não** para `b` sozinho',
      '* `where` mais seletivo primeiro, `order by` no fim',
      '* função na coluna anula o índice: `where lower(email) = $1` precisa de índice',
      '  em `lower(email)`',
      '* `create index concurrently` em tabela com tráfego — o normal trava escritas',
      '* toda chave estrangeira usada em `join` ou `where` precisa de índice: o',
      '  Postgres **não** cria automaticamente',
      '',
      '---',
      '',
      '# 🔎 5. Consultas',
      '',
      'Colunas explícitas, sempre. `select *` traz coluna larga que ninguém usa e',
      'quebra na primeira mudança de schema.',
      '',
      '✅',
      '',
      '```sql',
      'select',
      '  o.id,',
      '  o.total_cents,',
      '  u.email',
      'from orders o',
      '  join users u on u.id = o.user_id',
      'where o.status = \'paid\'',
      'order by o.created_at desc',
      'limit $1;',
      '```',
      '',
      'Palavra-chave em minúscula, uma cláusula por linha, dois espaços de indentação.',
      'Sempre `limit` em consulta de lista: a tabela cresce.',
      '',
      '---',
      '',
      '# 🚫 6. Nunca concatene SQL',
      '',
      'A regra mais importante.',
      '',
      '❌',
      '',
      '```ts',
      'const rows = await db.query(`select * from users where email = \'${email}\'`);',
      '```',
      '',
      '✅',
      '',
      '```ts',
      'const rows = await db.query(',
      '  \'select id, name from users where email = $1\',',
      '  [email],',
      ');',
      '```',
      '',
      'Identificador não aceita parâmetro. Se precisar ser dinâmico, valide contra uma',
      '**lista fixa** no código — inclusive em `order by`.',
      '',
      '---',
      '',
      '# ⚖️ 7. `null` em comparação',
      '',
      '`null` não é igual a nada, nem a si mesmo.',
      '',
      '❌',
      '',
      '```sql',
      'where deleted_at = null',
      'where status != \'paid\'   -- perde as linhas com status null',
      '```',
      '',
      '✅',
      '',
      '```sql',
      'where deleted_at is null',
      'where status is distinct from \'paid\'',
      '```',
      '',
      '`not in (subquery)` com um `null` no resultado devolve zero linhas. Prefira',
      '`not exists`.',
      '',
      '---',
      '',
      '# 🔄 8. Transações',
      '',
      'Uma transação por operação de negócio, aberta o mais tarde e fechada o mais cedo',
      'possível. **Nunca** mantenha transação aberta durante chamada de rede: um',
      '`await http.post()` segura locks pelo timeout do outro serviço.',
      '',
      'Ler-e-então-escrever na mesma linha precisa de bloqueio explícito:',
      '',
      '```sql',
      'select total_cents from accounts where id = $1 for update;',
      '```',
      '',
      'Sem `for update` é corrida: dois processos leem o mesmo saldo e o segundo',
      'sobrescreve o primeiro. Ordem de bloqueio consistente evita deadlock.',
      '',
      '---',
      '',
      '# 🗑️ 9. Escrita em massa',
      '',
      'Nunca sem `where`. Em volume, em lotes:',
      '',
      '```sql',
      'delete from events',
      'where id in (',
      '  select id from events',
      '  where created_at < now() - interval \'90 days\'',
      '  limit 10000',
      ');',
      '```',
      '',
      'Um `delete` de milhões de linhas numa transação infla o WAL, segura locks e pode',
      'travar a tabela por minutos. Havendo histórico a preservar, use exclusão lógica',
      '(`deleted_at timestamptz`) com índice parcial `where deleted_at is null`.',
      '',
      '---',
      '',
      '# 🚚 10. Migrações em três fases',
      '',
      'Migração é **imutável** depois de aplicada em qualquer ambiente. DDL numa',
      'migração, backfill em outra — em lotes e idempotente.',
      '',
      'Mudança que quebra tráfego vai em três fases:',
      '',
      '1. **expand** — adiciona a nova forma, nada lê ainda',
      '2. **migrate** — backfill, e a escrita passa a ir para as duas',
      '3. **contract** — leitura muda, depois a forma antiga cai',
      '',
      'Renomear coluna em um passo derruba a aplicação que ainda está no ar.',
      '`add column` com `default` é instantâneo no Postgres 11+, mas adicionar',
      '`not null` a coluna existente ainda faz varredura completa.',
      '',
      '---',
      '',
      '# 🧪 11. `explain` antes de otimizar',
      '',
      '```sql',
      'explain (analyze, buffers) select ...;',
      '```',
      '',
      'Ler o plano antes de criar índice: `seq scan` em tabela pequena é normal, e o',
      'problema costuma ser função na coluna, tipo divergente no `join` ou estatística',
      'velha. Palpite cria índice que só custa escrita.',
      '',
      '---',
      '',
      '# 🚦 12. Checklist final',
      '',
      '* [ ] `snake_case`, tabela no plural, sem palavra reservada',
      '* [ ] `timestamptz` para instante, `numeric` para dinheiro',
      '* [ ] restrição no banco, não só na aplicação',
      '* [ ] índice para toda FK usada em `join`/`where`',
      '* [ ] colunas explícitas, `limit` em toda lista',
      '* [ ] nenhuma string de SQL concatenada; identificador dinâmico vem de lista fixa',
      '* [ ] `is null` / `is distinct from` no lugar de `=`/`!=`',
      '* [ ] nenhuma chamada de rede dentro de transação',
      '* [ ] `for update` em ler-e-então-escrever',
      '* [ ] delete/update em massa em lotes e com `where`',
      '* [ ] migração imutável, DDL separado de backfill, mudança em três fases',
      '* [ ] `explain (analyze, buffers)` lido antes de criar índice',
      '',
      '---',
      '',
      '# 🏁 13. Regras principais',
      '',
      '> **Nunca concatene SQL. Parâmetro sempre.**',
      '',
      '> **`timestamptz` para instante. `numeric` para dinheiro.**',
      '',
      '> **Índice sem consulta é só custo de escrita.**',
      '',
      '> **Transação não espera a rede.**',
      '',
      '> **Mudança que quebra tráfego vai em três fases.**'
    ].join('\n')
  },
  {
    id: 'java-spring-essential',
    title: 'Java + Spring',
    area: 'backend',
    depth: 'essential',
    family: 'java-spring',
    summary: 'O essencial: record, Optional como retorno, construtor em vez de @Autowired, LAZY com join fetch e transação que não espera a rede.',
    icon: 'package',
    tags: ['referência', 'record', 'jpa'],
    stack: ['Java', 'Spring Boot'],
    fileName: 'java-spring-essential.md',
    source: '~/.claude/coding-styles/java-spring-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# ☕ Java + Spring — Essencial',
      '',
      '> Cartão de referência. A versão completa é `java-spring.md`.',
      '',
      'Só as regras que mais mudam decisão. Não define arquitetura de software.',
      '',
      '---',
      '',
      '# 🔤 1. Convenções de case',
      '',
      '| Elemento           | Padrão             | Exemplo             |',
      '| ------------------ | ------------------ | ------------------- |',
      '| Variáveis, métodos | `camelCase`        | `createUser`        |',
      '| Constantes         | `UPPER_SNAKE_CASE` | `MAX_RETRY_COUNT`   |',
      '| Classes, records   | `PascalCase`       | `CreateUserRequest` |',
      '| Valor de enum      | `UPPER_SNAKE_CASE` | `PENDING_REVIEW`    |',
      '| Pacotes            | minúsculo, sem `_` | `com.app.users`     |',
      '| Rotas              | `kebab-case`       | `/payment-methods`  |',
      '',
      'Sem `I` em interface, sem `Impl` como nome de classe. Código em inglês; texto de',
      'interface no idioma do produto.',
      '',
      '---',
      '',
      '# 🧬 2. `record` para dado',
      '',
      'Dado sem comportamento é `record`, não classe com getters e setters.',
      '',
      '✅',
      '',
      '```java',
      'public record UserResponse(String id, String name, Instant createdAt) {}',
      '```',
      '',
      '`record` dá imutabilidade, `equals`, `hashCode` e `toString` de graça. Setter num',
      'DTO é convite para mutação a meio caminho. Invariante vai no construtor',
      'compacto.',
      '',
      '---',
      '',
      '# 📛 3. `get` lança, `find` devolve `Optional`',
      '',
      '```java',
      'User getUserById(String userId);',
      'Optional<User> findUserByEmail(String email);',
      'List<Order> listOrders(String userId);',
      '```',
      '',
      'Trocar os dois é mentir na assinatura. Booleano parece pergunta: `isActive()`,',
      '`hasPermission()`. Coleção no plural sem sufixo: `users`, não `userList`.',
      '',
      '---',
      '',
      '# ❓ 4. `Optional` é tipo de retorno',
      '',
      'Nunca em campo, nunca em parâmetro, nunca em coleção.',
      '',
      '✅',
      '',
      '```java',
      'return findUserByEmail(email)',
      '    .map(User::name)',
      '    .orElseThrow(() -> new UserNotFoundException(email));',
      '```',
      '',
      '❌',
      '',
      '```java',
      'private Optional<String> nickname;      // campo',
      'void register(Optional<String> coupon); // parâmetro',
      '```',
      '',
      '`Optional.get()` sem `isPresent` é `NullPointerException` com outro nome.',
      'Coleção vazia é `List.of()`, nunca `null`.',
      '',
      '---',
      '',
      '# 🔄 5. Imutabilidade',
      '',
      'Campo `final` por padrão. Coleção exposta é cópia:',
      '',
      '```java',
      'public List<String> permissions() {',
      '    return List.copyOf(permissions);',
      '}',
      '```',
      '',
      'Devolver a lista interna deixa qualquer chamador mutar o objeto por fora.',
      '',
      '---',
      '',
      '# 💉 6. Injeção por construtor',
      '',
      '❌',
      '',
      '```java',
      '@Autowired',
      'private UserRepository repository;',
      '```',
      '',
      '✅',
      '',
      '```java',
      'private final UserRepository repository;',
      'private final Clock clock;',
      '',
      'public CreateUser(UserRepository repository, Clock clock) {',
      '    this.repository = repository;',
      '    this.clock = clock;',
      '}',
      '```',
      '',
      'Campo injetado não pode ser `final`, esconde dependência do teste e permite',
      'instância meio construída. Com um único construtor o Spring injeta sem anotação.',
      '`Clock` injetado é o que torna o tempo testável.',
      '',
      '---',
      '',
      '# 🧩 7. Camadas',
      '',
      '* **controller** traduz HTTP para caso de uso — sem regra, sem repositório',
      '* **service** tem a regra e não conhece `HttpServletRequest`',
      '* **repository** acessa dado e não tem regra',
      '',
      'Controller que chama repositório direto move a regra para dentro do HTTP, e ela',
      'deixa de existir para qualquer outro chamador.',
      '',
      '---',
      '',
      '# 🛡️ 8. Fronteira HTTP',
      '',
      '```java',
      'public record CreateUserRequest(@NotBlank String name, @Email String email) {}',
      '',
      '@PostMapping("/v1/users")',
      'public ResponseEntity<UserResponse> create(@Valid @RequestBody CreateUserRequest request) { /* ... */ }',
      '```',
      '',
      '**Entidade JPA nunca é DTO de entrada nem de saída.** No body, deixa o cliente',
      'escrever qualquer campo — `id` incluído. Na resposta, vaza schema e dispara',
      'carregamento LAZY na serialização.',
      '',
      'Recurso no plural, `kebab-case`, sem verbo na URL. `201` cria com `Location`,',
      '`204` remove, `409` conflito, `422` validação.',
      '',
      '---',
      '',
      '# 🗄️ 9. JPA',
      '',
      '* relacionamento **sempre** `LAZY`; `EAGER` carrega meio banco sem pedir',
      '* N+1 se resolve com `join fetch` ou `@EntityGraph`, não com `EAGER`',
      '* `equals`/`hashCode` por id de negócio',
      '* consulta de lista sempre paginada',
      '',
      'N+1 não aparece em teste com três linhas na tabela. Aparece em produção.',
      '',
      '---',
      '',
      '# 🔄 10. Transações',
      '',
      '`@Transactional` no caso de uso, curto, `readOnly = true` em leitura.',
      '',
      '* **nunca** chamada de rede dentro da transação: o lock fica preso pelo timeout',
      '  do outro serviço',
      '* funciona via proxy — chamada de um método para outro na **mesma** classe não',
      '  abre transação nenhuma',
      '* e-mail e fila vão depois do commit',
      '',
      '---',
      '',
      '# 🚨 11. Exceções',
      '',
      'Exceção de domínio, unchecked, com código estável, causa preservada.',
      '',
      '✅',
      '',
      '```java',
      'try {',
      '    processPayment();',
      '} catch (PaymentDeclinedException e) {',
      '    throw new PaymentFailedException("Unable to process payment", e);',
      '}',
      '```',
      '',
      '❌',
      '',
      '```java',
      'catch (Exception e) { /* ignore */ }',
      '```',
      '',
      'Nunca identifique erro por mensagem — use o tipo. Nunca capture `Throwable`.',
      'Um `@RestControllerAdvice`, um formato de erro só, com `code` estável e sem',
      'stack trace na resposta.',
      '',
      '---',
      '',
      '# 🔢 12. Dinheiro e datas',
      '',
      'Nunca `double` para dinheiro.',
      '',
      '✅',
      '',
      '```java',
      'BigDecimal total = new BigDecimal("0.1").add(new BigDecimal("0.2"));',
      '```',
      '',
      '`BigDecimal` de **String**, comparado com `compareTo` — `2.0` e `2.00` são',
      'diferentes para `equals`. Escala e `RoundingMode` explícitos na divisão.',
      '',
      'Instante é `Instant`, data civil é `LocalDate`. Nunca `Date` nem `Calendar`.',
      '',
      '---',
      '',
      '# 🌊 13. Streams e logs',
      '',
      'Stream pelo valor que devolve:',
      '',
      '```java',
      'var activeEmails = users.stream().filter(User::isActive).map(User::email).toList();',
      '```',
      '',
      '`stream().forEach(...)` com efeito colateral é laço disfarçado — use `for`.',
      '',
      'Log com placeholder, exceção como último argumento:',
      '',
      '```java',
      'log.error("Failed to update user: userId={}", userId, exception);',
      '```',
      '',
      'Concatenar impede agrupar por mensagem e é avaliado mesmo com o nível',
      'desligado. Nunca registre senha, token, chave ou dado financeiro completo.',
      '',
      '---',
      '',
      '# 🚦 14. Checklist final',
      '',
      '* [ ] `record` para dado, sem setter em DTO',
      '* [ ] campo `final`, coleção exposta como cópia',
      '* [ ] `Optional` só no retorno; `get` lança, `find` devolve `Optional`',
      '* [ ] injeção por construtor, nenhum `@Autowired` em campo',
      '* [ ] controller sem regra e sem repositório',
      '* [ ] entidade JPA nunca atravessa a fronteira HTTP',
      '* [ ] relacionamento `LAZY`, N+1 com `join fetch`, lista paginada',
      '* [ ] `@Transactional` curto, sem chamada de rede dentro',
      '* [ ] exceção de domínio com causa preservada, nunca `catch (Exception)` vazio',
      '* [ ] `BigDecimal` de String, comparado com `compareTo`',
      '* [ ] `Instant`/`LocalDate`, nunca `Date`',
      '* [ ] log com placeholder, nada sensível registrado',
      '* [ ] entrada validada com `@Valid` na fronteira',
      '',
      '---',
      '',
      '# 🏁 15. Regras principais',
      '',
      '> **`record` para dado. Classe quando houver comportamento.**',
      '',
      '> **`Optional` é retorno, não campo.**',
      '',
      '> **Dependência escondida é dependência não testada.**',
      '',
      '> **Entidade JPA não atravessa a fronteira HTTP.**',
      '',
      '> **Transação não espera a rede.**'
    ].join('\n')
  },
  {
    id: 'csharp-dotnet-essential',
    title: 'C# + .NET',
    area: 'backend',
    depth: 'essential',
    family: 'csharp-dotnet',
    summary: 'O essencial: PascalCase em método e constante, nullable ligado, nada de .Result, throw que preserva o stack e projeção com Select no EF.',
    icon: 'hexagon',
    tags: ['referência', 'nullable', 'async'],
    stack: ['C#', '.NET'],
    fileName: 'csharp-dotnet-essential.md',
    source: '~/.claude/coding-styles/csharp-dotnet-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# 🟣 C# + .NET — Essencial',
      '',
      '> Cartão de referência. A versão completa é `csharp-dotnet.md`.',
      '',
      'Só as regras que mais mudam decisão. Não define arquitetura de software.',
      '',
      '---',
      '',
      '# 🔤 1. Convenções de case',
      '',
      '| Elemento              | Padrão             | Exemplo             |',
      '| --------------------- | ------------------ | ------------------- |',
      '| Classes, records      | `PascalCase`       | `CreateUserRequest` |',
      '| Métodos, propriedades | `PascalCase`       | `CreateUser`        |',
      '| Parâmetros, locais    | `camelCase`        | `userId`            |',
      '| Campo privado         | `_camelCase`       | `_repository`       |',
      '| Constantes            | `PascalCase`       | `MaxRetryCount`     |',
      '| Interfaces            | `I` + `PascalCase` | `IUserRepository`   |',
      '| Método assíncrono     | sufixo `Async`     | `GetUserAsync`      |',
      '| Rotas                 | `kebab-case`       | `/payment-methods`  |',
      '',
      'Diferente de quase toda linguagem: método, propriedade **e constante** são',
      '`PascalCase` — não `UPPER_SNAKE_CASE`. Enum no singular, plural só em `[Flags]`.',
      '',
      '---',
      '',
      '# 🧬 2. `record` para dado',
      '',
      '```csharp',
      'public sealed record UserResponse(string Id, string Name, DateTimeOffset CreatedAt);',
      '```',
      '',
      '`record` dá igualdade por valor, `ToString` e `with` de graça. `set` público num',
      'DTO é convite para mutação a meio caminho — use `init`.',
      '',
      '`sealed` por padrão em classe que não foi desenhada para herança.',
      '',
      '---',
      '',
      '# ❓ 3. Nullable ligado',
      '',
      '`<Nullable>enable</Nullable>`, e o aviso tratado como erro.',
      '',
      '✅',
      '',
      '```csharp',
      'var user = FindUserByEmail(email)',
      '    ?? throw new UserNotFoundException(email);',
      '```',
      '',
      'O tipo diz se pode ser nulo; `!` desliga essa garantia e só aparece com',
      'comentário explicando por quê. `Get` lança, `Find` devolve anulável — trocar os',
      'dois é mentir na assinatura. Coleção vazia é `[]`, nunca `null`.',
      '',
      'Use `is null` / `is not null`, não `== null`: `==` pode ser sobrecarregado.',
      '',
      '---',
      '',
      '# 🔄 4. Imutabilidade',
      '',
      'Campo `readonly`, propriedade `init`, coleção exposta como `IReadOnlyList<T>`:',
      '',
      '```csharp',
      'public IReadOnlyList<string> Permissions => _permissions.AsReadOnly();',
      '```',
      '',
      'Expor `List<T>` deixa qualquer chamador mutar o objeto por fora. No retorno,',
      '`IReadOnlyList<T>` é melhor que `IEnumerable<T>`: o segundo não diz se já foi',
      'materializado.',
      '',
      '---',
      '',
      '# ⚡ 5. Async',
      '',
      '`async`/`await` até o topo. Nunca bloqueie.',
      '',
      '❌',
      '',
      '```csharp',
      'var user = GetUserAsync(userId).Result;',
      '```',
      '',
      '✅',
      '',
      '```csharp',
      'var user = await GetUserAsync(userId, ct);',
      '',
      'var userTask = GetUserAsync(userId, ct);',
      'var ordersTask = ListOrdersAsync(userId, ct);',
      'await Task.WhenAll(userTask, ordersTask);',
      '```',
      '',
      '`.Result` e `.Wait()` causam deadlock e consomem thread de pool. Nunca',
      '`async void` fora de event handler: exceção nele derruba o processo.',
      '',
      '`CancellationToken` é o **último** parâmetro de todo método assíncrono, e é',
      'repassado adiante — token que ninguém propaga é cancelamento que não acontece.',
      '',
      '---',
      '',
      '# 🚨 6. Exceções',
      '',
      '```csharp',
      'try',
      '{',
      '    await ProcessPaymentAsync(ct);',
      '}',
      'catch (PaymentDeclinedException e)',
      '{',
      '    throw new PaymentFailedException("Unable to process payment", e);',
      '}',
      '```',
      '',
      '❌',
      '',
      '```csharp',
      'catch (Exception) { }',
      'catch (Exception e) { throw e; }   // reseta o stack trace',
      '```',
      '',
      '`throw;` sozinho preserva o stack; `throw e;` o descarta. Identifique erro pelo',
      'tipo, nunca pela mensagem. `OperationCanceledException` de cancelamento pedido',
      'não é erro. Erro na resposta em `ProblemDetails`, sem stack trace.',
      '',
      '---',
      '',
      '# 💉 7. Injeção por construtor',
      '',
      '```csharp',
      'public sealed class CreateUser(IUserRepository repository, TimeProvider time)',
      '{',
      '    private readonly IUserRepository _repository = repository;',
      '}',
      '```',
      '',
      '`TimeProvider` injetado em vez de `DateTime.UtcNow` solto é o que torna o tempo',
      'testável.',
      '',
      'Ciclo de vida: `Singleton` sem estado mutável, `Scoped` para o que acompanha a',
      'requisição. **Nunca** injete `Scoped` dentro de `Singleton`: o objeto de',
      'requisição fica preso para sempre.',
      '',
      '`HttpClient` não se cria por requisição em `using` — esgota sockets. Use',
      '`IHttpClientFactory`.',
      '',
      '---',
      '',
      '# 🧩 8. Camadas e fronteira',
      '',
      'Endpoint traduz HTTP para caso de uso — sem regra, sem `DbContext`. O caso de',
      'uso tem a regra e não conhece `HttpContext`.',
      '',
      '**Entidade do EF nunca é DTO de entrada nem de saída.** No body deixa o cliente',
      'escrever qualquer campo; na resposta vaza schema e dispara lazy loading na',
      'serialização.',
      '',
      'Recurso no plural, `kebab-case`, sem verbo na URL. `201` cria com `Location`,',
      '`204` remove, `409` conflito, `422` validação. Config via `IOptions<T>` com',
      '`ValidateOnStart`.',
      '',
      '---',
      '',
      '# 🗄️ 9. Entity Framework',
      '',
      '❌',
      '',
      '```csharp',
      'var users = context.Users.ToList().Where(u => u.IsActive);',
      '```',
      '',
      '✅',
      '',
      '```csharp',
      'var users = await context.Users',
      '    .AsNoTracking()',
      '    .Where(u => u.IsActive)',
      '    .Select(u => new UserResponse(u.Id, u.Name, u.CreatedAt))',
      '    .Take(50)',
      '    .ToListAsync(ct);',
      '```',
      '',
      '`ToList()` no meio da consulta materializa, e o resto do filtro passa a rodar em',
      'memória. `AsNoTracking` em toda leitura, `Include` explícito, lista paginada.',
      'Transação curta e **nunca** com chamada de rede dentro.',
      '',
      '---',
      '',
      '# 🔍 10. LINQ',
      '',
      'LINQ pelo valor que devolve; laço quando houver efeito colateral. `Any()` em vez',
      'de `Count() > 0`, `FirstOrDefault(predicado)` em vez de',
      '`Where(...).FirstOrDefault()`, e nunca enumere duas vezes a mesma fonte',
      'custosa.',
      '',
      '---',
      '',
      '# 🔢 11. Dinheiro, datas e texto',
      '',
      'Dinheiro é `decimal`, nunca `double`:',
      '',
      '```csharp',
      'decimal total = 0.1m + 0.2m;',
      '```',
      '',
      'Instante é `DateTimeOffset`, data civil é `DateOnly`. `DateTime.Now` guarda a',
      'hora local do servidor e é a origem de metade dos bugs de fuso.',
      '',
      'Comparação de texto de sistema com `StringComparison.Ordinal`; `ToLower()` para',
      'comparar é alocação desnecessária e depende de cultura.',
      '',
      '---',
      '',
      '# 📢 12. Logs',
      '',
      '```csharp',
      'logger.LogError(exception, "Failed to update user: UserId={UserId}", userId);',
      '```',
      '',
      '❌',
      '',
      '```csharp',
      'logger.LogError($"Failed to update user {userId}");',
      '```',
      '',
      'Interpolar impede consultar por campo e é avaliado mesmo com o nível desligado.',
      'Nunca registre senha, token, chave, cookie sensível ou dado financeiro completo.',
      '',
      '---',
      '',
      '# 🚦 13. Checklist final',
      '',
      '* [ ] `PascalCase` em método, propriedade e constante',
      '* [ ] `sealed` por padrão; `record` para dado, sem `set` público',
      '* [ ] nullable ligado, sem `!` sem justificativa; `is null` em vez de `== null`',
      '* [ ] `readonly`/`init`, coleção exposta como `IReadOnlyList<T>`',
      '* [ ] nenhum `.Result`/`.Wait()`; nenhum `async void`',
      '* [ ] `CancellationToken` último e propagado',
      '* [ ] `throw;` para relançar; erro identificado por tipo',
      '* [ ] injeção por construtor; nenhum `Scoped` dentro de `Singleton`',
      '* [ ] `HttpClient` via `IHttpClientFactory`',
      '* [ ] entidade do EF nunca atravessa a fronteira HTTP',
      '* [ ] `AsNoTracking`, projeção com `Select`, lista paginada',
      '* [ ] `decimal` para dinheiro, `DateTimeOffset` para instante',
      '* [ ] log com template estruturado, nada sensível registrado',
      '* [ ] `dotnet format` e a suíte passaram',
      '',
      '---',
      '',
      '# 🏁 14. Regras principais',
      '',
      '> **`record` para dado. Classe quando houver comportamento.**',
      '',
      '> **Nullable ligado: o tipo diz se pode ser nulo, e `!` é dívida.**',
      '',
      '> **`async` até o topo. `.Result` é deadlock esperando acontecer.**',
      '',
      '> **`throw;` preserva o stack. `throw e;` joga fora.**',
      '',
      '> **`decimal` para dinheiro, `DateTimeOffset` para instante.**'
    ].join('\n')
  },
  {
    id: 'kotlin-android-essential',
    title: 'Kotlin + Android',
    area: 'mobile',
    depth: 'essential',
    family: 'kotlin-android',
    summary: 'O essencial: val e copy, sealed para estado impossível não ser representável, nenhum GlobalScope, asStateFlow e efeito em LaunchedEffect.',
    icon: 'zap',
    tags: ['referência', 'coroutines', 'compose'],
    stack: ['Kotlin', 'Android', 'Compose'],
    fileName: 'kotlin-android-essential.md',
    source: '~/.claude/coding-styles/kotlin-android-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# 🤖 Kotlin + Android — Essencial',
      '',
      '> Cartão de referência. A versão completa é `kotlin-android.md`.',
      '',
      'Só as regras que mais mudam decisão. Não define arquitetura de aplicação.',
      '',
      '---',
      '',
      '# 🔤 1. Convenções de case',
      '',
      '| Elemento           | Padrão             | Exemplo             |',
      '| ------------------ | ------------------ | ------------------- |',
      '| Variáveis, funções | `camelCase`        | `createUser`        |',
      '| Constantes de topo | `UPPER_SNAKE_CASE` | `MAX_RETRY_COUNT`   |',
      '| Classes            | `PascalCase`       | `UserRepository`    |',
      '| Composable         | `PascalCase`       | `UserCard`          |',
      '| Valor de enum      | `UPPER_SNAKE_CASE` | `PENDING_REVIEW`    |',
      '| Recurso, id, string| `snake_case`       | `user_not_found`    |',
      '',
      'Composable é a exceção deliberada: função em `PascalCase` porque devolve UI, não',
      'valor. Código em inglês; texto de UI sempre em `strings.xml`.',
      '',
      '---',
      '',
      '# 🧬 2. `data class` com `val`',
      '',
      '```kotlin',
      'data class User(val id: String, val name: String, val deletedAt: Instant? = null)',
      '',
      'val updated = user.copy(name = "Felipe")',
      '```',
      '',
      '`var` num modelo de dado é mutação a meio caminho. Para "mudar", `copy`.',
      '',
      '---',
      '',
      '# 🎚️ 3. `sealed` para estado de tela',
      '',
      '❌',
      '',
      '```kotlin',
      'data class UiState(val isLoading: Boolean, val user: User?, val error: String?)',
      '```',
      '',
      'Essa forma permite `isLoading = true` **com** erro preenchido: estados que não',
      'existem passam a ser representáveis.',
      '',
      '✅',
      '',
      '```kotlin',
      'sealed interface UserUiState {',
      '    data object Loading : UserUiState',
      '    data class Content(val user: User) : UserUiState',
      '    data class Failure(val message: UiText) : UserUiState',
      '}',
      '```',
      '',
      'Com `sealed`, o `when` é exaustivo sem `else` — acrescentar um estado quebra a',
      'compilação onde falta tratá-lo. `else` sobre `sealed` ou `enum` desliga isso.',
      '',
      '---',
      '',
      '# ❓ 4. Nunca `!!`',
      '',
      '`!!` é `NullPointerException` escrita à mão.',
      '',
      '✅',
      '',
      '```kotlin',
      'val name = user?.name ?: return',
      'val user = requireNotNull(cachedUser) { "user must be loaded before render" }',
      '```',
      '',
      '`lateinit` só onde o ciclo de vida do framework impõe (binding de Fragment), e',
      'sempre limpo no `onDestroyView` — senão a view fica retida.',
      '',
      '---',
      '',
      '# 🔄 5. Fluxo em uma direção',
      '',
      '```kotlin',
      'private val _items = MutableStateFlow<List<Item>>(emptyList())',
      'val items: StateFlow<List<Item>> = _items.asStateFlow()',
      '```',
      '',
      'Expor o `MutableStateFlow` deixa a UI escrever no estado, e a fonte de verdade',
      'deixa de existir. Coleção exposta é `List`, não `MutableList`.',
      '',
      '---',
      '',
      '# ⚡ 6. Coroutine tem dono',
      '',
      '❌',
      '',
      '```kotlin',
      'GlobalScope.launch { getUser(userId) }',
      '```',
      '',
      '`GlobalScope` não é cancelado com a tela: a resposta chega para um ViewModel',
      'morto.',
      '',
      '✅',
      '',
      '```kotlin',
      'viewModelScope.launch { /* ... */ }',
      '```',
      '',
      'Na UI, colete com `lifecycle.repeatOnLifecycle` — `launch` direto continua',
      'coletando com o app em segundo plano.',
      '',
      '`suspend` é sequencial. Independente vai em paralelo:',
      '',
      '```kotlin',
      'coroutineScope {',
      '    val user = async { getUser(userId) }',
      '    val orders = async { listOrders(userId) }',
      '    UserScreenData(user.await(), orders.await())',
      '}',
      '```',
      '',
      'Cancelamento é cooperativo: `CancellationException` **nunca** é engolida —',
      '`catch (e: Exception)` em volta de código suspenso relança a de cancelamento.',
      '',
      '---',
      '',
      '# 🧵 7. `Dispatcher` injetado',
      '',
      '```kotlin',
      'class UserRepository(private val io: CoroutineDispatcher) {',
      '    suspend fun load(userId: String): User = withContext(io) { /* ... */ }',
      '}',
      '```',
      '',
      'Quem escolhe a thread é a função que faz o trabalho, não quem chama.',
      '`Dispatchers.IO` hard-coded torna o teste dependente de thread real. Nenhuma',
      'função `suspend` pública bloqueia a thread de quem chama.',
      '',
      '---',
      '',
      '# 🎨 8. Compose: estado sobe, evento desce',
      '',
      '```kotlin',
      '@Composable',
      'fun UserCard(user: User, onSelect: (String) -> Unit, modifier: Modifier = Modifier)',
      '```',
      '',
      '* `modifier: Modifier = Modifier` é o **último** parâmetro, aplicado ao nó raiz',
      '* nada de estado derivado guardado: calcule no corpo',
      '* `remember` sobrevive à recomposição; `rememberSaveable` sobrevive à morte do',
      '  processo',
      '* efeito colateral vai em `LaunchedEffect` com chave, nunca no corpo',
      '* `ViewModel` não é parâmetro de composable reutilizável',
      '',
      '❌',
      '',
      '```kotlin',
      '@Composable',
      'fun UserCard(viewModel: UserViewModel) {',
      '    val user = viewModel.load()   // roda a cada recomposição',
      '}',
      '```',
      '',
      '---',
      '',
      '# 📱 9. Activity e Fragment sem lógica',
      '',
      'São pontos de entrada do sistema, não camada de aplicação: zero regra de',
      'negócio, zero repositório, zero formatação. O que fica lá é compor, observar',
      'estado, repassar evento e navegar. Um Fragment de 400 linhas é uma tela sem',
      'ViewModel.',
      '',
      '---',
      '',
      '# 🗺️ 10. Recursos e configuração',
      '',
      '* nenhuma string literal na UI: `stringResource(R.string.user_not_found)`',
      '* `plurals` para contagem — concatenar `"$n itens"` não traduz',
      '* nenhuma cor ou dimensão literal: tema',
      '* rotação, tema escuro, idioma, fonte grande e tela dividida acontecem: o estado',
      '  que precisa sobreviver vai em `SavedStateHandle` ou `rememberSaveable`',
      '',
      '---',
      '',
      '# 🛡️ 11. Entrada externa e segredos',
      '',
      'Resposta de API, deep link, `Intent` extra e `Bundle` de outro app são dados não',
      'confiáveis: valide na fronteira.',
      '',
      'Nenhum segredo no código nem em `strings.xml` — qualquer APK é decompilável.',
      'Dado sensível em `EncryptedSharedPreferences` ou Keystore. `WebView` sem',
      '`javaScriptEnabled` a menos que seja requisito.',
      '',
      '---',
      '',
      '# 🔢 12. Dinheiro, datas e logs',
      '',
      'Dinheiro em centavos (`Long`) ou `BigDecimal`, nunca `Double`. Instante é',
      '`Instant`; formatação para o usuário com o `Locale` do dispositivo, e nunca com',
      '`SimpleDateFormat` compartilhado — ele não é thread-safe.',
      '',
      'Nada de `println`. Log de debug não vai para release, e nunca registre token,',
      'senha, chave, e-mail completo ou dado financeiro.',
      '',
      '---',
      '',
      '# 🚦 13. Checklist final',
      '',
      '* [ ] `data class` com `val`, mudança por `copy`',
      '* [ ] estado de tela em `sealed`, `when` exaustivo sem `else`',
      '* [ ] nenhum `!!`; `lateinit` limpo no `onDestroyView`',
      '* [ ] `StateFlow` exposto com `asStateFlow()`',
      '* [ ] nenhuma coroutine em `GlobalScope`; coleta com `repeatOnLifecycle`',
      '* [ ] `CancellationException` nunca engolida',
      '* [ ] `Dispatcher` injetado, função `suspend` main-safe',
      '* [ ] composable recebe estado e emite evento, `Modifier` último com default',
      '* [ ] efeito em `LaunchedEffect` com chave',
      '* [ ] Activity e Fragment sem regra de negócio',
      '* [ ] nenhum literal de string, cor ou dimensão na UI',
      '* [ ] nenhum segredo no APK; entrada externa validada',
      '* [ ] dinheiro sem `Double`; data com `Locale` do dispositivo',
      '* [ ] `ktlint` e a suíte passaram',
      '',
      '---',
      '',
      '# 🏁 14. Regras principais',
      '',
      '> **`val` por padrão. Mudança é `copy`.**',
      '',
      '> **Estado impossível não deve ser representável: use `sealed`.**',
      '',
      '> **`!!` é `NullPointerException` escrita à mão.**',
      '',
      '> **Coroutine sem dono é vazamento. `GlobalScope` não é dono.**',
      '',
      '> **Estado sobe, evento desce. Composable não busca dado.**'
    ].join('\n')
  },
  {
    id: 'bash-shell-essential',
    title: 'Bash + Shell',
    area: 'infra',
    depth: 'essential',
    family: 'bash-shell',
    summary: 'O essencial: o preâmbulo que não é opcional, quoting sempre, trap EXIT, o glob em vez de ls e curl sem --fail gravando a página de erro.',
    icon: 'terminal',
    tags: ['referência', 'shellcheck', 'quoting'],
    stack: ['Bash', 'POSIX sh'],
    fileName: 'bash-shell-essential.md',
    source: '~/.claude/coding-styles/bash-shell-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# 🐚 Bash + Shell — Essencial',
      '',
      '> Cartão de referência. A versão completa é `bash-shell.md`.',
      '',
      'Só as regras que mais mudam decisão em script que outras pessoas vão rodar.',
      '',
      '---',
      '',
      '# 🧱 1. O preâmbulo que não é opcional',
      '',
      '```bash',
      '#!/usr/bin/env bash',
      'set -euo pipefail',
      'IFS=$\'\\n\\t\'',
      '```',
      '',
      '* `-e` aborta no primeiro comando que falha',
      '* `-u` variável não definida é erro — é o que impede `rm -rf "$PREFIX/"` de',
      '  virar `rm -rf /`',
      '* `-o pipefail` o pipe falha se qualquer etapa falhar; sem isso',
      '  `curl ... | tar ...` "passa" quando o `curl` morre',
      '',
      '`env bash` em vez de `/bin/bash`: no macOS o `/bin/bash` é a versão 3.2 de 2007.',
      'Usando qualquer bashismo (`[[ ]]`, array, `local`), o shebang é `bash` — não',
      '`sh`.',
      '',
      '---',
      '',
      '# 🔤 2. Convenções de case',
      '',
      '| Elemento             | Padrão             | Exemplo             |',
      '| -------------------- | ------------------ | ------------------- |',
      '| Variável local       | `snake_case`       | `user_name`         |',
      '| Constante            | `UPPER_SNAKE_CASE` | `MAX_RETRY_COUNT`   |',
      '| Variável de ambiente | `UPPER_SNAKE_CASE` | `DATABASE_URL`      |',
      '| Função               | `snake_case`       | `create_user`       |',
      '| Arquivo              | `kebab-case`       | `deploy-staging.sh` |',
      '',
      'Maiúscula só para constante e ambiente: o shell não tem namespace, e maiúscula',
      'em variável local convida a sobrescrever `PATH` ou `IFS` por acidente.',
      '',
      '---',
      '',
      '# 💬 3. Quoting é a regra',
      '',
      '❌',
      '',
      '```bash',
      'rm -rf $build_dir',
      'if [ $name = "test" ]; then',
      '```',
      '',
      '✅',
      '',
      '```bash',
      'rm -rf "$build_dir"',
      'if [[ "$name" == "test" ]]; then',
      '```',
      '',
      'Sem aspas, caminho com espaço vira dois argumentos e `*` vira a lista de',
      'arquivos. `"My Documents"` sem aspas apaga `My` e `Documents`.',
      '',
      'Em array, `$*` junta e `$@` preserva:',
      '',
      '```bash',
      'command "${args[@]}"   # certo, um argumento por item',
      'command "${args[*]}"   # errado, junta tudo numa string',
      '```',
      '',
      '---',
      '',
      '# 🧪 4. `[[ ]]` para texto, `(( ))` para número',
      '',
      '```bash',
      'if [[ -f "$config" && -r "$config" ]]; then',
      'if [[ "$branch" == feature/* ]]; then',
      'if (( attempts > MAX_RETRY_COUNT )); then',
      '```',
      '',
      '`[[ ]]` não faz divisão de palavras e entende `&&`, `||` e `=~`.',
      '',
      '---',
      '',
      '# 🧯 5. `mktemp` mais `trap`',
      '',
      '```bash',
      'temp_dir="$(mktemp -d)"',
      'trap \'rm -rf "$temp_dir"\' EXIT',
      '```',
      '',
      '`EXIT` cobre saída normal, `set -e` e `Ctrl+C`. Sem `trap`, todo caminho de erro',
      'precisa lembrar de limpar — e um deles não vai. Nome fixo em `/tmp` é falha de',
      'segurança e quebra duas execuções simultâneas.',
      '',
      '---',
      '',
      '# 🚫 6. Nunca parseie `ls`',
      '',
      '❌',
      '',
      '```bash',
      'for file in $(ls *.txt); do',
      '```',
      '',
      '✅',
      '',
      '```bash',
      'for file in *.txt; do',
      '  [[ -e "$file" ]] || continue',
      '  process "$file"',
      'done',
      '```',
      '',
      'O glob já é a lista e sobrevive a espaço no nome. O `|| continue` cobre o caso',
      'de nenhum arquivo casar — sem ele o laço roda uma vez com o literal `*.txt`.',
      '',
      'Para árvore, `find -print0`:',
      '',
      '```bash',
      'while IFS= read -r -d \'\' file; do',
      '  process "$file"',
      'done < <(find . -name \'*.log\' -print0)',
      '```',
      '',
      '`IFS= read -r` é o par que preserva espaço e barra invertida.',
      '',
      '---',
      '',
      '# 📤 7. `local` e retorno',
      '',
      '```bash',
      'create_user() {',
      '  local name="$1"',
      '',
      '  printf \'%s\\n\' "$name"',
      '}',
      '```',
      '',
      'Toda variável de função é `local`, senão é global. `return` devolve **status**',
      '(0–255), não valor — valor sai pelo stdout e é capturado com `$(...)`.',
      '',
      '`local var="$(cmd)"` esconde o status do `cmd`. Quando o status importa, declare',
      'e atribua em linhas separadas.',
      '',
      '---',
      '',
      '# 🖨️ 8. `printf`, não `echo`',
      '',
      '`echo` com conteúdo variável interpreta `-n`, `-e` e barra invertida de forma',
      'diferente entre shells.',
      '',
      '```bash',
      'printf \'%s\\n\' "$user_input"',
      '',
      '_die() {',
      '  printf \'error: %s\\n\' "$1" >&2',
      '  exit 1',
      '}',
      '```',
      '',
      'Dado no stdout, diagnóstico no stderr — é o que permite `script.sh > data.txt`',
      'continuar mostrando os erros.',
      '',
      '---',
      '',
      '# 🔀 9. Default e expansão',
      '',
      '```bash',
      ': "${ENVIRONMENT:=staging}"        # define se não vier',
      'readonly TIMEOUT="${TIMEOUT:-30}"  # usa default sem definir',
      '"${OPTIONAL:-}"                    # ler opcional sob set -u',
      '',
      '"${path%/*}"      # dirname',
      '"${path##*/}"     # basename',
      '```',
      '',
      '`$(basename ...)` funciona, mas cria um processo por chamada dentro de laço.',
      'Heredoc quotado (`<<\'EOF\'`) para texto literal: sem as aspas, `$HOME` na ajuda',
      'vira o caminho.',
      '',
      '---',
      '',
      '# 🌐 10. Dependência, rede e portabilidade',
      '',
      '```bash',
      'for cmd in curl jq git; do',
      '  command -v "$cmd" >/dev/null || _die "$cmd is required"',
      'done',
      '',
      'curl --fail --show-error --silent --location --retry 3 --max-time 30 \\',
      '  --output "$temp_file" "$url"',
      '```',
      '',
      '`command -v`, não `which`. `--fail` é o que faz o `curl` retornar erro em HTTP',
      '404 — sem ele o script grava a página de erro e segue como se tivesse dado',
      'certo. `--max-time` impede pendurar no CI.',
      '',
      'Diferenças que quebram script: `sed -i` precisa de `\'\'` no macOS, `readlink -f`',
      'e `date -d` são GNU, `grep -P` não existe em toda instalação.',
      '',
      '---',
      '',
      '# 🔐 11. Segredos e destruição',
      '',
      'Segredo nunca no script, nunca em log, nunca sob `set -x` — um `echo "$TOKEN"`',
      'no CI fica no log do job para sempre. Token na linha de comando aparece no `ps`.',
      '',
      '```bash',
      '[[ -n "${build_dir:-}" && "$build_dir" != "/" ]] || _die \'refusing to remove\'',
      'rm -rf -- "${build_dir:?}"',
      '```',
      '',
      'O `--` encerra as opções (sem ele um arquivo chamado `-rf` é lido como flag); o',
      '`:?` aborta se a variável estiver vazia.',
      '',
      '---',
      '',
      '# 🔁 12. Idempotência',
      '',
      '```bash',
      'mkdir -p "$config_dir"',
      'ln -sfn "$target" "$link"',
      'grep -qxF "$line" "$file" || printf \'%s\\n\' "$line" >> "$file"',
      '```',
      '',
      '`>>` sem verificação duplica a linha a cada execução — é assim que um `.bashrc`',
      'acumula quinze `export PATH`.',
      '',
      '---',
      '',
      '# 🧪 13. Verificação',
      '',
      '```text',
      'shellcheck script.sh      lint — quoting, $?, subshell, uso de ls',
      'shfmt -d -i 2 -ci         formatação',
      'bash -n script.sh         sintaxe, sem executar',
      '```',
      '',
      '`shellcheck` no CI com aviso como erro: ele acha a classe de bug que só aparece',
      'com nome de arquivo estranho, que é o que ninguém testa à mão. Supressão sempre',
      'justificada e localizada.',
      '',
      'Indentação de 2 espaços, nunca tab.',
      '',
      '---',
      '',
      '# 🚦 14. Checklist final',
      '',
      '* [ ] `#!/usr/bin/env bash` e `set -euo pipefail`',
      '* [ ] bashismo só com shebang `bash`',
      '* [ ] toda expansão entre aspas; `"${array[@]}"`, nunca `[*]`',
      '* [ ] `[[ ]]` para texto, `(( ))` para número',
      '* [ ] `mktemp` e `trap ... EXIT`',
      '* [ ] nenhum parse de `ls`; `IFS= read -r -d \'\'` com `find -print0`',
      '* [ ] toda variável de função é `local`',
      '* [ ] `printf` no lugar de `echo`; diagnóstico em stderr',
      '* [ ] dependência verificada com `command -v`',
      '* [ ] `curl` com `--fail` e `--max-time`',
      '* [ ] nenhum segredo no código, em log ou sob `set -x`',
      '* [ ] `rm -rf` com `--`, `:?` e guarda contra raiz',
      '* [ ] setup idempotente',
      '* [ ] `shellcheck` e `shfmt` passaram',
      '',
      '---',
      '',
      '# 🏁 15. Regras principais',
      '',
      '> **`set -euo pipefail` na primeira linha útil. Sempre.**',
      '',
      '> **Aspas em toda expansão. O caminho com espaço existe.**',
      '',
      '> **`mktemp` mais `trap EXIT` — o caminho de erro não lembra de limpar.**',
      '',
      '> **Nunca parseie `ls`. O glob já é a lista.**',
      '',
      '> **`curl` sem `--fail` grava a página de erro e segue felizmente.**'
    ].join('\n')
  },
  {
    id: 'docker-ci-essential',
    title: 'Docker + CI',
    area: 'infra',
    depth: 'essential',
    family: 'docker-ci',
    summary: 'O essencial: multi-stage, cache que não invalida à toa, nenhum latest, não-root, segredo fora da imagem e a mesma imagem em todos os ambientes.',
    icon: 'cloud',
    tags: ['referência', 'dockerfile', 'ci'],
    stack: ['Docker', 'Compose', 'GitHub Actions'],
    fileName: 'docker-ci-essential.md',
    source: '~/.claude/coding-styles/docker-ci-essential.md',
    updatedAt: '2026-09-10',
    content: [
      '# 🐳 Docker + CI — Essencial',
      '',
      '> Cartão de referência. A versão completa é `docker-ci.md`.',
      '',
      'Só as regras que mais mudam decisão em Dockerfile, Compose e pipeline.',
      '',
      '---',
      '',
      '# 🧱 1. Multi-stage é o padrão',
      '',
      '```dockerfile',
      'FROM node:22.14.0-alpine AS build',
      'WORKDIR /app',
      'COPY package*.json ./',
      'RUN npm ci',
      'COPY . .',
      'RUN npm run build',
      '',
      'FROM node:22.14.0-alpine AS runtime',
      'WORKDIR /app',
      'ENV NODE_ENV=production',
      'COPY package*.json ./',
      'RUN npm ci --omit=dev && npm cache clean --force',
      'COPY --from=build /app/dist ./dist',
      'USER node',
      'CMD ["node", "dist/main.js"]',
      '```',
      '',
      'Estágio único vai com `devDependencies`, `.git`, cache de build e o gerenciador',
      'de pacotes inteiro na imagem final.',
      '',
      '---',
      '',
      '# 🔢 2. Ordem de camada pelo que muda menos',
      '',
      '❌',
      '',
      '```dockerfile',
      'COPY . .',
      'RUN npm ci',
      '```',
      '',
      '✅',
      '',
      '```dockerfile',
      'COPY package*.json ./',
      'RUN npm ci',
      'COPY . .',
      '```',
      '',
      'Copiar tudo antes de instalar invalida o cache de dependência a cada alteração',
      'de qualquer arquivo — o build volta a levar minutos por uma vírgula no README.',
      '',
      '`RUN` relacionados num só comando, com a limpeza no **mesmo** `RUN`:',
      '',
      '```dockerfile',
      'RUN apt-get update \\',
      '  && apt-get install --no-install-recommends -y ca-certificates curl \\',
      '  && rm -rf /var/lib/apt/lists/*',
      '```',
      '',
      '`update` numa camada e `install` em outra é o clássico: o `update` fica em cache',
      'e o `install` busca índice velho. Apagar em camada seguinte não reduz a imagem,',
      'só esconde o conteúdo.',
      '',
      '---',
      '',
      '# 📌 3. Pin de versão',
      '',
      'Nada de `latest`, nem na base, nem em action de CI.',
      '',
      '```dockerfile',
      'FROM node:22.14.0-alpine',
      '```',
      '',
      '`latest` faz o build de hoje e o de amanhã produzirem imagens diferentes com o',
      'mesmo Dockerfile — e o de amanhã é o que quebra. Digest é o que dá build',
      'reproduzível de verdade. `uses: actions/checkout@v4` é aceitável; `@main` não é.',
      '',
      '---',
      '',
      '# 🔐 4. Usuário não-root',
      '',
      '```dockerfile',
      'USER node',
      '```',
      '',
      'Container roda como root por padrão. `USER` vem depois do que precisa de',
      'privilégio e **antes** do `CMD`. Com volume montado, processo root no container',
      'escreve como root no host.',
      '',
      '---',
      '',
      '# 🚫 5. `.dockerignore` não é opcional',
      '',
      '```text',
      '.git',
      'node_modules',
      'dist',
      '*.log',
      '.env',
      '.env.*',
      '```',
      '',
      'Sem ele, o contexto sobe `node_modules`, `.git` e `.env`: build lento, cache',
      'invalidado por arquivo irrelevante e **segredo dentro da imagem**. Um `.env`',
      'copiado por `COPY . .` fica na camada para sempre, mesmo com `RUN rm` depois.',
      '',
      '---',
      '',
      '# 🔑 6. Segredo nunca em camada',
      '',
      '❌',
      '',
      '```dockerfile',
      'ARG NPM_TOKEN',
      'RUN echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > .npmrc && npm ci',
      '```',
      '',
      '`docker history` mostra: quem tem a imagem tem o token. `ENV` é pior — fica em',
      'toda camada seguinte e em `docker inspect`.',
      '',
      '✅',
      '',
      '```dockerfile',
      'RUN --mount=type=secret,id=npm_token \\',
      '  NPM_TOKEN="$(cat /run/secrets/npm_token)" npm ci',
      '```',
      '',
      'Em runtime, segredo entra por ambiente injetado ou arquivo montado.',
      '',
      '---',
      '',
      '# 🎯 7. `CMD` na forma exec',
      '',
      '❌',
      '',
      '```dockerfile',
      'CMD node dist/main.js',
      '```',
      '',
      'Roda sob `/bin/sh -c`, que **não repassa `SIGTERM`**: o container não faz',
      'shutdown limpo e morre no timeout do `docker stop`.',
      '',
      '✅',
      '',
      '```dockerfile',
      'CMD ["node", "dist/main.js"]',
      '```',
      '',
      'A aplicação trata `SIGTERM`: para de aceitar conexão nova, termina o que está em',
      'voo e sai. Sem isso, todo deploy derruba requisição no meio.',
      '',
      '---',
      '',
      '# 🩺 8. `HEALTHCHECK` e `COPY`',
      '',
      '```dockerfile',
      'HEALTHCHECK --interval=30s --timeout=3s --start-period=10s --retries=3 \\',
      '  CMD curl --fail --silent http://localhost:3000/health || exit 1',
      '',
      'COPY --from=build --chown=node:node /app/dist ./dist',
      '```',
      '',
      '`--start-period` evita reinício em loop enquanto a aplicação sobe. Use `COPY`,',
      'não `ADD` — `ADD` baixa URL e extrai tarball, comportamento implícito que',
      'ninguém espera. `--chown` no próprio `COPY`, para não duplicar a camada.',
      '',
      '---',
      '',
      '# 🏷️ 9. Uma imagem, todos os ambientes',
      '',
      'Configuração vem do ambiente, não de arquivo dentro da imagem: a **mesma**',
      'imagem serve staging e produção.',
      '',
      'Uma imagem por commit, com tag rastreável e **nunca** sobrescrita:',
      '',
      '```text',
      'registry/app:1.4.2',
      'registry/app:sha-9f3c1ab',
      '```',
      '',
      '`latest` em deploy é irrastreável. Promoção entre ambientes é a mesma imagem com',
      'outra configuração — rebuild para promover significa artefatos diferentes em',
      'staging e produção.',
      '',
      '---',
      '',
      '# 🐙 10. Compose para desenvolvimento',
      '',
      '```yaml',
      'services:',
      '  api:',
      '    build: { context: ., target: build }',
      '    depends_on:',
      '      db:',
      '        condition: service_healthy',
      '',
      '  db:',
      '    image: postgres:17.2-alpine',
      '    healthcheck:',
      '      test: ["CMD-SHELL", "pg_isready -U app"]',
      '      interval: 5s',
      '      retries: 10',
      '    volumes:',
      '      - db-data:/var/lib/postgresql/data',
      '',
      'volumes:',
      '  db-data:',
      '```',
      '',
      '`depends_on` simples só espera o container **iniciar**, não ficar pronto —',
      '`condition: service_healthy` é o que evita a aplicação subir antes do banco.',
      'Sem `version:` (obsoleto), volume nomeado para dado, nenhum segredo real',
      'versionado.',
      '',
      '---',
      '',
      '# ⚙️ 11. CI: job único e permissão mínima',
      '',
      'Um job por responsabilidade: `lint`, `test`, `build`, `scan`, `deploy`. `needs`',
      'declara dependência real, não ordem estética. Falha rápida: `lint` antes do teste',
      'de integração.',
      '',
      '❌',
      '',
      '```yaml',
      'permissions: write-all',
      '```',
      '',
      '✅',
      '',
      '```yaml',
      'permissions:',
      '  contents: read',
      '',
      'jobs:',
      '  publish:',
      '    permissions:',
      '      contents: read',
      '      packages: write',
      '```',
      '',
      'O token do CI é credencial de produção. Nunca rode workflow com segredo em',
      '`pull_request_target` disparado por fork sem revisão.',
      '',
      '---',
      '',
      '# 💾 12. CI: cache e reprodutibilidade',
      '',
      '```yaml',
      'key: npm-${{ hashFiles(\'package-lock.json\') }}',
      'restore-keys: npm-',
      '```',
      '',
      'Chave derivada do lock. Chave fixa serve cache velho para sempre. Cache é',
      'otimização: o build tem de funcionar frio.',
      '',
      '* `npm ci`, não `npm install` — respeita o lock e falha se ele divergir',
      '* runtime pinado no workflow, igual ao da imagem',
      '* teste sem rede externa; dependência sobe como container',
      '* artefato construído **uma vez** e reutilizado pelos jobs seguintes',
      '',
      '---',
      '',
      '# 🛡️ 13. Verificação e log',
      '',
      '```text',
      'hadolint Dockerfile         lint de Dockerfile',
      'docker build --no-cache     prova que o build funciona do zero',
      'trivy image registry/app    CVE de sistema e de aplicação',
      'docker history --no-trunc   confere que não há segredo em camada',
      '```',
      '',
      'Aplicação em container escreve log em **stdout/stderr**, nunca em arquivo dentro',
      'do container: o coletor lê da saída padrão, e arquivo em camada de escrita cresce',
      'até encher o disco do nó. Nada sensível no log.',
      '',
      '---',
      '',
      '# 🚦 14. Checklist final',
      '',
      '* [ ] multi-stage: runtime sem compilador e sem código-fonte',
      '* [ ] dependência instalada antes de copiar o código',
      '* [ ] `RUN` relacionados juntos, limpeza no mesmo comando',
      '* [ ] versão pinada; nenhum `latest`',
      '* [ ] `USER` não-root antes do `CMD`',
      '* [ ] `.dockerignore` cobrindo `.git`, `node_modules` e `.env`',
      '* [ ] nenhum segredo em `ARG`, `ENV` ou camada',
      '* [ ] `CMD` na forma exec; `SIGTERM` tratado',
      '* [ ] `HEALTHCHECK` com `--start-period`; `COPY` em vez de `ADD`',
      '* [ ] mesma imagem em todos os ambientes; config do ambiente',
      '* [ ] tag por commit, nunca sobrescrita',
      '* [ ] `permissions` mínimas; `needs` real',
      '* [ ] cache com chave do lock; build funciona frio',
      '* [ ] `npm ci`; artefato construído uma vez',
      '* [ ] log em stdout/stderr',
      '* [ ] `hadolint` e varredura de CVE no pipeline',
      '',
      '---',
      '',
      '# 🏁 15. Regras principais',
      '',
      '> **Ordem de camada é do que muda menos para o que muda mais.**',
      '',
      '> **`latest` faz o build de amanhã ser outro. E é o de amanhã que quebra.**',
      '',
      '> **Segredo em `ARG` ou `ENV` está na imagem para sempre.**',
      '',
      '> **`CMD` na forma shell não recebe `SIGTERM`.**',
      '',
      '> **A mesma imagem serve todos os ambientes.**'
    ].join('\n')
  }
]);
